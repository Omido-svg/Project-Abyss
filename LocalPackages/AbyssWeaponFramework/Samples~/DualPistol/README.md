# Dual Pistol Sample

Pistol prefab: `WeaponInstance + Grip_Main + HitscanWeaponModule + WeaponAttackController`.

Character: LeftHand / RightHand sockets.

The same `WeaponDefinition` is instantiated twice with different instance keys and explicit socket bindings. This is the intended representation for dual wielding.
