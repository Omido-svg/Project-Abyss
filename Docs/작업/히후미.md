# 히후미

## 결정 대기 (구조)
- **반격 굴림에서 대실패(1 · 2 · 3)가 나왔을 때** — 자해 60 · 뼈 60 · 반격 굴림 +1이 또 붙는가(무한 연쇄 가능성), 아니면 값 0인가. 시뮬 v3은 값 0
- **✅ [09-16 해소] 짓눌림 대응 재설계** — **A안(그대로 둔다) 채택.** 구 반감(짓눌림 시 받는 피해 1/2)은 발악이 캐릭터 공용으로 통일되기 전의 히후미 전용 패치였다. 발악이 5.4에서 전 캐릭터 공용(짓눌림 종료 → 다음 턴 판정값 +1)으로 재설계된 이상, 그 자리를 다시 캐릭터 전용으로 쪼개는 건 통일 원칙과 충돌한다. 반감 소실로 짓눌림 중 받는 피해가 실질 2배가 된 것은 맞지만, 그건 밸런스(절대 수치) 문제지 새 장치가 필요한 구조적 공백이 아니다 — 지금 국면(콘텐츠 우선·승률 절대값 판단 보류)에서 다룰 계층 아님. 패시브(19.4, 고조 +3)는 별개 축(열광)이라 이 결정과 무관하게 그대로 유지
- **이관 카드 셋을 히후미 색으로 감쌀지**
  - 빚지기(대여형 에너지 회복: 승리 시 빛 +1 즉시, 다음 턴 최대치 −1) — 올라프에서 이관
  - 체력 → 흐트러짐 전환 도사림(올라프 「흐트러짐을 삼키다」의 반대 방향) — 비율 미정
  - 「모든 것을 걸다」(도박형, 유진에서 이관 후보) — 스킬 또는 직업 아이템
- 「수비 굴림을 쓰지 않는다」 지침 — RED·BLUE 후속.md
- 스킬 색(RED/BLUE) 전부 미배정

## 수치 대기
- **평타① 「푼돈 걸기」 기본위력 12 → 13?** — 09-09 「평타도 곡선」 개정을 적용하면 곡선 12 + 빛 0 + 히후미 1 = 13. 표의 12는 구 평타 사다리 값
- 자기 거버너 — 「다음 턴 결투 위력 감소」의 감소량. 시뮬 v3은 임의값
- 도사림 C 「살은 내주지」 — 「다음 턴 받는 피해 증가」의 증가량
- 위세 2형 「올인」 — 배당(최고 결과에 따라 뼈가 어떻게 되는가)이 없다. 시뮬 v3은 「최고 18 이상이면 뼈 2배, 대실패 하나면 0」으로 근사
- 평타② 「과감한 판단」의 「완전 차단」 정의 — 시뮬 v3은 근사

## 풀 확충
- **✅ 육참골단 트리 완결(09-16) — 결투 D 「명줄을 걸다」 추가.** 3굴림·**태그 없음**(자기 반격을 새로 만들지 않는다), 조건: 뼈 100 이상(미만이면 카드 자체를 못 낸다 — 장부 정리 · 최후의 저항과 같은 종류의 사용 조건 게이트). 사용시 뼈(소모량 미정 — 윈도우 계산 대기) 소모 → **이번 합에 쌓인 반격 굴림 수만큼 친치로를 한 번에 굴림**(위세 2형 「올인」과 같은 방식) → 하나라도 시고로 · 아라시면 **골단(B)의 만개 반격을 그대로(축소 없이) 즉시 발동**(뼈 500 문턱 우회) — 유진 낙일 「때를 앞당기다」와 같은 자원→시점 전환형이라 결과 재사용이 의도된 설계. 성공 · 실패 무관 반격 굴림 풀 소진, 실패해도 추가 페널티 없음(소모한 뼈만 소실)
  - **설계 경위**: 처음 "확정 발동"안(때를 앞당기다 초기안과 같은 문제 — 너무 강력해 아무도 안 쓸 것 같다는 지적)에서 확률형 도박으로 절충 → (반격) 태그를 붙였다가 "그럼 A · B처럼 자기 반격 효과가 따로 있어야 하는데 정의가 없다" 지적으로 **태그 제거, 공용 반격 풀을 빌려 쓰는 카드로 재정의** → "성공 보상을 골단 만개보다 축소(순수 데미지만)해야 하나" 검토했으나 "그러면 평타① 반격이랑 카테고리가 안 갈린다"는 지적으로 **축소 없이 그대로 재사용, 차별점은 크기가 아니라 시점(조기 발동)과 대가(게이트·소모·확률)** 로 확정. 낙일 때를 앞당기다 선례와 같은 패턴
  - 엔진 구현 필요: (1) 뼈 하한 미달 시 카드 자체를 못 내는 게이트(장부 정리와 공유 가능한 신규 정책) (2) 「이번 합 반격 굴림 풀 수만큼 다이스 동시 굴림 → 시고로/아라시 포함 여부 판정 → 골단 만개 반격 강제 발동(태그 없이 실행) → 풀 소진」 처리 — 위세 2형 올인의 다중 굴림 로직 재사용 가능
- **✅ 골단(B)에 부가효과 추가(09-16)** — **만개 시 반격 굴림 수 ×2.** 명줄을 걸다가 골단의 만개 반격을 강제 발동시킬 때도 이 배율이 걸려서, 도박이 성공하면 반격 자체도 더 커지는 콤보가 생김
- **✅ 과부화 트리 진행(09-16) — 결투 E 「판돈을 올리다」 추가.** 태그 없음, **교환 승리시** 위력 +1(전투 내내 누적·전투 종료 시 리셋) · **교환 패배시** 빛 비용 +1(전투 내내 누적) → 비용 4 도달 시 카드 자체를 못 낸다. 처음 안(패배시 위력 0 리셋)을 사용자가 "비용 증가 또는 완전 봉쇄"로 대체 제안 → **둘을 섞어서 확정**(비용 누적형 페널티 + 상한 도달 시 봉쇄). 굴림 · 빛 · 위력 초기값 · 색 전부 미정(윈도우 계산 대기)
  - 엔진 구현 필요: 「카드별 전투 내 영구 위력 누적 카운터」 · 「카드별 전투 내 영구 빛 비용 누적 카운터 + 상한 도달 시 사용 불가 게이트」 — 둘 다 신규(기존 크기형 버프는 1턴 고정이라 재사용 불가, 이건 "전투 종료까지" 스코프)
- **✅ 동귀어진 → 평타로 이적(09-16)** — 결투 슬롯으로 브레인스토밍했으나 "뼈 스케일링은 만개(회복+유틸)보다 항상 약하고, 고정 데미지는 결투 한 자리 값으로는 임팩트가 부족하다"는 지적으로 폐기 검토 → **평타 ③으로 이적, "자기 피해로 뼈를 벌면서 동시에 상대에게도 같은 고정 피해"로 재정의.** 자기 피해=상대 피해 동일값으로 확정(비대칭 검토했으나 대칭으로 확정). 판정 없는 확정형이라 "육참골단"(살을 주고 뼈를 취한다) 컨셉을 평타 단계에서도 구현
  - 엔진 구현 필요: 사용시 자기 피해 이벤트를 뼈 적립 규칙(19.2, 받은 피해 1:1)에 정상적으로 태우는지 확인 — 신규 코드 불필요할 가능성(기존 피해→뼈 파이프라인 재사용)할 수도 있음, 값 확정 후 점검
- **✅ 과부화 트리 완결(09-16) — 결투 F 「본전을 챙기다」 추가.** 태그 없음, **사용시** 이번 판정을 포기(자동 패배 처리) → **다음 턴 이 카드 위력 확정 대폭 증가**. 확률·조건 분기 없는 결정론적 시간 교환 — C(뼈 문턱 조건 분기)·E(전적 누적 리스크)는 둘 다 위험 감수형인데, F는 확정 손해 ↔ 확정 이득을 맞바꾸는 세 번째 시간 구조라 안 겹침
  - **경위**: 처음 "무리를 하다"(굴림 +1, 다음 턴 고통)를 제안했으나, (1) 굴림 수를 판정 도중 늘리면 상대 잔여 굴림과의 매칭 방식이 애매해지는 시스템 문제(골절 −1의 "마지막 굴림 삭제" 미해결 이슈와도 얽힘)로 굴림 조작 자체를 포기 → 위력 증가로 대체했으나 (2) "지금 위력↑·다음 턴 디버프"가 이미 C의 기존 얼개라 신선함이 없다는 지적 → **시간축 자체를 바꿔 확정 교환형으로 재설계**
  - 빛 · 위력 · 굴림 수 전부 미정(윈도우 계산 대기)
- **무난 트리 브레인스토밍 착수(09-16)** — 히후미 카드 전체를 보면 **적에게 직접 상태이상을 거는 카드가 아예 없음**(디버프는 전부 자기 자신 대상)이 큰 빈틈으로 확인됨. 후보: 표준 안정딜 / 직접 디버프(약화·골절) / 안정 방어형 / 속도·판정값형
- **✅ 결투 G 「곱절을 노리다」(임시명) 추가** — 앞서 과부화 자리로 검토했다가 "즉각적 판단형이 아니라 순수 확률 도박이라 과부화엔 안 맞는다"고 폐기했던 아이디어를 **무난으로 이적**. 태그 없음, **매 굴림마다** 뼈 K 배팅 → 승리시 뼈 2K(순이익 +K) / 패배시 K 소실, 굴림 단위 독립 정산. 판돈을 올리다(E, 전투 내내 지속)와 달리 **이 카드 한 번 쓰는 동안만 도는 순간 변동성**이라 안 겹침. 이름은 임시(나중에 일괄 개명 예정), 수치 전부 미정
- **✅ 결투 H 「운명을 흔들다」(임시명) 추가(09-16)** — 사용자가 "다른 캐릭터(올라프)엔 컨셉에 맞는 공용 룰 브레이커가 있는데 히후미는 상대적으로 약하다"고 지적해서 진짜 룰 브레이커로 설계. 처음 "상대 굴림을 훔쳐온다"안을 냈으나 (1) 적 RNG는 올라프식 균등 주사위라 친치로(카테고리형)와 분포가 달라 값 변환이 지저분함 → "판정 방식 자체를 강탈(내 친치로로만 승패 결정)"로 전환 → (2) "위력만 훔쳐서 다음 굴림에 얹는" 안도 검토했으나 "위력은 합에서 이기려고 쓰는 건데 해석 끝난 뒤 훔쳐봐야 의미 없다(그냥 위력 증가의 하위호환)"는 지적으로 기각 → **최종: 굴림 1개(단발) 카드, 승패를 위력 비교 대신 친치로 (미정)회 굴려 하나라도 시고로·아라시면 승리(위세 2형 올인 로직 재사용) / 아니면 패배. 승리 시 상대의 이번 합 남은 굴림 전부 무효화.** 위세 1(도박수, 뼈 전량 태워 전 슬롯 위력+6)과 시너지 — 승패를 도박에 맡기고 위력은 순수 데미지 배율로만 쓰게 되어, 위세 1로 위력을 몰빵해도 승패 리스크가 없어짐
  - 게이트·비용·확률 굴림 횟수 전부 미정(윈도우 계산 대기)
  - 엔진 구현 필요: 「위력 비교를 건너뛰고 친치로 N회 중 시고로/아라시 여부로 승패 강제 판정」(위세 2형 올인 로직 재사용 가능) · 「승리 시 상대 잔여 굴림 전체 무효화」(신규 — 지금까지 없던 처리)
- **✅✅ 결투 I 「몸에 새기다」(임시명) 추가 — 결투 풀 9/9 완결(09-16)** — "모든 것을 걸다"(유진 브레인스토밍 때 이름만 나오고 실제 설계는 없던 도박형 이관 후보, 히후미 숙제로만 남아있던 항목)를 채우려다가, 위세 2형(올인)과 "전부 걸기" 컨셉이 겹쳐서 폐기 → **런 전체에 걸쳐 영구 성장하는 카드**로 완전히 새 방향 전환. 시작은 낮은 위력·적은 굴림 수 → 사용할 때마다 일정 확률로 단련(위력 영구 소폭 상승) → 위력이 임계치에 도달하면 굴림 +1·위력 리셋 → 총 4~5굴림 도달 시 완전히 다른 최종형 스킬로 진화(효과 미설계)
  - **⭐ 신규 카드 카테고리 — "훈련형 영구 성장 카드"**: 지금까지 이 프로젝트의 모든 스킬 효과는 "전투 동안 영구"까지만 있었고(11.1 크기형 등), **전투를 넘어 런 전체에 걸쳐 지속되는 카드 성장은 이게 최초**. 굴림 수 변경이 전투 시작 전(레벨업 시점)에 확정되므로 "무리를 하다"에서 걸렸던 "판정 도중 굴림 수 변경" 시스템 문제를 구조적으로 피해감
  - **성장 트리거는 캐릭터마다 다르게 갈 수 있음(사용자 제안, 09-16)** — 히후미는 확률형(사용시 일정 확률로 단련, 그녀의 친치로/도박 정체성과 자연히 맞물림)으로 확정. 다른 캐릭터에 같은 카드 종류를 줄지, 준다면 트리거를 뭘로 할지(고정 횟수형 · 처치시형 등, 환세취호전의 "사용할 때마다 확률로 기술 강화" 참고 사례 언급됨)는 **별도 구조 결정 — 아직 미정, 다음에 다룰 것**
  - **✅ 최종형 굴림 수 확정(09-17) — 4굴림.** 3~4 중 사용자가 4로 결정
  - **✅ 최종형 컨셉 확정(09-17) — 가칭 "체득".** 방향 A(아픔을 졸업한 형태) 채택 — (반격) 태그 제거, **절단** 타입 유지(육참골단 트리 소속). 위력은 **골단 만개 반격의 위력 수치를 그대로 채용**하되 만개의 라이더 효과(회복 350·즉시 약화·조건부 파괴·바+25)는 안 가져옴(사용자: "위력만 가져오고 효과는 안 주려고 함") — 골단의 실제 유틸리티(파괴 권한·회복)는 그대로 골단 전용이라 하위/상위호환 문제 없음
  - **✅ 사용 조건 확정(09-17) — 뼈 N 이상 게이트**(명줄을 걸다와 같은 패턴, 미달이면 카드 자체를 못 냄. 사용시 뼈 N 소모). 부가 효과(승리시 뼈 획득 등)는 안 넣기로 — 굴림 4개·골단급 위력만으로 이미 메리트 충분(사용자)
  - ⭐ **다른 캐릭터 훈련형 성장 카드는 히후미와 다르게 간다** — A(아픔을 졸업) 서사는 히후미 전용, 올라프·유진에 같은 카드를 줄지 · 준다면 어떤 서사로 갈지는 별도 결정(사용자, 09-17)
  - ⭐⭐ **성장형 영구 카드 = 캐릭터마다의 "후회"를 담은 스킬 (09-17, 카테고리 전체 설계 원칙)** — 히후미는 원래 골단(육참골단의 힘)을 자유자재로 쓸 수 있었으나 도박에 빠지며 전성기를 잃었고, 그 대가로 (반격) 태그(맞아야만 쓸 수 있음)가 생겼다는 서사로 확정. 체득(최종형)에서 태그가 빠지는 게 "도박에 빠지기 전 전성기의 자신을 되찾는" 순간 — 단순 "성장"이 아니라 **"잃어버린 것을 되찾는다"**는 무게. 올라프·유진도 성장 카드를 만들 때 각자의 "후회"를 서사 축으로 잡을 것(실제 설계는 보류, 원칙만 기록)
  - **✅ 단계 구성 확정(09-17)** — 1→2→3→4굴림 4단계(4단계=체득). 전 단계 절단 타입 고정, 1~3단계 패배시(반격) 유지, 체득만 태그 제거
  - **✅ "단련"도 패배시(반격)과 같은 트리거에 얹음** — 이겨서 크는 게 아니라 **져서 크는** 구조. "몸에 새기다"(아픔을 몸에 새겨 성장) 서사와 (반격) 태그가 같은 순간에 맞물림. 체득에서 태그가 빠지는 게 "더 이상 아픔이 필요 없어진" 졸업 지점
  - **✅ 빛 비용 확정(09-17)** — **1·2단계 빛0 / 3단계·체득 빛1**(소극적 안 채택 — 사용자: "빛이랑 소모된 시간 대비 성능이 안 나오면 효과를 더 달아주는 식으로"). 처음엔 전 단계 빛0(런 내내 공짜 — "이건 자원이 아니라 그녀의 몸이다" 서사)를 제안했으나 사용자가 반려, 단계가 오를수록 비용이 붙는 쪽으로. 성능 미달 시 추가 효과를 얹는 걸 전제로 지금은 최소 구성만
  - 각 단계 위력 = 그 굴림 수의 공식값(곡선)을 그대로 쓰고, "단련"이 그 위에 영구 소폭 보정을 얹는 구조(합의됨, 정확한 값은 미정)
  - 남은 미정 — 확률(단련 성공률)·단계별 임계치·게이트 뼈량 N(체득)·위력 수치·필요시 추가 효과. 전부 윈도우 계산 대기
  - 엔진 구현 필요: 「카드별 런 전체 영구 상태(현재 단계·현재 단계 내 위력·사용 횟수)를 세이브에 저장」 — 완전히 새로운 저장 범위(지금까지 전투 밖에 남는 건 아이템 · 골드 · 최대체력뿐, 스킬 자체 상태가 런 레벨로 영속하는 건 처음)
- 결투 트리 — **전 트리 완결(9/9)**
- ⚠⚠ **이하 「⚠임시값(09-18)」 표시는 전부 미정이다 — 자리만 채운 값.** 구조(트리거·조건·효과 종류)는 ✅로 확정이지만, 숫자(위력·빛·N값·확률·문턱)는 실제 계산·밸런싱을 윈도우 세션(Excel VBA 시뮬)에서 해야 정본이 된다. 규칙서 · 스펙 엑셀에 옮길 때 이 값을 그대로 베끼지 말 것. **표기는 09-21 규칙서 11장(N·T 문법)에 맞춰 정리함** — 상태이상 부여 = `상태 N만큼 T턴 부여`(줄임형 `힘 2·3`), 값 없는 지속형 = `상태 T턴 부여`. 원문에 T가 없던 곳은 기본 1턴(즉시 적용)으로 채우고 `⚠임시값(09-21 표기 정리)`로 표시. **도발 · 은신 · 핏값(가칭)은 정본 11장에 미등록** — 아래 각 항목에 각주. **슬롯 잠금은 09-21부터 「봉인」**(구 「과부화」 폐기, 공용 상태이상 아님)
- **평타 풀 확충 착수(09-17)** — 목표 9장, 구성 = 반격 4 · 뼈수급 2 · 정화 1(①③=기존, ②=기존)
  - **✅ ⑤ 안정형 필러 추가** — 반격(패배시), 빛1·3굴림·절단1·타격2·효과 없음, 대박 없이 폭을 좁혀 "저비용·거친 변동성"인 ①푼돈 걸기(빛0·2굴림·타격2·대박 있음)와 텍스처만 다른 필러. 평타라 굳이 큰 차별화 없이도 채택(사용자 — "그렇게 큰 효과는 아니여도 됨")
  - **✅ ⑦ 정화 A 추가** — 빛1·2굴림·타격 위주, 사용시 뼈 N 소모 → 자신에게 걸린 **부정적 효과 종류 중 무작위 1종을 골라 그 종류의 살아 있는 항목 전부 제거** / 지울 부정적 효과가 없으면 대신 뼈 지급. 부위형 상태는 정화 대상이 아니다. 완전 차단시 환급안은 ②과감한 판단과 조건이 겹쳐 기각. 반격 트리(①⑤)가 뼈를 쌓고 이 카드가 깎아 쓰는 순환 축(뼈 소모 강제 = 반격 트리 악용 방지 장치)
  - ❌ 기각: 게이트형 고위력 반격(뼈 문턱→강한 반격) — 결투 골단(만개 반격)의 완전 하위호환이라 반격 대신 쓸 이유가 없고, 비슷하게 세게 만들면 평타 반격만 계속 쓰이게 돼 조정도 어려움
  - ❌ 기각: 정화 B(턴디버프 결제) — 뼈를 안 건드리고도 정화가 되면 "뼈 강제 소모" 취지가 무너짐
  - ❌ 기각: 정화 C(강한 정화) 이중지불형(뼈+턴디버프) · 도박형(친치로 판정 성공시 전부 제거) · 게이트형(디버프 2종 이상 조건) — 셋 다 시도했으나 차별점을 못 찾아 반려. **정화는 A 한 장으로 확정**(폐기 아니라 "지금은 안 만든다")
  - **✅ ⑥ 승리시/패배시 양쪽 보상형 추가** — 패배시(반격, **다음 턴 보호** 소량 부여) / 승리시(**다음 턴 열기** 소량 부여). ①⑤가 "져야만 의미 있는" 카드였다면 이건 이기든 지든 남는 게 있는 카드. 히후미 크기형 배정(보호·열기, [[character-differentiation-no-hard-weakness]]) 재사용 — 승리=열기·패배=보호로 배정(사용자)
    - ⚠임시값(09-21): 빛1·2굴림(①과 같은 반격 기본 셰이프) → 기본위력 = 곡선(2)12+2+1 = **15**, 범위 **16~21 · 대박33**. 패배시 **다음 턴 보호 2만큼 1턴 부여**, 승리시 **다음 턴 열기 2만큼 1턴 부여**. 예약 효과는 다음 턴 시작에 실체화되고, 열기는 그 턴 종료 시 위세 +2를 지급한 뒤 만료한다
    - 원문 취지 — 승리=열기 · 패배=보호의 양쪽 보상 구조와 +2 임시량은 유지하고, 즉시 1턴만 다음 턴 예약으로 임시 이동
  - **✅ ⑦ 짓눌림 조건부형 추가** — 패배시(반격)는 그대로, **사용시 짓눌림 상태(바 열세 이하)면 이번 판정 위력 +N**(조건부 보너스). 올라프 「전부 되갚다」 패턴 재사용. 히후미 패시브(열세·짓눌림에서 턴 종료시 고조 +3)와 결이 맞물림
    - ❌ 게이트형(짓눌림 아니면 카드 자체를 못 냄)은 기각 — **바가 매 턴 0으로 리셋**되는 구조상 하드 게이트가 성립 안 됨(사용자 지적)
    - ⚠임시값(09-18): 빛1·2굴림(①과 같은 반격 기본 셰이프) → 기본위력 = **15**, 범위 **16~21 · 대박33**. 짓눌림 상태면 이번 판정 위력 **+6**
  - **반격 목표 4장 완결** — ①푼돈 걸기 · ⑤안정형 필러 · ⑥양쪽 보상형 · ⑦짓눌림 조건부형
  - 남은 자리 2 — 반격·뼈수급·정화 목표는 다 찼음(9장 중 7장). 나머지 2장은 다음 세션에서 아이디어 더 모으기로(사용자)
  - ❌ 기각(오독): "적에게 상태이상을 건다"는 첫 제안 — 사용자 의도는 **자기 부위**를 봉인(구 표기 「과부화」)하는 것이었음(부위=적이 아니라 자신)
  - **✅ ⑧ 자기 봉인형 추가(09-17, 구 명칭 「자기 과부화형」)** — 사용시: **뼈 N 이상 보유 시** 그 뼈 N 소모 → 봉인 없이 위력만 상승 / **뼈 N 미만이면** → 이 카드를 낸 슬롯이 **다음 턴 1턴 봉인**된다(그 슬롯 사용 불가) + 위력 상승 + 뼈 즉시 획득. **09-21 용어 통일: 슬롯 잠금은 「봉인」이다**(구 「과부화」는 슬롯 봉쇄 의미에서 폐기 — `Docs/코더 전달 - 봉인 용어 통일 (0921).md`). 봉인은 공용 N/T 상태이상이 아니라 이 카드의 개별 규칙이므로 지속(1턴)은 이 카드 문구에 적는다. ②과감한 판단의 "뼈 있으면 소모/없으면 +20" 분기 패턴 재사용 — 뼈 부자는 안전하게, 뼈 빈곤하면 도박으로 세게. 완전 차단 보너스는 안 붙임(②랑 안 겹치게). 결투 F(본전을 챙기다: 지금 손해→나중에 강화)와 **시간축이 반대**(지금 강화→나중에 손해)라 거울 쌍으로 대비됨. 수치 미정
    - ⚠임시값(09-18): 빛1·2굴림(반격 계열 기본 셰이프 유지 — 이 카드는 반격 태그는 없지만 급이 비슷) → 기본위력 = **15**, 범위 **16~21 · 대박33**. 뼈 문턱 N = **40**(②의 "뼈40 소모"와 맞춤). 위력 상승(뼈 충분시) **+8** / 위력 상승(봉인 감수시) **+8** + 뼈 즉시 **+25**
  - ❌ 기각: 무작위 디버프안 — 사용자가 고정 디버프로 변경 요청
  - **✅ ⑨ 확정(09-17)** — 승리시 → 상대에게 **골절**(굴림 최댓값 −N, 다음 턴) 고정 부여 → 09-21 표기: `다음 턴 골절 N만큼 T턴 부여`. 무작위 아닌 고정 — "육참골단"(살을 베고 뼈를 부순다) 컨셉과 이름값이 맞아 골절로 확정. 결투·도사림·평타 통틀어 **적에게 직접 상태이상을 거는 첫 카드**
    - ⚠임시값(09-18): 빛1·2굴림(표준 디버프형 셰이프) → 기본위력 = **15**, 범위 **16~21 · 대박33**. 골절 **−2** → **`다음 턴 골절 2만큼 1턴 부여`** — ⚠임시값(09-21 표기 정리)(T=1)
  - **평타 풀 확충 완결(9/9)** — ①푼돈 걸기 · ②과감한 판단 · ③동귀어진 · ⑤안정형 필러 · ⑥양쪽 보상형 · ⑦짓눌림 조건부형 · ⑦정화 A · ⑧자기 봉인형 · ⑨골절 부여(번호 중복은 설계 중 임시 라벨, 최종 정리 때 1~9 재배정)
  - **✅ 평타 10 「당첨형」 확정(09-17)** — 매칭시(각 굴림마다) → 그 굴림이 아라시 또는 시고로면 → **빛 +1**(대가 없이 즉시). 후보 검토: 위력 추가(사용자가 "단순하다"고 반려) → 반격 굴림 풀 적립(❌ 이 카드는 (반격) 태그가 없어 반격 풀에 손댈 자격이 없음 — "태그 없으면 반격 없음" 원칙 위반, 사용자 지적으로 정정) → 뼈 직접 획득(❌ 이미 도사림 C·B가 쓰는 패턴이라 겹침) → **빛 직접 획득 채택**. 도사림 E(뼈 내고 사는 빛)와 달리 "운으로 공짜로 받는 빛"이라 결이 안 겹침. 히후미의 두 번째 빛 회복 카드
  - 평타 10/9+(목표 초과 확충)
  - ⚠임시값(09-18): 빛0·2굴림(무료 rider라 비용도 가볍게) → 기본위력 = 곡선(2)12+0+1 = **13**, 범위 **14~19 · 대박31**
- **✅ 히후미 결투 풀 확충 재착수(09-17) — "최대한 만들 수 있을 때까지"(사용자), 9→10+**
  - **✅ 결투 10 「도발형」 확정(재설계, 09-17 밤)** — 처음엔 "다음 적 조준을 강제로 지정 슬롯에 고정"하는 1회성 액션으로 제안했다가 → "그냥 버프로 만들자"고 해서 부위 한정 상태 부여로 전환 → 굴림 없이 도발만 있는 "대충 만든" 초안이라는 지적으로 재설계
    - 이 게임은 솔로 편성이라 일반적인 "도발"(아군 대신 맞기)과 다르게, **적의 조준 방식(1.5, 1스테이지 보스=랜덤)을 히후미가 조작**하는 것으로 재해석. 지정(유진, 적 부위에 3턴 부여)과 같은 문법을 **내 부위**에 쓰는 첫 사례 — 표식·지정이 이미 적 부위 단위로 추적되는 선례가 있어 엔진 부담 적음
    - **최종형**: **BLUE, 2굴림**. 사용시 → 내 특정 부위에 **`도발 T턴 부여`**(값 없는 지속형 취급 — ⚠ 도발은 **정본 11장 미등록**, 09-19 (3) 배치 논의에서 「부위형 턴형 마커 + 턴 수」로만 분류됨) / 굴림마다 승리시 → **견고 + 핏값**(다음 턴 적용) → **`다음 턴 견고 N만큼 T턴 부여` + `다음 턴 핏값 N만큼 T턴 부여`**
    - ⭐ **신규 버프 "핏값"(가칭) = 뼈 획득량 증가.** 경위 — 견고(흐트러짐 피해 ∓)는 원래 히후미한테 안 좋다고 봤었는데(피해량이 뼈로 치환되는데, 흐트러짐 피해가 줄면 그만큼 뼈 획득량도 준다는 게 사용자 진단), 그 손해를 바로 상쇄하는 "뼈 획득량 증가" 버프를 같은 굴림에 페어링해서 해결. 이참에 이 "뼈 획득량 증가"를 히후미 고유 버프로 이름 붙여 재사용 가능하게 함(도사림 「바 태우기형」의 "피격당 뼈+N 증가"류 효과도 핏값으로 통일 가능할지는 다음에 검토)
    - 반격 태그 슬롯을 일부러 맞게 유도해 반격 적립을 엔지니어링하는 콤보 가능. 도발 T 임시값은 아래
    - ⚠임시값(09-18): 빛1(일반 결투 유틸형 기준) → 공식 기본위력 = 곡선(2굴림)12+빛2+1 = **15**, 범위 **16~21 · 대박33**. 도발 지속 **3턴**(지정과 동일 기준) → `도발 3턴 부여`. 굴림 승리시 **견고 +2 · 핏값(뼈 획득 +8)** → **`다음 턴 견고 2만큼 1턴 부여` · `다음 턴 핏값 8만큼 1턴 부여`** — ⚠임시값(09-21 표기 정리)(견고·핏값 T=1). ※ **핏값은 수치형으로 취급, 정본 미등록**(효과 = 받은 피해→뼈 적립 시 +N, 정본 11.2 목록에 없음)
    - ⚠임시값(09-21): **도발 재부여는 값 없는 지속형의 갱신 규칙을 임시 준용** — 새 남은 지속 = `max(현재 남은 지속, 새로 부여된 지속)`. 정본에는 아직 등록되지 않은 처리다
    - 원문 취지 — 도발의 부위 조준 조작과 기존 지속값은 유지하고, 중복 부여 시 지속 처리만 임시로 정의
  - **✅ 결투 11 「매칭시 굴림별 보너스」(가칭) 확정** — 매칭시(각 굴림마다), 그 굴림 결과에 따라 다른 보너스가 붙는 "굴림 도감"형 카드
    - **아라시(1~6)** → 눈 크기 3단 티어(낮은 눈=자원형·중간 눈=방어형·높은 눈=공격형). 처음엔 1~6 전부 따로 지으려 했으나 설계량이 커서 3그룹으로 절충
    - **시고로** → 일부러 **심심한(약한) 보너스**. 위세3형 속임수가 "아라시 또는 대실패"로만 고정하지 시고로는 못 만들어서, 시고로를 잭팟으로 두면 위세3으로 못 노리는 자리가 되어 의미가 약해짐 — 오히려 위세3이 만들어내는 두 결과(아라시·대실패) 쪽에 무게를 싣는 게 낫다는 판단
    - **대실패** → **뼈 획득**(오히려 이득). ⭐ **위세3형 속임수와 시너지** — 속임수는 그 턴 모든 친치로를 "아라시 또는 대실패"로 고정하는데, 이 카드가 깔려 있으면 **어느 쪽이 나와도 이득**이 생겨서 속임수가 순수 업사이드가 됨("대실패해도 몸으로 배운다"는 도박사 정체성과도 맞음). 참고: 반격 굴림 자체의 대실패 처리(결정 대기 항목, 현재 값 0)와는 별개 트리거·별개 맥락 — 이 카드는 매칭시 판정 굴림, 그쪽은 반격 굴림 적립 시점
    - 눈 티어 보너스·시고로 보너스·대실패 뼈량 전부 미정
    - ⚠임시값(09-18): 빛1·3굴림(표준 결투 도감형) → 기본위력 = 곡선(3)11+2+1 = **14**, 범위 **15~20 · 대박32**. 아라시 낮은눈(1~2)=**뼈+15** · 중간눈(3~4)=**방어도+15** · 높은눈(5~6)=**위력+10**(그 굴림 즉시). 시고로 = **뼈+5**(아라시 최저 티어보다도 낮게, 의도적으로 심심함). 대실패 = **뼈+40**(도사림B 뼈+30/도사림C 뼈+100 사이)
  - **✅ 결투 12 「전체 정화형」 확정** — 사용시 → 뼈 **대량 소모**(N) → 자신에게 걸린 **부정적 효과 전부 제거**. 부위형 상태는 정화 대상이 아니다. 정화 A(평타, 부정적 효과 종류 무작위 1종의 항목 전부 제거·소량 뼈)의 결투급 상위형 — 결투 슬롯 값에 맞게 확실하고 비쌈. 결투에 정화 카드가 하나도 없던 빈틈을 메움. N값 미정
    - ⚠임시값(09-18): 빛2·2굴림(비용 대부분을 뼈로 치르니 결투 중에서도 무거운 축) → 기본위력 = 곡선(2)12+4+1 = **17**, 범위 **18~23 · 대박35**. 뼈 소모 N = **200**(명줄을 걸다 문턱 100과 위세 문턱 500 사이 — "대량"이되 위세급은 아님)
  - 결투 12/10+, 계속 확충
- **도사림 풀 확충 착수(09-17)** — 목표 9장(올라프·유진 둘 다 9장 기준 — 공용2+전용7), 현재 6장(공용2·A/B/C 3·환전1). A·B·C가 전부 뼈/방어 축에 쏠려 성격이 겹친다는 지적으로 남은 3자리부터 시작
  - ❌ 기각: 위세 형별(도박수/올인/속임수) 전용 강화 도사림 3장(유진 노림수·선금받기 패턴 재사용안) — 사용자가 의도한 "위세 연계"가 아니라고 반려
  - **✅ 도사림 D 추가(09-17)** — 사용시 → 이번 턴 모든 친치로 판정 **시고로·아라시 확률 +N%p**(범위: 반격 굴림·위세2형 올인·결투 D/H 등 친치로 쓰는 전부에 넓게 적용, 한 장으로 시고로·아라시 둘 다 커버). 유진 노림수(코인 확률 조정)와 같은 패턴 — 도박 컨셉인 히후미한테 지금까지 "운 자체를 올리는" 카드가 없었던 빈틈을 메움. 확률 수치 미정
    - ⚠임시값(09-18): 각각 **+5%p**(아라시·시고로 각 기본 2.78%에서 → 약 7.78%로, 유진 노림수 +10%p는 기본 50%대 코인이라 상대 규모가 다름 — 히후미 쪽은 기저 확률이 낮아 같은 %p라도 체감 배율이 훨씬 큼)
  - **✅ 도사림 E 추가(09-17)** — 사용시 → 뼈 N 소모 → 빛 +1. 올라프 「광기를 가라앉히다」(자원→빛) 대응, 위력 낮게. 메모의 "빛 회복 공용축" 대기 항목 중 히후미분 — [[pending-universal-mechanics-notes]]
    - ⚠임시값(09-18): 뼈 소모 N = **30**(도사림B의 "진 교환당 +30"과 같은 단위 눈금, 저렴하게)
  - ❌ 기각: 도사림 F 1차안(만개 연계 게이트 — 지난 턴 골단 만개 반격 성공시만 사용 가능) — 사용자: 크로스 캐릭터 연계 기믹으로 나중에 쓸 아이디어라 지금 안 씀
  - ❌ 기각: 도사림 F 2차안(반격 풀 1개 소모 → 뼈 +N 현금화) — **타이밍 오류**: 도사림은 턴 시작에 내는데 반격 굴림 풀은 그 턴 합이 진행돼야 쌓인다. 턴 시작 시점엔 풀이 항상 비어있어 성립 안 함(사용자 지적)
  - ❌ 기각: 도사림 F 3차안(받는 피해→뼈 전환율 그냥 ×2, 대가 없음) — 비용 없는 순수 버프라 사용자가 "자신에게 균열 부여 + 배율은 낮게(×1.5)"로 대가 있는 형태 제안
  - **✅ 도사림 F 확정(09-17)** — 사용시 → 자신에게 **균열**(받는 체력 피해 +N, [[skill-design-no-copypaste-rewards]] 기존 공용 상태이상 재사용) 부여(09-21 표기: `균열 N만큼 T턴 부여`) + **이번 턴 뼈 획득량 ×1.5**. 균열 자체가 받는 피해를 늘려 1:1 적립과 겹쳐 곱으로 커지는 올인형 카드. 수치·지속 미정
    - ⚠임시값(09-18): 균열 **+5** → **`균열 5만큼 1턴 부여`**(즉시, 이번 턴 뼈 획득 ×1.5와 같은 턴) — ⚠임시값(09-21 표기 정리)(T=1)
  - **도사림 풀 확충 완결(9/9)** — 공용2(웅크리기·노려보기) · A포커페이스 · B뼈에새긴다 · C살은내주지 · 환전 이 판 접지 · D확률형(시고로·아라시 확률↑) · E뼈→빛 · F균열+뼈배율
  - **✅ 도사림 신규(09-17) — 상호 쇠약형(10번째, 목표 초과 확충)** — 사용시 → 나와 상대 **모두에게 쇠약** 부여(09-21 표기: `쇠약 N만큼 T턴 부여`, 나·상대 각각 독립 항목). 나중에 **정화 A**(뼈 소모 → 무작위 부정적 효과 종류 1종의 항목 전부 제거, 이미 존재)로 자기 것만 지우면 상대만 약해진 상태가 남는 시간차 콤보. 별도 클린즈 카드 신설 불필요(정화 A가 그대로 커버) — 사용자 확인. 위력감소량 미정
    - ⚠임시값(09-21): 나와 상대에게 각각 **쇠약 2만큼 2턴 부여**
    - 원문 취지 — 나와 상대에게 같은 크기의 쇠약을 걸고 다음 턴 정화로 자기 것만 지우는 시간차 콤보 유지. T=2는 정화가 개입할 다음 턴까지 남도록 한 임시값

## 기세 바 직접 조작 (09-17, 3캐릭터 공용 구조 신설)
- 사용자 제안 — 지금 기세 바를 직접 미는 수단이 백우 표식발화(유진 전용, +50) 하나뿐. **캐릭터별로 바를 직접 얻거나 잃는 카드 축을 신설**: 유진(백우 한정, 기존 유지)·올라프는 **얻는 쪽**, 히후미는 **잃는 쪽**으로 배분 확정. 히후미는 패시브(열세·짓눌림 종료시 고조+3)·(반격) 태그 전부 "져야 이득"인 축이라 바를 스스로 깎는 카드가 정체성에 자연히 맞음. 올라프·유진 쪽 실제 카드는 오늘 안 만듦(구조만 확정)
- **✅ 히후미 신규 — 「바 태우기형」(도사림) 확정** — 사용시 → 기세 바 **−30**(고정값, 0 리셋 아님 — 다키스트 던전 신비학자/성녀 비유: 모양이 비슷해도 수치로 밸런스 가르면 된다는 원칙) → **이번 턴** "피격당 뼈 +N" 보너스를 포커페이스(도사림 A, 피격당 뼈+4)보다 훨씬 크게 증가
  - 효과 후보 검토: 견고(❌ 흐트러짐 피해 감소는 "맞아야 버는" 히후미 경제랑 상극) → 뼈 환산/빛 회복 → **포커페이스식 "피격당 고정 보너스" 확대 채택**(사용자: 배율형(F의 ×1.5)이 아니라 "획득할 때마다 증가"하는 A(포커페이스) 방식)
  - **이번 턴 적용 확정** — 도사림은 웅크리기·노려보기처럼 턴 준비 단계에서 먼저 해석돼 "그 턴" 적용이 표준(다음 턴으로 미룬 초안은 착오, 정정)
  - ⚠임시값(09-18): 피격당 뼈 **+20**(포커페이스 +4의 5배 — "훨씬 크게"라는 합의를 반영)
- **✅ 결투 13 「판을 엎다」(가칭, 과부화 4번째) 확정** — RED, 3굴림. 사용시 → **기세 바를 절대값 그대로 전량 소모**(지금 우세든 열세든 그 크기를 통째로 자원화 — "지금 상황을 갈아엎는다") + **자신에게 디버프**(균열 또는 골절) 부여(09-21 표기: `균열 N만큼 T턴 부여`) → 소모량에 비례해 굴림마다 순차 강화(1번째: 위력↑ · 2번째: 뼈 획득 · 3번째: 승리시 상대 확정 디버프)
  - 바 태우기형(도사림, 고정 −30)과 다르게 **전량·절대값**이라 지금 바 상태와 무관하게 늘 쓸 수 있고, 우세일 때 쓰면 쌓은 걸 크게 터뜨리고 열세일 때 써도 손해를 자원으로 만회하는 이중 용도
  - ⭐ **정화 경제와 연결**(사용자 요청) — 자신에게 붙는 디버프가 정화 A(평타)·전체 정화형(결투 12)의 타겟이 됨. 상호 쇠약형(도사림, 나+상대 쇠약)까지 포함해 "디버프를 만들고(상호 쇠약·판을 엎다) → 정화로 내 것만 지운다(정화 A·전체 정화)"는 하나의 경제 루프로 묶임
  - 과부화 트리 4/3(목표 초과), 결투 13/10+
  - N값·디버프 종류·굴림별 정확한 배율 전부 미정
  - ⚠임시값(09-18): 빛1(대가 대부분이 바+디버프라 빛은 가볍게) → 기본위력 = 곡선(3)11+2+1 = **14**, 범위 **15~20 · 대박32**. 자기 디버프 = **균열 +6** → **`균열 6만큼 1턴 부여`**(⚠임시값(09-21 표기 정리), T=1)(골절 대신 채택 — 균열은 받는 체력피해 증가라 뼈 1:1 적립과 직접 맞물려 "크게 지르고 대가도 뼈 경제 안에서 순환"하는 그림이 됨, 골절은 그냥 순수 페널티라 카드 경제랑 무관함). 굴림별: 1번째 위력 += (소모한 바÷10)×**3** · 2번째 뼈 += (소모한 바÷10)×**5** · 3번째 승리시 상대 **골절 −2**(다음 턴) → **`다음 턴 골절 2만큼 1턴 부여`**

## 메모
- 위세 형1 「도박수」를 켜면 만개 반격이 거의 사라진다(구 엔진 실측 만개 0.187 → 0.001) — 두 엔진이 같은 뼈를 두고 경쟁
- 도사림 A · B · C는 전투당 1~2회에서 값이 난다. 매 턴 쓰면 붕괴