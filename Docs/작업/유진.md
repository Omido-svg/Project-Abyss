# 유진

## 09-26 유진 구조 확정 동기화 — 평타 10 · 결투 15 · 도사림 10 · 성장 「집행 / 판단」

**유진은 이번 동기화로 평타 · 결투 · 성장 · 도사림의 구조 설계가 끝난 상태로 본다.** 수치는 전부 (미정) — 계산기로 정한다. 정본 반영 = 규칙서 18장 전면 재작성 · 11.3 · 요약본 · 전투 콘텐츠 명세.xlsx(유진 시트).

### ⛔ 결정 대기 — 문서끼리 충돌하거나 해석이 필요한 곳

1. **은신 재부여 — 「덮어쓰기」와 「누적」이 부딪힌다.** 09-23 결정은 「재부여는 덮어쓴다(은신 3에 2를 걸면 2)」였는데, 09-26 카드 문구는 은신 부여 평타 · 결투 모두 「각 굴림 사용시 다음 턴 은신 N **누적**」이다. 백우는 한 스킬이 3번 부여한다
   - 정본에는 **「한 스킬의 여러 굴림이 부여하는 은신은 누적」**만 넣고 **「이미 은신이 걸린 부위에 다시 부여할 때의 처리 (미정)」**으로 비워뒀다
   - 엔진(sim_py) — **파라미터 「유진 은신 재부여 (0덮어쓰기/1누적)」**, 기본 0(09-26). 결정 나면 기본값만 바꾼다
2. **[09-26 결정 — 현행 유지]** 은신은 **카드를 쓴 부위**에 걸고, 보상도 **쓴 부위** 기준(사용자). ⏭ 남은 확인 — 플레이어가 카드를 둘 부위 슬롯을 직접 고를 수 있는가. 엔진 계측기 「보상 카드 사용 중 사용 부위가 은신이었던 비율」로 확인한다. 아래는 원래 기록 — ~~⚠⚠ 은신 보상 카드가 「이 카드를 쓰는 부위」의 은신을 본다 — 09-22 「부위 비고정」 문제가 되살아날 수 있다.** 09-22 맥 측정에서 「부위 배정이 매 턴 속도굴림으로 다시 정해져서, 은신을 건 부위에 다음 턴 보상 카드가 올라간다는 보장이 없다」는 구조 문제가 나왔고, 09-23에 「보상 카드는 자기 은신 부위를 **골라** 쓴다」로 풀었다. 09-26안은 다시 **카드를 쓰는 부위 자신**의 은신을 본다
   - 플레이어가 카드를 어느 부위 슬롯에 둘지 고를 수 있으면 문제없다. 고를 수 없으면 보상 카드가 은신 부위에 걸릴 확률에 값이 좌우된다 → **부위 배정 방식 확인 필요**
3. **퇴로 차단의 「상대가 짓누름 상태」** — 기존 카드 의미(내 기준 기세가 짓누름 구간 = 내가 상대를 누르는 중)로 정본에 적었다. 「상대가 나를 짓누르는 중」이라는 뜻이었다면 정반대라 고쳐야 한다
   - 엔진 — **파라미터 「유진 퇴로 차단 짓누름 (0내가누름/1상대가누름)」**, 기본 0(바 +70 이상)
4. **✅ [09-26 결정] 낙일 처형 = 모든 낙일 타격**(앞면 · 뒷면 무관, 무기 기본 효과 — 사용자). 이유: **1코인 고정이라는 약점을 「상시 처형 + 이기면 상대 남은 굴림 삭제」로 돌려주는 구조**. 규칙서 18.1 · 요약본 · 명세 반영. 아래는 원래 기록 — ~~낙일 처형이 붙는 타격 — 정본은 기존대로 앞면(크리) 타격에 처형 권한을 두고 「미리 약화될 필요 없음」만 바꿨다. 크리가 아닌 낙일 타격에도 처형이 붙는다는 뜻이었다면 고쳐야 한다(판단 4단계 「낙일 기본 처형」 · 마침표를 찍다 「처형에 성공」 문구가 크리를 언급하지 않는다)
5. **판단 단계가 앞 단계를 유지하는지** — 집행은 「앞 단계 효과 유지」가 명시돼 있고 판단은 명시가 없다. 정본은 집행에만 적었다
   - 엔진 — **파라미터 「유진 성장 판단 누적 (0/1)」**, 기본 1(누적)
6. **알림 — 도사림 트리거 표기** · 지시문은 도사림에 「사용시」를 썼지만 정본 3.4는 도사림을 「도사림 발동시」로 통일해 뒀다 → 정본 표기를 따랐다(의미는 같다)
7. **알림 — 기존 값 유지** · 구조가 안 바뀐 곳은 기존 값을 그대로 뒀다(재계산 대상) — 웅크리기 12 · 노려보기 +1 · 노림수 +10%p · 선금 받기 +2 · 환형 공격 2/4/8 · 수비 +30 · 기회를 엿보다 +1 · 잔혹한 마무리 소비 1 · 추가타 상한 표 · 끝장을 보다 빛 4(사용마다 −1) · 기본 평타/결투 색 · 빛
8. **알림 — 11.3에 은신 · 덫을 전용 키워드로 등록**했다. 19장의 「골단 만개 반격이 약화 선행을 면제받는 **유일한** 경로」는 5장(처형도 예외)과 모순이라 「처형과 함께 둘뿐」으로 고쳤다

### 폐기한 구버전

- 낙일 처형 = 「이미 약화된 부위 필요」 → **한 타격 정상 → 약화 → 파괴**
- 은신 = 「N턴 지속」 · 보상 카드의 「은신 부위를 골라 전량 제거」 → **행동 카운트 · 사용 부위 은신 확인 · 따로 소비 안 함**
- 평타 「한 발 물러서다」, 결투 「그림자 속으로 · 흔들어놓다 · 약점을 캐내다 · 모두를 노리다」 → 활성 풀에서 뺐다
- 뒷조사의 별도 표식 → **낙점만**(기준 평타)
- 장부 정리의 「표식 20 이상」 하드 게이트 → **게이트 없음, 굴림별 환전 시도**
- 현상수배 · 확인 사살 · 정조준 · 숨 고르기 · 손에 익히다 → **굴림마다 발동**
- 믿는 구석 사용시 → **전투 시작시**, 확정 크리는 자동 승리가 아님
- 빌려 쓰다의 「플레이어가 무기 선택」 → **무작위**, 전투 프로필 전체를 빌림
- 시한을 정하다의 「시한 2턴 · 표식 전염 · 연쇄」(09-23) → **공명 덫 설치**. 「시한」 상태명 폐기
- 소문을 내다 「그 대상에게 대량」(09-23) → **모든 적에게**(숨통을 끊다의 전역판)
- 숨통을 끊다 「무기별 조건부 위력」(09-23) → **약화 · 파괴 시 그 대상에게 표식**
- 덫을 놓다의 「덫 1회 · 확정 피해 +20」 → **폭발 덫 스택**
- 때를 앞당기다 「살수의 감 4 → 표식 즉시 44」 → **굴림별 감 소비 · 표식 가속**
- 끝장을 보다 「소모 1당 위력 +2」 → **마무리 피해 풀(하나의 총량)**
- 숨을 죽이다 · 칼을 갈다의 「상대 표식 소모」 → **살수의 감 소모**(도사림은 상대를 조준하지 않는다)
- 성장 공통 몸체 「기존 표식 있으면 위력↑ · 없으면 위력↓」 → **기본 / 강화 상태**. 집행 2 · 3단계와 판단 2~4단계 전면 교체

### ✅ [09-26] 엔진(sim_py) 18장 따라잡기 완료 — 검증 `validate_engine_yujin_0926` 통과

- 반영 — 낙점 자동화(이긴 굴림마다, 카드 효과와 별도) · 덫 2종(폭발 · 공명 스택, 발화에 동반) · 은신(다음 턴 예약 · 굴림별 누적 · 쓰는 부위 확인 · 비소비) · 낙일 처형 = 모든 타격 · 29장 카드 코드 재배치 · 성장 2행(집행 / 판단) · 손질하다 · 빌려 쓰다 무작위
- 올라프 · 히후미는 같은 시드에서 **결과가 한 자리도 안 바뀐다**(보스 · 잡몹4 대조)
- 수량은 전부 **임시값** — 카드 칸(`sim_py/data/extra_skills.txt`)과 파라미터(기본 0: 폭발 덫 스택당 피해 · 공명 덫 스택당 강화 · 낙일 발화 피해 · 끝장 감당 풀 · 성장 판단 2~4 · 집행 2 재부여). **파라미터가 0이면 그 효과가 꺼진 채로 잰다** — 측정 때 `--set`으로 넣을 것
- ⚠ **낙점이 이제 모든 유진 카드에 붙는다** — 09-17 · 09-24 유진 실측은 전부 「뒷조사 · 의뢰 대상만 표식」 기준이라 재측정 대상
- 신규 계측기 — 은신 보상 켜짐 비율 · 감 소비/판(판단 진행도) · 성장 직접 발화/판(집행 진행도) · 처형 성공/판 · 폭발/공명 덫 발동/판 · 끝장 풀 피해/판 · 낙점 표식/판
- 첫 스모크(임시값, 은신 4장 로드아웃, 보스 500판) — **은신 보상 켜짐 비율 0.62**. 결정 2번의 「부위 배정」 확인에 쓸 숫자

#### ⛔ 구조 확인 — 은신 보상 결투가 결투 게이트와 부딪힌다
- 은신 부위의 슬롯은 적이 조준하지 못해 **합이 안 붙는다 → 결투 대 결투가 성립하지 않는다.** 3.3을 그대로 읽으면 은신 보상 결투(N)의 「각 굴림 사용시 확정 추가 피해」는 **은신이 켜진 순간 발동할 수 없다**
- 엔진은 원래부터 **사용시(U) 효과를 결투 게이트로 막지 않는다**(승리시 · 패배시만 막음) — 그래서 지금은 발동한다. 3.3 문구(「결투 스킬의 효과는 결투 대 결투가 성립해야」)와 엔진이 어긋나 있는 곳이고, 올라프 · 히후미의 사용시 결투 효과도 같은 상태다
- → 은신 결투 2장을 결투 게이트 예외로 둘지, 3.3의 「사용시」 취급을 엔진대로 고칠지 **사용자 결정**

#### 엔진이 해석으로 정한 곳 — 문구와 다르면 알려줄 것
- **끝장을 보다 풀 분배** — 적중마다 「남은 풀 ÷ 남은 굴림 수(올림)」. 진 굴림 몫은 다음 적중으로 넘어가고, 스킬이 끝나면 소멸
- **공명 덫 강화** — 스택당 백우 바 +N · 적설 봉인 +N턴 · 낙일 발화 피해 +N(가산). 집행 4단계 「2배」는 무기 원래 발화 효과에만 곱하고 공명 가산분은 안 곱한다
- **잔혹한 마무리 추가타 위력** — 그 무기 코인 1회 굴림(앞면 = 곡선 + 크리값 + 빛, 뒷면 = 13 + 빛). 추가타에도 낙일 처형은 붙는다. 낙점 · 승리시 · 피해 처리 뒤 효과는 없다
- **숨통 · 소문 · 마침표 · 의뢰 대상(처형)** — 「각 굴림의 피해 처리 뒤」를 엔진 전용 트리거 H로 뒀다(교환 승리 · 일방 공통). 결투는 승리시와 같은 게이트를 따른다
- **장부 정리 · 퇴로 차단 2굴림의 표식 소비** — 그 굴림의 **대상 부위**에서만 뺀다(구 엔진은 「표식이 가장 많은 부위」)
- **성장 강화 상태** — 스킬 시작 시 **대상 부위**에 표식이 있었는가. 판단 4단계 적설 「피해 0」은 합이 붙은 경우만 본다(합 종료시 판정)
- **은신 예약** — 다음 턴 시작(적 조준 전)에 실제 은신으로 바뀐다. 부위가 그 사이 파괴되면 버린다

### 09-27 쳇 1차 측정 (`yujin_batch` 배치 4~11 · 보스 · 600판) — ⛔ 틀 문제로 값으로 읽지 않는다

원자료 `Docs/스킬 측정 자료/yujin_batch_4_11_combined.csv`. 보정 적 체력(기준 승률 ≈ 50%) = 보스 부위 백우 76 · 적설 40 · 낙일 57 / 잡몹4 개체 159 · 164 · 46
- 기준선 흔들림(600판, 시드 3벌) = **±1.9 ~ 3.5pp** → 2σ가 4 ~ 7pp라 ±5 대역을 가를 수 없다 → 다음부터 1,000판 이상
- ⭐ **무기 순위가 바뀌었다** — 같은 50%를 만드는 보스 체력이 백우 76 > 낙일 57 > 적설 40. 09-17은 낙일이 가장 약했다(44). 낙일 처형 = 모든 타격의 효과로 보인다. 잡몹4에서 낙일만 46(한 방 44 근처라 체력-승률이 계단형)

**틀 문제 3개 (09-27 교정함)**
1. **평타2 자리가 한 번도 안 나갔다** — 결투 2장이 먼저 공격 슬롯을 차지해 평타는 늘 평타1만 나간다. BLUE 평타(숨 고르기 · 현상수배 · 은신 부여)는 사용 0회였다 → 평타는 전부 평타1에 넣는다
2. **빛 경쟁** — 빛 수급이 턴당 1인데 결투 2장이 빛1씩이라 빛은 늘 모자란다. 빛1 평타(정조준 · 확인 사살 · 믿는 구석)는 결투를 밀어내서 −15 ~ −45가 나왔다. 유진은 위력도 빛에서 나온다(11 + 빛×2) → **「같은 빛의 빈 카드」와의 차이(Δ효과)**를 따로 낸다
3. **결투 기준 카드 의뢰 대상 자체가 무기별로 크다** — 빈 결투 대비 백우 −5.7 · 적설 +19.8 · 낙일 −3.7(적설 골절이 크다). 1장 교체 Δ가 의뢰 대상 값에 끌려갔다

**그래도 읽히는 것 (다음 측정에서 확인)**
- **보스전에서 조건이 안 걸리는 카드** — 숨통을 끊다 · 소문을 내다 · 마침표를 찍다 · 퇴로 차단: 수치를 몇 배 바꿔도 결과가 같다. 값이 아니라 걸릴 조건이 없다 → 잡몹4에서 다시 잰다
- **손질하다** — 낙점 +3 ~ +12 전부 같다(전투당 1회 + 한 턴)
- **때를 앞당기다 표식량 6 = 12** — 값은 표식이 아니라 빛 2(위력)에서 나온다
- **공명 덫 백우(바 강화)는 무반응**, 적설(봉인 +1턴/스택)은 +13 ~ +31로 가장 센 다이얼
- **잔혹한 마무리** — 재굴림을 끄면 감이 남아돌아 추가타가 매번 나간다. 적설 +43(빈 결투 대비)
- **빌려 쓰다** — 백우는 손해, 적설 · 낙일은 +30 이상. 무작위로 받는 무기가 무기마다 달라 **값 하나로 세 무기를 못 맞춘다**(구조)
- **장부 정리 백우** — 문턱을 올릴수록(=덜 바꿀수록) 좋아진다 → 표식이 빛 1보다 값지다

### ⏭ 계산 대기

무기별 표식 부여량 · 살수의 감 획득 / 소비량 · 빛 회복 / 소비량 · 골절 · 쇠약 · 균열 수치 · 흐트러짐 추가 피해 · 추가 고정 피해 · 방어도 · 신속 · 은신량(2 / 3 또는 1 / 2 안) · 덫 스택과 스택당 효과 · 성장 단계별 요구 진행도 · 판단 4단계 살수의 감 보상량 · 끝장을 보다 살수의 감 → 총 추가 피해 변환량 · 손질하다 무기별 낙점 증가량 · 낙일 조건 X% · 낙일 발화 피해 · 지정 N · T · 은신 카드 · 성장 · 손질하다의 색과 빛

## 결정 대기
- ⚠ **[09-26 되살아날 수 있음 — 위 「09-26 동기화」 결정 대기 2번]** 보상 카드가 다시 「사용 부위」의 은신을 보게 됐다. 아래는 09-23 기록 — ~~✅✅ [09-23 해소] 은신 재설계로 부위 비고정 문제를 비켜갔다 — 규칙서 18.7 등재.** 턴 지속형을 폐기하고 **은신 N**(그 부위가 슬롯 행동 1회를 마칠 때마다 −1, 턴 종료로는 안 줄음 · 재부여는 덮어쓰기)으로 바꿨고, **페이오프 카드가 자기 슬롯 부위와 무관하게 자신의 은신 부위 하나를 골라** 쓰도록 했다. 「부여한 부위가 다음 턴에 다시 안 나오면 해제 카드를 못 쓴다」가 구조적으로 사라졌다
  - ⏭ 남은 수치 — 부여량(평타 2 / 결투 3 안 · 너무 세면 1 / 2로), 페이오프 보너스(결투 = 공격 / 평타 = 표식). 계산기 안정 뒤 측정
  - ✅ **[09-24] 엔진 재구현 완료**(abyss-workspace `ec97704`) — 행동 단위 감소 · 덮어쓰기 · 페이오프가 은신 부위 선택. `uStealthVer` 버전 카운터로 「재부여한 그 행동이 끝나며 새 은신이 바로 깎이는 것」까지 막았다. 낙일 발화 · 유진 3카드도 같이 반영됐다
- ~~⭐⭐ 은신 축 4장 — 엔진 구현·검증 완료(09-22, 맥 sim_py), 측정하다 구조 문제 발견. 코덱스 판단 대기~~ (아래는 폐기 전 기록)
  - **엔진 구현 확인** — 부위형 마커 `uStealth[u][p]`(남은 턴, 재부여 시 max 갱신), 효과 코드 `STEALTH,T`(부여) · `UNSTEALTHPOW,N`/`UNSTEALTHMARK,N`(해제+보너스, `USEIFSTEALTH` 게이트로 사용 조건). 적 조준(`AddEnemySlot`)에서 은신 부위 제외 — 2만 회 통계로 해당 부위 0회 확인. 카드 선택 함수(`PickDuel`/`PickBasic`/`DuelUsable`/`BasicUsable`/`UseGateOk`)에 배정 예정 부위(`part`)를 관통시켜야 했음(원래 스킬 ID만 보고 판단했음 — 이 게이트만 예외)
  - ⚠⚠⚠ **구조 문제 — 부위 배정이 매 턴 속도굴림으로 다시 정해져 고정 슬롯이 아니다.** 「은신 부여 → 같은 부위가 다시 나오면 해제」 설계가 **부여·해제 카드가 우연히 같은 물리 부위에 걸려야만** 작동한다. 결투는 매 턴 3부위 전부를 먼저 검토(우선순위)해 부여·해제가 자주 마주치지만, 평타는 결투가 못 채운 자리에만 끼어들어 기회가 적고 은신형(1턴 지속)이라 다음 턴 그 부위가 안 걸리면 그냥 날아간다
  - **실측**(4000판, 백우, 1장교체 아님 — 부여+해제 페어로 빌드): 평타 페어(부여1턴+해제) 보스 −31.8pp / 잡몹4 −26.1pp · **결투 페어(부여3턴+해제) 보스 +10.7pp / 잡몹4 −3.9pp** · 4장 전체 교체는 −46.6/−43.3pp(원래 공격력 있는 카드 4장을 전부 저위력 유틸로 바꾼 거라 당연히 나쁨, 참고값 아님)
  - **판단 필요한 것** — ① 평타 은신형의 지속을 늘릴지(결투처럼 3턴급으로, 다만 평타는 빛0이 정체성이라 그대로 두고 싶을 수 있음) ② "같은 부위"라는 조건 자체를 완화할지(예: 아무 부위나 은신 상태면 발동) ③ 부위 배정을 결투처럼 우선순위를 줄지(평타도 먼저 검토되게) — 어느 쪽도 순수 수치 조정이 아니라 구조 판단
  - 도발(히후미)도 "적이 내 특정 부위를 조준"하는 대칭 메커니즘이라 같은 문제(부위 비고정)에 걸릴 가능성 — 도발 엔진 구현 시 먼저 확인할 것
  - 엔진 코드는 abyss-workspace에 커밋(아래), VBA 미이식 — sim_py 전용 신규 효과 코드
- **패 보기(살수의 감 사용 ③)의 UI 흐름** — 사용 ①(재굴림)은 08-02에 *합 도중 프롬프트를 없애기로* 합의한 결과인데, 패 보기(09-10)는 **결투 교환 도중 선택 UI**를 다시 요구한다. 코더와 UI 흐름 정할 때 확인
  - 시뮬 v3은 「이길 확률이 기준(0.3)보다 낮으면 접는다」 정책으로 근사
- 결투 A 「의뢰 대상」의 바 +15 — 기세 바 v5 스케일(히트 20 · 결투 40)에서 다시 볼 것
- ✅ [09-17 구현 완료] 결투 K 「덫을 놓다」 · L 「시한을 정하다」 — 부위별 덫(`uTrap`) · 시한(`uTimer`) 상태 신설, `AddMark` 발화 지점에 훅 추가. 시한 안 발화 시 효과 2배(백우 바 ×2 / 적설 봉인 ×2 / 낙일 두 곳 약화)
  - (구 기록) - **결투 K 「덫을 놓다」 · L 「시한을 정하다」 엔진 구현 (09-15 수치 확정)** — 둘 다 표식 44 발화에 얹는 지연 라이더라, 부위별로 "덫 걸림 여부" · "시한 남은 턴 수"를 추적하는 상태를 새로 둬야 한다. 표식 발화 처리 지점(18.2 낙점)에 두 훅을 추가
- **백우 「타격당 흐트러짐 +20」 복원 (09-15)** — 09-13 재작성 때 누락된 것을 확인 후 규칙서 18.1 · `p3_core.bas`(Hit 함수) · `spec_data.ps1`에 복원 완료. 값 자체는 08-19에 이미 확정돼 있던 것(재측정 불필요), 다음 윈도우 세션에서 정상 작동만 확인하면 됨 → ✅ 09-16~17 유진 측정에서 정상 작동 확인

- 결투 H 「약점을 캐내다」 — 새 상태 문법에 맞춰 **일단 다음 턴 [상태] N만큼 1턴 부여**. 현재 정본은 백우 쇠약 1·1 / 적설 골절 1·1 / 낙일 균열 1·1을 다음 턴 예약한다. 이번 턴에도 걸지는 성능 보고 결정(백우 기준 이번 턴을 더하면 보스전 +32pp — 표는 `올라프.md`). 적설 · 낙일은 보스전 기준 승률이 바닥이라 측정 불가


## 측정 환경 (09-17 확정 — `tools/sim_v3/yujin_sweep.ps1`)
유진은 올라프와 달리 **기본 환경에서 기준 승률이 바닥 아니면 천장**이라 그대로는 카드 차이가 안 보인다(보정 전: 보스 .066 / .000 / .017, 백우 잡몹3 1.000).

- **적 체력 보정(측정용, 규칙 수치 아님)** — 무기 · 적별로 이분 탐색해 기준을 .49~.59로 맞췄다

  | 무기 | 보스 부위 체력 | 잡몹3 개체 | 잡몹4 개체 |
  |---|---|---|---|
  | 백우 | 100 (원래 150) | 532 (원래 135) | 207 (원래 101) |
  | 적설 | 58 | 252 | 86 |
  | 낙일 | 44 | 166 | 59 |

  - ⭐ **보정 폭 자체가 무기 격차다** — 같은 기준 승률을 만드는 데 백우는 낙일보다 보스 체력 2.3배 · 잡몹3 체력 3.2배가 필요하다
  - 무기별로 기준이 다르므로 **무기끼리 pp를 직접 비교하지 말 것**. 같은 무기 안에서 카드끼리만 비교한다
- **플레이 정책 둘을 바꿔야 측정이 된다(09-17 사용자)**
  - **살수의 감 지정 = 0(재굴림 끔)** — 기본값(결투 슬롯)에서는 감이 지는 교환마다 자동 소모돼 **감 소비 카드가 차례를 못 받는다**(때를 앞당기다 0.00회 · 믿는 구석 0.00회, 재굴림 4.8회/판). 끄면 나오기 시작한다. ⚠ 이건 카드 값 문제가 아니라 **자원 경쟁 구조** — 감 소비 카드(때를 앞당기다 · 믿는 구석 · 끝장을 보다)와 재굴림이 같은 지갑을 쓴다
  - **환형 정책 = 1** — 정책 0이면 환형이 무기를 실제로 안 바꿔(`SwapTarget()`이 현재 무기 반환) **손에 익히다의 조건이 영원히 거짓**이다(0.00 → 1.08회/판)


## 실측 — 빛 비용 대비 효율 (09-17, 유진 32장 × 무기 3 × 적 3)

**틀** — 1장 교체 · 턴당 1회, 4000판 · 시드 914. 원자료 `tools/sim_v3/results/yujin_0917.csv`, 표 `yujin_table.ps1`.
기본 로드아웃 = 평타 뒷조사 · 숨 고르기 / 결투 의뢰 대상 · 잔혹한 마무리 / 도사림 노림수(1회) · 웅크리기. 값 = 그 무기 · 그 적의 기준 대비 **승률 차(pp)**.
⛔ **무기끼리 pp를 직접 비교하지 말 것** — 무기마다 기준(적 체력)이 다르다. 위 「측정 환경」 참고.

### 값으로 읽어도 되는 것 (보스 / 잡몹3 / 잡몹4)

| 무기 | 센 쪽 | 약한 쪽 |
|---|---|---|
| 백우 (기준 .491 / .586 / .503) | 끝장을 보다 **+43.3** · 약점을 캐내다 +37.2 · 현상수배 +33.7 · 그림자 속으로 +26.2 · 덫을 놓다 +14.8 · 정조준 +14.1 · 확인 사살 +13.0 | 한 발 물러서다 −12.0 · 시한을 정하다 −18.8 · 목표 지정 −19.5 · 모두를 노리다 −25.7 · 빌려 쓰다 −27.8 |
| 적설 (기준 .520 / .500 / .499) | 끝장을 보다 **+27.6** · 정조준 +13.7 · 확인 사살 +10.0 | 한 발 물러서다 −21.5 · 빌려 쓰다 −25.6 · **BLUE 결투 다섯 −35 ~ −49** |
| 낙일 (기준 .521 / .514 / .499) | 빌려 쓰다 **+17.8** · 끝장을 보다 +6.8 · 믿는 구석 +6.3 | 한 발 물러서다 −20.0 · **BLUE 결투 넷 −22 ~ −24** |

### 세 무기 공통

- **한 발 물러서다**(BLUE 빛0 평타 · 방어도 +10) — −12.0 / −21.5 / −20.0으로 **평타 중 항상 꼴찌인데 판당 8~10회로 가장 많이 쓰인다.** 빛0이라 빛이 없을 때 계속 나오는데 값이 마이너스다
- **BLUE 결투 전반이 바닥** — 적설 −35~−49 · 낙일 −22~−24. 올라프와 같은 「우선 결투 자리에서 체력 피해 카드를 밀어낸 값」이되 폭이 훨씬 크다. 실제 용법(뒤쪽 슬롯)과 다른 틀이라 그대로 값으로 쓰면 안 된다
- **끝장을 보다(빛4)가 백우 · 적설에서 1위** — 재굴림 정책과 무관하다(재굴림 켜도 백우 +36.8 · 적설 +21.2). 살수의 감 전량을 위력으로 바꾸는 구조 자체가 세다
- **잡몹 열에서 여러 카드가 소수점까지 같다** — 잡몹은 결투를 안 내 **승리시 효과가 통째로 꺼지고 몸체만 남는다**(백우 퇴로 차단 · 마침표를 찍다 · 숨통을 끊다 · 소문을 내다 전부 +15.6 / +3.4). 잡몹 결투 열은 굴림 · 색 · 빛만 잰 값


### ✅ 끝장을 보다 — 위력 기준 빛 2로 고정 (09-17)
결투 1위였는데, 분해하니 **값의 거의 전부가 효과가 아니라 빛에서 나왔다**(백우 보스).

| 변형 | 값 | 사용/판 |
|---|---|---|
| 효과 없음 · 빛1 고정 | +12.4 | 7.5 |
| 효과 없음 · 빛2 고정 | +15.4 | 5.7 |
| 효과 없음 · 빛4 고정 | +28.8 | 2.9 |
| **효과 없음 · 빛4 + 누적 감소** | **+42.8** | 4.5 |
| 감 전환만(빛4 고정) | +31.1 | 2.9 |
| 현행 전체 | +43.3 | 4.2 |

- 살수의 감 전환 기여 **0.5pp**, 위력 배수 A는 1→3이 **2.7pp**뿐
- ⭐ **원인 = 유진만 위력이 빛에서 파생된다**(4.2 「빛 1 = 평균 위력 +2」). 「쓸 때마다 빛 −1」이 내는 빛만 깎고 위력은 원래 빛 4를 그대로 써서, **빛 1만 내면서 빛 4짜리 위력(+8)을 유지**했다. 올라프 닥치는 대로는 굴림 범위가 표에 박혀 있어 같은 문제가 없다 — **유진 전용 공백**
- 위력 기준별(백우 / 적설 / 낙일): 4 +43.3 / +27.6 / +6.8 · 3 +40.1 / +22.6 / +5.5 · **2 +27.4 / +13.2 / +4.7 채택(사용자)** · 1 +6.3 / +0.8 / +1.3
  - 사용 횟수는 4.2~4.8로 거의 안 변한다 → 순수 위력 몫. 채택값이면 백우 결투 1위 → 3위권
- 엔진: 효과 코드 `POWLIGHT,N`(위력 계산에 쓸 빛을 따로 둔다. 효과가 아니라 스킬 속성으로 흡수)
- **✅ [09-17 해소] 「위력 기준 빛 분리」는 이 카드 전용 각주로 확정** — 일반 규칙으로 안 못 박음. 지금 유진에서 비용이 변하는 카드가 끝장을 보다 하나뿐이라, 안 쓰이는 패턴을 미리 규칙서에 일반 조항으로 적을 필요 없다는 판단(사용자). 나중에 비슷한 카드가 또 생기면 그때 일반화. 엔진상으로도 POWLIGHT,N이 애초에 카드별 속성으로 구현돼 있어 추가 작업 불필요

### ⚠ 값으로 읽으면 안 되는 것

- ⛔ **환형(공격) · 환형(수비)의 적설 +47.2 / +43.3 · 낙일 +47.9 / +46.3은 폐기** — 환경 누수다. 환형은 무기를 백우 · 낙일로 바꾸는데, 적설 · 낙일로 보정한 환경은 그 무기들에게 훨씬 쉽다

  | 보스 부위 체력 | 백우 | 적설 | 낙일 |
  |---|---|---|---|
  | 100 (백우 기준) | .491 | .007 | .066 |
  | 58 (적설 기준) | **.977** | .520 | .258 |
  | 44 (낙일 기준) | **.999** | .777 | .521 |

  - 적설 환경에서 백우는 .977, 낙일 환경에서 백우는 .999다. **환형 값 = 무기 격차 × 보정표**이지 카드 값이 아니다
  - 환형을 제대로 재려면 무기별 보정을 안 쓰는 별도 틀이 필요하다(미해결)
- ⛔ **손에 익히다 세 무기 모두 0.00회 = 측정 실패** — 평타 측정 틀의 도사림이 노림수 · 웅크리기뿐이라 **환형이 로드아웃에 없어** 조건(지난 턴 환형 완료)이 영원히 거짓이다. 올라프 피의 맹세와 같은 종류 — 공급원을 넣은 별도 틀로만 잰다 [[skill-measure-single-swap]]
- **때를 앞당기다 0.0~0.4회** — 감 4를 모으는 일 자체가 드물어 거의 안 나온다(백우 0.0 · 적설 0.1 · 낙일 0.4). 값이 아니라 빈도 문제

## 풀 확충
- **평타 풀 브레인스토밍 진행 중(09-15)** — 4장으로 확장
  - **장부 정리**(RED, 빛0, 조건: 표식 20 이상, 사용시 표식 20 소모 → 빛 +1 — 유진의 첫 에너지 회복 카드). 엔진 구현엔 "표식 20 미만이면 카드 자체를 못 낸다"는 사용 조건 게이트가 새로 필요(최후의 저항과 같은 종류의 신규 정책)
  - **한 발 물러서다**(BLUE, 빛0, 사용시 방어도 +10) — 평타 최초의 방어 카드
  - 김에 **결투 D 「그림자 속으로」에 패배시 살수의 감 +1 라이더 추가**(엔진 반영 완료, `YJ-D04`)
  - **정조준**(RED, 빛1, 승리시 크리(앞면)면 무기별 분기 — 백우 표식+6/적설 골절(다음 턴)/낙일 확정 추가피해+8). 엔진에 "크리 여부를 조건으로 쓰는 효과 코드"가 아예 없어서 신설 필요(`CRITBRANCH` 태그만 데이터에 넣어둠)
  - **확인 사살**(RED, 빛1, 승리시 조준 부위가 「지정」 상태면 바 +10 추가) — 지정 연계 첫 카드. 결투 C(목표 지정)로 깔고 이걸로 확인사살하는 콤보 구조. 엔진에 "지정 상태를 조건으로 쓰는 효과 코드"도 신설 필요(`PUSHIFDESIG`)
  - **손에 익히다**(BLUE, 빛0, 조건: 지난 턴 환형 완료로 새 무기 활성화 첫 턴, 사용시 살수의 감 +1) — 아래 「코더 스코프 확인」 항목에서 나온 카드. 코더의 `WeaponChanged` 이벤트를 그대로 재사용 가능
  - **믿는 구석**(RED, 빛1, 사용시 살수의 감 소모(백우3/적설2/낙일2) → 이 스킬 전체 굴림을 확정 크리 취급) — 지금까지 살수의 감 소모는 자동(재굴림·잔혹한 마무리)이거나 결투 한정(패 보기)이었는데, 평타에서 직접 원하는 순간 쓰는 첫 카드. 낙일은 코인 1개뿐이라 "스킬 전체"가 곧 그 1개지만, 코인당 가치(크리값 24)가 커서 소모량을 적설(2코인)과 동일하게 프리미엄 책정. 엔진에 "스킬 전체 굴림 강제 크리" 처리 신설 필요
  - **현상수배**(BLUE, 빛1, 승리시 모든 적에게(다음 턴) 무기별 분기 — 백우 쇠약/적설 골절/낙일 균열) — 결투 H(약점을 캐내다, 단일 대상)와 같은 무기별 디버프 매핑을 재사용해 "단일에 강하게(H) / 전원에 얕게(현상수배)" 대응 구조. F·G(숨통을 끊다·소문을 내다)와 같은 패턴
- **✅ 평타 풀 브레인스토밍 완결 — 2장 → 9장.** 이번 배치: 장부 정리·한 발 물러서다·정조준·확인 사살·손에 익히다·믿는 구석·현상수배(7장 신규)
- **✅ 지정 연계 확장 완료** — **모두를 노리다**(결투 M, BLUE, 빛2, 사용시 모든 적에게 지정 3턴) 추가. 목표 지정(C)의 전체 대상 버전 — F/G(숨통을 끊다·소문을 내다) 같은 단일/전체 짝 패턴. 지정 연계 = 부여2(C·M)·소비1(확인 사살)
- **✅ 낙일 처형 시너지 첫 카드 — 마침표를 찍다(결투 N, RED, 빛1)**: 승리시 백우·적설은 "처치"(cHitKill, 적 전체 사망) / 낙일은 "처형"(cHitDestroy+크리) 조건으로 살수의 감 +2. 시뮬 엔진이 이미 `cHitBreak`(약화·파괴·처치 전부) / `cHitDestroy`(파괴·처치) / `cHitKill`(처치만) 세 플래그를 정확히 구분해두고 있어서(`cHitKill`은 지금까지 아무 카드도 안 쓰던 플래그) 그대로 재사용. 백우·적설은 파괴 권한이 없어 "처치"로 조건을 대신함 — 낙일은 부위 단위(보스 중간에도 여러 번)/백우·적설은 적 전체 단위(다수전에 유리, 단일 보스전은 그 처치가 곧 전투 종료라 드묾)로 자연히 결이 갈림
- **✅ 살수의 감 소비처 2장 추가(결투 O·P, 09-16)**
  - **때를 앞당기다**(O, RED, 빛2) — 사용시 살수의 감 4 소모 → 조준 부위 표식 즉시 44까지 채워 발화(무기별 효과 즉시 발동). K·L과 달리 **발화 자체를 직접 일으킨다**(K·L은 발화에 얹는 라이더일 뿐). 처음엔 "판정 자체를 무조건 승리로 만드는" 안을 제안했으나 사용자가 "믿는 구석(확정 크리)이랑 사실상 다를 게 없고 성능도 별로"라고 반려 — 판정 보장 계열이 아니라 자원 전환 계열(재굴림 자원 → 표식 시스템 즉시 발동권)로 완전히 틀어서 재설계
  - **끝장을 보다**(P, RED, 빛4·쓸 때마다 −1 최소1 누적) — 사용시 살수의 감 전부 소모 → 위력 +2/스택. 올라프 「닥치는 대로」와 같은 비용 감소 구조를 유진 색(살수의 감)으로 재현 — 터뜨리고 다시 모으는 리듬
  - 이걸로 살수의 감 카드 소비처가 잔혹한 마무리·믿는 구석·때를 앞당기다·끝장을 보다 4장으로(+ 범용 재굴림·패 보기)
- **도사림 브레인스토밍 착수(09-15)** — 규칙서 8장에 "도사림은 자기 자신에게만 효과, 상대 조준 불가" 명문화(기존 21장 전부 이미 그랬던 걸 확정한 것). **기회를 엿보다**(강한, 빛0, 사용시 살수의 감 +1) 추가 — 도사림 최초의 살수의 감 획득 카드
  - **숨을 죽이다**(강한, 빛0, 사용시 표식 10 소모 → 방어도 +20) / **칼을 갈다**(강한, 빛0, 사용시 표식 10 소모 → 다음 턴 힘 1) — 표식을 방어/공격 버프로 바꾸는 짝 카드. 표식 소모라는 자원 대가가 "힘" 같은 민감한 스탯의 게이트 역할을 대신함. 엔진에 "표식 소모 → 스탯 전환" 처리 신설 필요
- 도사림 전용 카드가 이제 노림수·선금받기·기회를 엿보다·숨을 죽이다·칼을 갈다 5장
- 도사림 풀
- 직업 아이템 — 표식 상한 조작 카드가 후보 / **환형 완료 시 살수의 감 보상**(아래 참고)도 후보 추가

## ⚠ 코더 스코프 확인 필요 (09-15)
- **`YujinHwanhyeongFlowPassive`("환형의 흐름") 패시브가 기획 승인 없이 코더 쪽에서 만들어짐** — "환형 완료 시 살수의 감 획득(기본값 +1, TODO2 수치 미확정)". 확인 결과 이 자리 자체(환형 완료를 보상하는 것)는 괜찮은 발상이지만, **무비용·무조건 상시 패시브로 두기엔 너무 강함**(카드 슬롯·빛·승패 조건 전부 없이 자원이 생김 — 지금까지 만든 다른 살수의 감 카드들과 격이 안 맞음). **결정: 패시브 폐기 → 직업 아이템 후보로 이관.** 같은 컨셉을 카드로는 「손에 익히다」로 별도 확정(위 참고)
- 환형(공격)/(수비) 2종 구조 자체는 코더 구현이 기획과 일치함(확인 완료) — 걱정할 필요 없음
- **룰 브레이커 후보 (09-14, 올라프 브레인스토밍에서 이관)**: "표식 걸린 대상은 평타를 내도 결투로 취급"(결투 게이트를 강제로 연다) — 올라프는 명예 컨셉상 안 맞아서 제외, 유진은 표식 시스템에 자연스럽게 얹힘. 히후미(도박 컨셉)도 완전히 배제는 아님, 최종 배정 미정

## 09-23 코덱스 구조 확정 반영 — 성장 스킬 · 은신 재설계 · 카드 3장 · 낙일

⚠ **09-26 재동기화로 이 절의 시한을 정하다 · 소문을 내다 · 숨통을 끊다 · 은신 보상 방식 · 성장 단계 설명은 폐기됐다.** 현행은 규칙서 18장과 위 「09-26 동기화」 절. 아래는 이력이다.

규칙서 반영 완료(18.1 · 18.4 F/G/L · **18.7 은신 신설** · **18.8 성장 스킬 신설**). **수치는 전부 (미정)** — 계산기 개선이 끝난 뒤 측정한다.

**성장 스킬 — 「내가 결정하고, 내가 끝낸다」**
- 두 축 = **집행**(표식 44 발화마다 +1) / **판단**(살수의 감 소비 스택만큼 +1)
- 공통 몸체 = 표식을 부여하는 공격. **대상에게 이미 표식이 있으면 위력↑ / 없으면 위력↓**, 자기가 방금 부여한 표식으로는 조건을 못 채운다
- 집행 4단계가 **자기 표식으로 44를 직접 터뜨리면 발화 효과 2배** → 이 자리가 겹쳐서 **「시한을 정하다」를 재설계**했다
- 판단 4단계는 무기별 역할 성공으로 살수의 감 — 백우 창 열기 / 적설 **무피해**(체력·흐트러짐 둘 다) / 낙일 파괴
- ⚠ **성장 트리는 지정을 부여하지 않는다**(목표 지정 · 모두를 노리다의 자리를 침범하지 않기 위해)

**카드 3장 재설계 — 역할 겹침을 푼 것**
| 카드 | 전 | 후 |
|---|---|---|
| 시한을 정하다 | 시한 안에 발화하면 **발화 효과 2배** | **표식 전염** — 시한 2턴, 그 대상이 발화하면 다른 적들에게 표식 (미정) 전염 · 연쇄 가능 · 한 연쇄에서 각 대상은 1회만 |
| 소문을 내다 | 약화/파괴 시 **모든 적**에게 표식 +8 | 약화/파괴 시 **그 대상**에게 표식 대량 (미정) |
| 숨통을 끊다 | 약화/파괴 시 표식 +20 | **무기별 조건부 위력** — 백우 흐트러짐 / 적설 봉인 / 낙일 처형 구간 |

- 겹침의 사슬 — 성장 집행 4단계가 「발화 2배」를 가져가서 시한이 밀렸고, 시한이 「전염」으로 가면서 소문의 광역이 밀렸고, 소문이 「약화/파괴 → 표식」으로 가면서 숨통이 밀렸다. **세 장이 도미노로 자리를 옮긴 것**이다

**낙일 — 약화 의존 완화**
- 표식 발화 「즉시 약화」 **폐기** → **그 부위에 확정 체력 피해 (미정)**, 파괴 권한 없음
- 낙일 카드의 「대상이 약화 상태라면」 조건은 **「부위 체력이 최대의 (미정)% 이하라면」**으로 바꾼다
- ✅ **「이미 약화된 부위에 낙일 크리 → 파괴」는 그대로 유지**
- 흐름 — 체력을 깎는다 → 일정 이하에서 낙일 보너스 켜짐 → 체력 1에서 약화 → 크리로 파괴

**⏭ 계산 대기(계산기 안정 뒤)** — 성장 임계치 · 위력 증감폭 · 표식 부여량 · 전염량 N · 은신 부여량(2/3 또는 1/2) · 페이오프 보너스 · 낙일 발화 피해 N · 낙일 조건 X% · 살수의 감 획득량

## 은신 신설 (09-17)
- ⚠⚠ **이하 「⚠임시값(09-18)」 표시는 전부 미정이다 — 자리만 채운 값.** 구조(트리거·조건·효과 종류)는 확정이지만, 숫자(빛·N값·지속턴)는 실제 계산·밸런싱을 윈도우 세션(Excel VBA 시뮬)에서 해야 정본이 된다. 규칙서·스펙 엑셀에 옮길 때 이 값을 그대로 베끼지 말 것. 유진은 기본위력 11 고정 + 무기가 코인수·크리값·앞면확률 결정(규칙서 4.4)이라 카드 자체엔 빛 비용만 있으면 됨 — 빛N → 뒷면 판정 13+N×2. **표기는 09-21 규칙서 11장(N·T 문법)에 맞춰 정리함** — 값 없는 지속형 = `상태 T턴 부여`. **은신은 정본 11장에 미등록**(09-19 (3) 배치 논의에서 「부위형 턴형 마커 + 턴 수」로만 분류) — 여기서는 값 없는 지속형으로 취급해 `은신 T턴 부여`로 적는다. 내 부위 하나에 붙고, 재부여 시 max 갱신(11.1 값 없는 지속형 규칙 준용 — 미확정)
- 사용자 요청 — 유진(청부업자 컨셉)한테 은신 키워드를 주고 싶다. 히후미 「도발」(내 부위에 강제/우선 조준 부여)의 정반대 짝으로 설계 — **은신 = 그 부위 조준 불가**(적이 다른 부위 중에서 조준)
- ❌ 도사림안 기각 — 사용자: "도사림은 추가하기 힘든 게 [적 조준은] 랜덤이어야 함", 차라리 결투·평타가 낫다는 판단
- **✅ 평타 은신형 확정** — 사용시 → 내 특정 부위에 은신(짧게, 예: 1턴) → 09-21 표기: `은신 1턴 부여`. 가볍고 자주 쓰는 기본형
  - ⚠임시값(09-18): 빛 **0**(한 발 물러서다·손에 익히다 같은 빛0 평타 방어계열과 같은 급) · 은신 지속 **1턴** → **`은신 1턴 부여`** — ⚠임시값(09-21 표기 정리)(T는 원문 그대로, 표기만 변환)
- **✅ 결투 은신형 확정** — 사용시 → 내 특정 부위에 은신(길게, 예: 2~3턴) → 09-21 표기: `은신 T턴 부여` + **살수의 감 획득**(라이더). 순수 은신 + 자원 획득으로 단순하게(다른 라이더는 안 얹음 — 사용자)
  - ⚠임시값(09-18): 빛 **1**(목표 지정·그림자 속으로 같은 빛1 결투 방어/자원계열) · 은신 지속 **3턴**(지정과 같은 기준) → **`은신 3턴 부여`**(⚠임시값(09-21 표기 정리), T는 원문 그대로) · 살수의 감 **+1**(흔들어놓다와 같은 단위)
- 도발(히후미, 적 부위에 강제 조준)·은신(유진, 내 부위에 조준 불가) — 두 캐릭터가 "적 조준 방식(1.5)을 조작"하는 대칭 메커니즘 한 쌍이 됨
- ⚠임시값(09-21): **은신 재부여는 값 없는 지속형의 갱신 규칙을 임시 준용** — 새 남은 지속 = `max(현재 남은 지속, 새로 부여된 지속)`. 정본에는 아직 등록되지 않은 처리다
- 원문 취지 — 은신의 부위 조준 불가와 카드별 기존 지속값은 유지하고, 중복 부여 시 지속 처리만 임시로 정의
- N턴·살수의 감 획득량 등 수치는 윈도우 계산 대기
- **✅ 은신 해제 페이오프 2장 추가(09-17)** — 은신 상태를 소비(해제)하면서 발동하는 "기습" 카드 쌍
  - **결투 — 데미지형**: 조건: 자기 부위가 은신 상태여야 사용 가능 → 은신 해제(그 부위의 `은신` 제거, 09-21 표기) + **큰 피해 보너스**(암살 일격)
    - ⚠임시값(09-18): 빛 **1**(표준 결투 셰이프) · 피해 보너스 = 위력 **+8**(믿는 구석의 확정 크리급 프리미엄을 위력으로 환산)
  - **평타 — 유틸형**: 조건: 자기 부위가 은신 상태여야 사용 가능 → 은신 해제(그 부위의 `은신` 제거, 09-21 표기) + **표식 대량 획득**(결투 은신형이 이미 살수의 감을 주므로 겹치지 않게 표식으로 차별화 — "숨어서 접근해 표적을 낙점한다"는 그림)
    - ⚠임시값(09-18): 빛 **0**(빛0 평타 계열) · 표식 **+15**(숨통을 끊다의 부위 표식+20보다 살짝 적게 — 이쪽은 승리 조건 없이 은신만 있으면 확정 발동이라)
  - 이걸로 은신 축 4장 — 평타(부여, 짧게) · 결투(부여, 길게+감) · 결투(해제, 데미지) · 평타(해제, 표식)
  - 수치 전부 미정

## 기록된 구조 과제 (값 조정 때 볼 것)
- 적설은 중립 내성에서도 약하다(.20)
- 낙일은 굴림이 적어(1코인) 창이 덜 열린다 — 불리할 때 우회로도 막힌다