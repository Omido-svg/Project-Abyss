# 전투 시스템 규칙

Project Abyss 전투 규칙의 **정본.** 지금 유효한 규칙만 담는다.

- **(미정)** = 아직 정해지지 않은 칸이다. 값이 적혀 있으면 임시값이다 — 하드코딩하지 말고 데이터로 뺄 것
- 스킬 · 카드 수치는 **강화 2회를 마친 만렙 기준**이다(15장)
- 결정 근거는 `Docs/개발로그.md`, 논의 중인 것은 `Docs/작업/`에 있다

## 목차
**공통 규칙**
1. 전투 구조 · 부위
2. 턴 진행
3. 합 — 굴림 소모전
4. 굴림
5. 기세 바 — 기세 · 고조 · 열광
6. 타격 타입 · 내성 · 흐트러짐
7. 빛(에너지)
8. 도사림
9. 위세
10. 데미지 · 방어도
11. 공용 상태이상
12. 적 설계
13. 스킬 구성 · 키워드 · 설계 원칙
14. 감정 증강
15. 스킬 강화
16. 아이템 · 런 경제

**캐릭터**

17. 올라프 · 18. 유진 · 19. 히후미

---

## 1. 전투 구조 · 부위

### 1.1 편성
- **솔로.** 런 시작 시 캐릭터 1명을 골라 그 캐릭터로 끝까지 간다. 파티 없음
- 플레이어블 = **올라프 · 유진 · 히후미** (캐릭터 4~7은 구현 대상 아님, 더미 데이터로만)
- 부위 시스템은 **플레이어 + 엘리트 · 보스**만 갖는다. 일반 몹은 부위 없는 단일 체력

### 1.2 부위와 슬롯
플레이어의 부위는 4개 고정 — **머리 / 왼팔 / 오른팔 / 다리**(엘리트 · 보스의 부위는 12.4). 몸통은 부위가 아니다(슬롯도 피격 대상도 아님). 부위 하나가 턴당 행동 슬롯 하나를 준다.

| 부위 | 쓸 수 있는 행동 |
|---|---|
| 왼팔 · 오른팔 | 공격 전용 — 평타 또는 결투 |
| 머리 | 공격(평타 · 결투) **또는** 도사림 — 유일한 겸용 슬롯 |
| 다리 | 도사림 전용 |

- 공격 슬롯은 최대 3개(팔 2 + 머리). 도사림 칸은 최대 2개(다리 + 머리)
- **위세는 슬롯을 쓰지 않는다** (준비 행동, 2장 참고)

### 1.3 체력 — 두 개의 풀, 이중 차감
- 체력은 **부위 체력**과 **전체 체력** 두 풀이다
- **부위에 N 피해 → 그 부위 체력 −N, 전체 체력 −N.** 같은 피해가 두 곳에 동시에 기록된다(흡수·전환 아님)
- **전체 체력 > 부위 체력의 합.** 부위를 부수며 가는 길(빠름)과 체력만 미는 길(느림)이 실제로 갈린다. 배수는 대상별로 다르다(12장 적 설계 · 체력 배수)
- 이미 파괴된 부위를 맞으면 깎을 부위 체력이 없으므로 **전체 체력만** 준다
- **사망 = 모든 부위 파괴 또는 전체 체력 0.** 두 조건은 서로 독립인 경로다

### 1.4 부위 3단계 — 정상 → 약화 → 파괴
1. **정상** — 풀 기능
2. **약화** — 부위 체력이 1에 닿으면 더 깎이지 않고 멈춘다(**죽음의 저항**). 슬롯은 쓸 수 있지만 디버프가 걸린다. 이후 그 부위로 오는 피해는 전체 체력만 깎는다
3. **파괴** — **파괴 권한을 가진 피해가 「이미 약화된」 부위의 남은 체력을 0으로 만들 때만** 도달한다. 슬롯 상실
   - 약화 부위는 보통 체력 1이라 파괴 권한 한 방이면 끊긴다. 회복으로 체력이 올라가 있으면 그만큼 더 깎아야 한다(1.7)

- **판정 기준 = 그 피해가 들어오기 전 상태.** 한 타격이 약화와 파괴를 동시에 할 수 없다(모든 파괴는 약화를 거친다 = 텔레그래프)
- **타격당 상태 변화는 단일 이벤트다** — 「약화됐나」「파괴됐나」를 따로 판정하면 한 타격에서 둘 다 참이 되어 보상이 두 번 발동한다
- 파괴 권한은 캐릭터 고유 루트와 적의 파괴 스킬에만 있다(각 절 · 5.5)

**부위별 약화 · 파괴 디버프**

| 부위 | 약화 | 파괴 |
|---|---|---|
| 팔 ×2 | 그 슬롯 **굴림 −1** (최소 1 보장) | 슬롯 상실 |
| 다리 | **속도 최댓값 −1** | 슬롯 상실 |
| 머리 | 그 슬롯은 **평타만** (결투 · 도사림 불가) | 슬롯 상실 + **빛 최대치 −1** |

- 잃는 종류가 셋 다 다르다 — **양 / 승률 / 선택지**
- 최소 1 굴림 보장 때문에 **1굴림 스킬은 팔 약화에 면역**이다
- **도사림 슬롯 0은 허용되는 극단 상태다** — 다리 파괴 + 머리 약화 이상이 겹치면 도사림을 못 쓴다. 공격 슬롯 3개는 그대로다
- 부위 디버프는 적에게도 대칭 적용된다. 새 부위 디버프를 설계할 땐 *적에게 걸었을 때 내가 뭘 얻는가*도 함께 볼 것

### 1.5 피격 부위 = 조준한 슬롯
- 데미지는 **합을 건(조준한) 상대 슬롯의 부위**에 들어간다. 남은 굴림 일방타격도 같은 부위로
- **파괴된 슬롯을 조준하면** 합이 성립하지 않는다 → 전 굴림 일방타격 + 전체 체력 직접 차감
- 공격하지 않는 슬롯(다리 등)도 조준할 수 있다 — 그쪽이 나를 조준하지 않았으니 일방타격
- 적의 조준 방식은 적마다 정한다(12장). 1스테이지 보스 = 랜덤
- 다리를 조준했을 때의 별도 처리 **(미정)**

### 1.6 일반 몹(부위 없는 대상)
- **죽음의 저항 없음** — 약화 단계 없이 전체 체력 0 = 즉시 사망
- **부위를 지정하는 효과는 몹 자체를 대상 단위 하나로 보고 적용된다**(출혈 · 표식 등). 조준은 「어느 몹을 때릴까」로만 작동
- **약화 · 파괴를 조건으로 하는 효과는 기본적으로 발동하지 않는다.** 카드가 치환을 명시하면 그것을 따른다(예: 유진 처형 → 큰 체력 한 방 / 「약화 또는 파괴 시」류 카드 → 처치 = 파괴로 취급)

### 1.7 전투 중 부위 회복
- **모든 체력 회복은 두 곳에 들어간다 — 전체 체력 +N 과 체력이 가장 낮은 부위 1곳 +N** (1:1, 값을 나누지 않음)
  - 파괴된 부위는 회복 대상이 아니다 — 파괴되지 않은 부위 중 가장 낮은 곳에 들어간다
- **회복은 체력만 올린다. 약화 · 파괴 상태는 풀지 않는다**
  - 약화 부위도 체력은 1 위로 올라간다. 상태는 약화 그대로이고, 올라간 체력만큼 파괴가 늦어진다(1.4)
- 상태를 되돌리는 수단은 **정비 노드(75골드)**와 **연민 3티어 「부위 재생」**(파괴 → 약화)뿐이다
- 최저 부위가 동점이면 인덱스 순, 머리는 마지막

---

## 2. 턴 진행

턴제 + 매 턴 속도 굴림(라이브러리 오브 루이나식). 한 턴은 **계획 단계**와 **해결 단계**로 나뉜다.

**[ 계획 단계 ] — 누르는 즉시 적용**
0. **턴 시작** — 지난 턴에 예약된 효과(「다음 턴」 부여 · 예약 표식)가 여기서 실제로 부여된다. 속도 굴림보다 먼저라 예약된 신속은 그 턴 속도 굴림에 적용된다(11.1)
1. **속도 굴림** — 아군 4슬롯 + 적의 모든 행동 슬롯이 각자 속도를 굴린다. 범위는 항상 플레이어에게 보인다
2. **위세 발동** — 쓰기로 했다면 여기서 발동하고 즉시 적용된다. 위세가 준 것은 같은 턴 도사림 · 슬롯 배치에 바로 쓸 수 있다
3. **준비 행동** — 도사림 · 무기 교체(유진 「환형」). 즉시 적용되며, 자원을 주는 준비 행동이면 그 자원을 같은 턴 슬롯 배치에 바로 쓸 수 있다
4. **슬롯 배치 · 빛 지불** — 각 슬롯의 스킬 · 조준을 정하고 **여기서 빛을 낸다**

**[ 해결 단계 ] — 「턴 종료」 이후**

5. **속도순 합 처리** — 아군 · 적 슬롯을 한 풀에 섞어 빠른 순으로 처리한다. 합 결과마다 기세 바가 즉시 움직이므로 처리 순서가 뒤 슬롯의 상황을 바꾼다

- **준비 행동 = 위세 · 도사림 · 무기 교체.** 셋 다 합이 붙지 않고 팔 슬롯을 쓰지 않는다
- 유진 「환형」의 **무기 교체는 다음 턴부터** 적용된다(카드 보너스는 그 턴 즉시 — 18.2). 그래서 이번 턴 위세는 언제나 현재 무기 기준이다
- 위세는 합이 붙지 않고 슬롯을 소모하지 않는다(예외 없음)
- 자원을 생산하는 준비 행동은 그 자원이 같은 턴에 쓰여 사실상 그 턴 빛 상한을 올린다 — 값은 소비형과 따로 매긴다
- 적의 예정 행동은 **그 턴 것만** 보인다

### 2.1 속도 보정
- 합 판정값에 **빠른 쪽에만** 속도차 구간별 보정을 더한다. 느린 쪽을 깎지 않는다
- **속도차 1~5 → +1 / 속도차 6 이상 → +2** — 최대 격차(균등 1~8 기준 7)까지도 곧바로 상한에 안 닿게 해서, 신속(최댓값 +N)과 다리 약화(최댓값 −1) 같은 속도차를 벌리는 투자가 계속 값을 갖게 한다
- 속도는 턴 시작에 한 번 굴리므로 **그 합의 모든 교환에 같은 보정**이 붙는다
- **데미지에는 속도 보정이 붙지 않는다.** 속도는 「교환에서 이기느냐」만 정한다
- 처리 순서와 판정 보너스는 별개다. 「항상 먼저 처리」와 「속도 무한」은 다른 효과다

---

## 3. 합 — 굴림 소모전

### 3.1 기본 절차 (라오루식)
- **스킬마다 굴림 N개.** 합이 붙으면 양쪽이 굴림을 하나씩 꺼내 맞부딪히고, 한 교환마다 **양쪽 굴림이 둘 다 소모**된다
- 교환마다 판정값(위력 굴림 + 속도 보정)이 높은 쪽이 이긴다. **판정값이 동률이면 위력 굴림만 다시 굴린다**(속도는 고정) — 승부가 날 때까지
- 한쪽 굴림이 먼저 바닥나면 **남은 굴림은 일방타격**이 된다(무저항 적중)
- **합 도중 조준 부위가 파괴되거나 대상이 처치되면 그 슬롯의 남은 굴림은 소멸한다**
- **합 성립** — 내 공격 슬롯이 상대의 **공격 슬롯을 조준하면** 합이 붙는다. 그 상대 슬롯이 누구를 조준했든, 속도가 어떻든 상관없다(느린 슬롯으로 받으면 불리할 뿐이다)
  - **한 상대 슬롯에는 합이 하나만 붙는다** — 그 슬롯을 **마지막으로 조준한** 내 슬롯이 합을 가져간다(속도 무관). 같은 슬롯을 조준한 나머지 내 슬롯은 그 부위에 일방타격
  - **합이 붙지 않은 공격은 전부 일방타격이다** — 아무도 받지 않은 공격 · 공격하지 않는 슬롯(다리 등)을 조준한 공격 · 파괴된 슬롯을 조준한 공격
- **기본 교환 승률 = 50%.** 우위는 증강 · 아이템 · 강화에서 온다

### 3.2 합 승리의 정의
- **한 합의 승자 = 맞부딪힌 교환에서 이긴 수가 더 많은 쪽(다수결).** 일방타격은 세지 않는다
- 동수면 합 무승부 — 합 단위 효과가 발동하지 않는다
- **합 단위로 판정하는 것은 「합 종료시」 트리거를 쓰는 카드 효과(3.4)뿐이다.** 그 밖의 승패 효과는 전부 교환 단위 — 위세 충전(9.1)과 유진 낙일 무기 효과(18.1)도 교환 단위다
- 합 승리는 「안 맞았다」가 아니다 — 2:1로 이겨도 진 교환 1회분 피해는 받는다

### 3.3 결투 효과 게이트 — 결투 대 결투
- **결투 스킬의 효과는 「결투 대 결투」가 성립해야 사용할 수 있다.** 결투 성립 자체는 개별 스킬 트리거가 아니라 공통 게이트다. 상대가 평타로 받거나 조준을 피하면 그 결투는 효과 없이 위력만 남는다
- **승리 효과와 패배 시 메리트(올라프 광기 · 히후미 뼈 등) 둘 다 이 게이트 안**이다
- 게이트가 열린 뒤 상대 굴림이 먼저 끝나 남은 굴림이 일방타격이 되어도, 그 굴림의 **사용시** 효과는 발동한다. 실제 교환이 없으므로 **승리시 · 패배시**는 발동하지 않는다
- 판정 단위는 **굴림과 실제 교환**이다 — 굴림별 효과는 해당 시점에 즉시 정산된다
- 결투 패배 페널티는 따로 없다 — 상대 결투 효과를 그대로 맞는 것이 대가다
- **RED / BLUE 색과 무관하게 같은 게이트를 쓴다**
- 그래서 적이 결투를 내지 않으면 플레이어의 결투 효과는 발동하지 않는다(12.1)

### 3.4 스킬 효과 트리거
| 트리거 | 발동 조건 |
|---|---|
| **전투 시작시** | 해당 스킬의 합이 시작되는 순간, 첫 굴림 전에 카드당 1회. 상대 스킬 강제 교체나 소비량에 따른 굴림 수 확정처럼 **첫 굴림 전에 스킬 구조 자체를 정해야 하는 효과**에만 제한적으로 쓴다 |
| **사용시** | 그 굴림을 실제로 사용하는 순간, 판정 전에 굴림별로 발동. 일방타격으로 넘어간 굴림도 실제로 사용하면 발동한다 |
| **승리시** | 상대 굴림과 실제 교환이 발생했고 그 교환에서 이겼을 때만 발동. 일방타격은 승리로 취급하지 않는다 |
| **패배시** | 상대 굴림과 실제 교환이 발생했고 그 교환에서 졌을 때만 발동 |
| **합 종료시** | 그 스킬의 합이 끝났을 때(모든 교환 · 남은 굴림 일방타격까지 처리한 뒤). 그 합 전체의 결과(진 교환 수 등)를 조건으로 쓸 수 있다. 합이 붙지 않은 스킬에는 없다 |
| **도사림 발동시** | 굴림이 없는 도사림을 발동하는 순간 카드당 1회 처리한다 |

- **`매칭시` 트리거는 사용하지 않는다.** 결투 성립은 3.3의 공통 게이트로만 처리한다
- 단순 자원 소비 · 상태 확인 · 버프/디버프 부여를 편의상 **전투 시작시**로 올리지 않는다. 해당 굴림의 **사용시**에 처리한다
- 스킬이 시작되기 전에 그 슬롯이 끝나면(부위 파괴로 슬롯 상실 등) **전투 시작시 · 사용시 효과도 없다.** 이미 낸 빛은 돌려받지 않는다
- 「승리시」 효과는 그 굴림이 이기는 순간의 **부가 상태**(기세 바 상태 · 자기 게이지 · 상대 스택)를 조건으로 걸 수 있다. **다른 굴림의 결과는 참조할 수 없다**(4.4 참고)

**스킬 사용 조건(하드 게이트)**
- 하드 게이트는 계획 단계에서 카드 자체를 낼 수 있는지를 결정한다. 조건이 거짓이면 카드를 배치하거나 사용할 수 없다
- **「지난 턴 ~였다면」처럼 현재 턴 시작 시 이미 참/거짓이 확정된 조건**은 하드 게이트로 쓸 수 있다
- 같은 턴 앞선 행동으로 달라질 수 있는 **현재 자원 · 체력 조건**은 하드 게이트로 막지 않는다. 카드는 정상 배치하고, 실제 **사용시** 상태를 판정해 조건부 효과만 발동하거나 불발시킨다

### 3.5 상대 결투에 대한 세 대응
| 대응 | 상대 결투 효과 | 합 | 바 |
|---|---|---|---|
| 결투로 받기 | 이기면 봉인 + 내 효과 / 지면 그대로 맞음 | 도박 | 이긴 교환당 결투 푸시 |
| 평타로 받기 | 봉인 | 정상 진행 | 이긴 교환당 히트 푸시 |
| 안 받기(회피) | 봉인 | 없음 | 상대 굴림 전부 일방타격 |

- **회피는 효과만 막고 몸은 못 피한다** — 조준을 붙이지 않은 공격은 위력이 그대로 일방 피해로 들어가고 히트로 세어 바를 민다
- 평타의 존재 이유 = 싸고 확실하게 + **상대 결투 봉인**

---

## 4. 굴림

### 4.1 굴림의 구성과 스킬의 색 — RED / BLUE
- **굴림 하나 = 위력 + 타격 타입(절단 / 관통 / 타격) + 효과.** 공격 / 수비 굴림 구분은 없다
- **스킬 전체가 RED 또는 BLUE 한 색을 갖는다**

| 색 | 이긴 교환에서 상대에게 | 이긴 교환에서 자신에게 |
|---|---|---|
| **RED** | 굴림 값만큼 **체력 피해 + 흐트러짐 피해** (둘 다 상대 내성 적용) | — |
| **BLUE** | 굴림 값만큼 **흐트러짐 피해** (상대 내성 적용) | 굴림 값만큼 **흐트러짐 회복 — 고정값, 내성 무관** |

- 합 절차 · 결투 게이트 · 기세 바 이동은 **색과 무관하게 같다.** 색은 이긴 교환의 효과 적용 방식만 가른다
- BLUE 회복이 고정값인 이유 — 방어 도구는 「쓰면 확실히 되는 것」이어야 한다
- 결투의 콤보 보너스(자원 지급 등)는 색과 무관하게 그대로 준다. 색은 그 위에 얹히는 별개 층이다

### 4.2 위력 산출 — 셋 공통 문법
> **위력 굴림 = 기본위력 + RNG 소스 값**

- **기본위력 = 곡선값(굴림 수) + 빛 × 2** — 평타 · 결투 공통
- **곡선 (굴림 수 → 곡선값)**

| 굴림 수 | 1 | 2 | 3 | 4 | 5 |
|---|---|---|---|---|---|
| 곡선값 | **20** | **12** | **11** | **9** | **8** |

- **빛 1 = 평균 위력 +2** — 고정 상수다
- 굴림이 많을수록 개별 위력이 낮다(반비례). 개수 차이에는 상한이 없다 — 굴림이 적은 쪽은 일방타격을 내주는 대신 한 방이 깊어 **합 도중 파괴**를 노린다
- 캐릭터별 예외: **유진은 기본위력이 11 고정**이고 곡선은 앞면 값에만 적용된다(4.4)
- 파괴 권한 · 폭발처럼 강한 권한을 가진 스킬은 곡선에서 할인받을 수 있다(카드별 표기)

### 4.3 폭과 위치별 텍스처
- **표준 폭 = 8** — 올라프 소스는 균등 1~8이므로 표준 스킬 범위 = **기본위력 +1 ~ +8, 평균 기본위력 + 4.5**
- **위치별 텍스처** — 한 스킬의 여러 굴림이 서로 다른 범위를 가질 수 있다. 최솟값과 최댓값을 위치마다 따로 움직일 수 있다
- **규칙: 위치별 평균의 평균 = 기본위력 + 4.5.** 그 안에서 강약 · 폭 모양은 자유
- 굴림 수나 폭이 바뀌면 목표 평균도 다시 맞춘다
- 완전 동일한 flat 범위는 컨셉상 이유(무작위 효과 등)가 있을 때만 쓴다

### 4.4 RNG 소스 — 캐릭터별
세 소스는 「교환마다 값이 얼마나 흔들리는가」에서 자리가 겹치지 않는다: **유진(양극단) > 올라프(균등) > 히후미(고정 조합)**. 모두 **교환마다 새로 굴린다.**

**올라프 — 주사위**: 균등 1~8. 크리 없음.

**유진 — 코인**: 무기가 코인 수 · 앞면 확률 · 크리값을 정한다(캐릭터 절 무기 표).
- 기본위력 **11 고정**
- **뒷면 = 2** → 뒷면 판정 13(+빛 × 2)
- **앞면 = 곡선값(코인 수) + 크리값** → 이것이 크리

**히후미 — 친치로**: 주사위 3개를 한 번에 굴려 조합 하나를 낸다(= 굴림 1개분).

| 결과 | 경우 | 확률 | 값 |
|---|---|---|---|
| 아라시(같은 눈 3개) | 6 | 2.78% | **+18** |
| 시고로(4 · 5 · 6) | 6 | 2.78% | **+18** |
| 목(2개 일치) | 90 | 41.67% | 짝을 안 이룬 눈 그대로 (1~6) |
| 꽝(전부 다름) | 108 | 50.00% | 가장 작은 눈 (1~3) |
| **대실패(1 · 2 · 3)** | 6 | 2.78% | **확정 패배 + 자해 60 → 뼈 60 + 반격 굴림 +1** (19.3) |

평균 보너스 3.15. 216가지 중 아무 일도 안 일어나는 경우가 없다.

**적 — 균등 주사위**(일반 적 공통, 1~8). 엘리트 · 보스는 고유 소스 예외를 허용한다.

### 4.5 정수 스케일
- **모든 판정값 · 데미지 · 자원은 정수다.** 화면에 소수가 없다
- 소수가 생기는 연산은 **버림, 최소 1 보장**
- 「2배」 같은 효과도 **flat 값으로 적는다**(아라시 +18 등)

---

## 5. 기세 바 — 기세 · 고조 · 열광

이 게임 전투의 중심 정체성은 **한쪽이 기세로 짓누르고, 짓눌렸다가 역전하는 흐름**이다.

### 5.1 세 개의 게이지
| 이름 | 무엇인가 | 단위 |
|---|---|---|
| **기세** | 매 턴 0에서 시작하는 줄다리기 바. 그 턴 최종 위치가 고조 충전량을 정한다 | −100 ~ +100 |
| **고조** | 기세로 차는 누적 게이지 | 칸 |
| **열광** | 고조가 문턱을 넘을 때마다 오르는 레벨 | 0 ~ 3 |

- 위세 버프 「열기」(턴 종료마다 위세 게이지 +N — 11.2)와는 다른 것이다

### 5.2 기세 — 줄다리기 바
- **진영 대 진영 바 하나.** 적이 여러 마리여도 바는 하나다. `+` = 내가 미는 쪽 / `−` = 내가 밀리는 쪽
- **매 턴 0으로 리셋된다.** 턴 안에서는 슬롯이 처리될 때마다 실시간으로 움직인다
- **바는 데미지에 곱해지지 않는다.** 바가 주는 것은 고조 칸 · 발악이다

**구간 (내 기준 B)**

| 구간 | 범위 |
|---|---|
| 짓누름 | +70 ≤ B |
| 우세 | +30 ≤ B < +70 |
| 중립 | −30 < B < +30 |
| 열세 | −70 < B ≤ −30 |
| 짓눌림 | B ≤ −70 |

**미는 양 — 이긴 교환 횟수로 움직인다(데미지량 무관)**

| 사건 | 이긴 쪽으로 |
|---|---|
| 이긴 교환 1회 (평타 · 일방타격 · 회피한 공격 포함) | **20** |
| 결투 대 결투 교환 승리 | **40** |

- 실제 이동 = 내가 이긴 교환 − 상대가 이긴 교환. 비슷하게 주고받으면 상쇄된다
- **방어도로 피해를 다 막아도 히트는 히트**다
- **짓눌림을 만드는 것은 결투 대 결투 합 수다.** 평타로 받으면 적이 결투를 꽂아도 크게 못 민다
- 다수전의 압박은 굴림 경제에서 온다 — 적 굴림이 내 공격 슬롯(3)을 넘으면 그만큼 일방타격을 허용한다

### 5.3 고조와 열광
**턴 종료 시 기세 위치 → 고조 칸**

| 구간 | 짓누름 | 우세 | 중립 | 열세 | 짓눌림 |
|---|---|---|---|---|---|
| 칸 / 턴 | **10** | **5** | **2** | 0 | 0 |

**열광 레벨 문턱** — 0→1 = **4칸** · 1→2 = **8칸** · 2→3 = **10칸** (총 22칸). **상한 3**

**레벨업 보상 (매 레벨업마다 전부)**
1. **빛 최대치 +1**
2. **빛 전량 회복**(새 최대치까지)
3. **감정 증강 3중 1 선택** (8장)

- 캐릭터 예외: 히후미 패시브는 열세 · 짓눌림에서도 고조를 채운다(캐릭터 절)
- 레벨 3 이후에는 바가 고조로 주는 것이 없다. **바 「상태」(짓누름/짓눌림)는 계속 움직이므로 스킬 조건으로 쓰면 후반에도 살아 있는 레버다**

### 5.4 턴 종료 보상 — 짓누름과 발악
- **짓누름(B ≥ +70)으로 턴을 끝내면 다음 턴 빛 +1**(즉시, 최대치까지)
- **짓눌림(B ≤ −70)으로 턴을 끝내면 다음 턴 판정값 +1**(발악 — 노려보기와 같은 종류의 효과, 스킬이 아니라 상태로 걸린다)
- **대칭** — 플레이어와 적 모두. 단 **적은 엘리트 · 보스만** 가진다(일반 몹 없음)
- 바는 스스로 중앙으로 돌아오지 않는다(감쇠 없음)

### 5.5 부위 파괴 원칙 — 죽음의 저항 + 파괴 권한
- 부위를 가진 대상의 모든 부위 피해는 **체력 1(약화)에서 멈춘다**
- **파괴는 파괴 권한을 가진 피해가 「이미 약화된」 부위의 남은 체력을 0으로 만들 때만** 일어난다 — 약화 여부는 피해 전 상태로 판정(1.4)

| 단계 | 누가 | 하는 일 |
|---|---|---|
| 1단계 | 누구나 | 부위를 약화(체력 1)까지 |
| 2단계 | 파괴 권한자만 | 이미 약화된 부위를 끊는다 |

- **처형** — 피해량이 대상 부위의 남은 체력을 0까지 내릴 만큼 충분하면, **같은 타격 안에서 약화 → 파괴**까지 처리하는 권한. 남은 체력을 무시하는 즉사나 무조건 파괴가 아니다
- **파괴 권한 보유자** — 올라프 파괴형 결투 · 처형을 얻은 「피의 맹세」 / 유진 처형(낙일) / 히후미 반격(만개) / **적의 파괴 스킬**
- **약화 선행 예외** — 처형과 히후미 만개 반격. 히후미 만개 반격은 처형과 별개의 캐릭터 예외다
- **적의 파괴 스킬** — 적은 **파괴 스킬을 가진 적만** 부위를 파괴할 수 있다. 일부 잡몹 · 엘리트 · 보스가 가진다. **기세 바와 무관하다.** 대상 = 조준한 슬롯의 부위
- **자기 부위를 스스로 약화 · 파괴하는 효과**(올라프 자가 약화 등)는 이 원칙 밖이다 — 합 없이 즉시 확정
- **부위 파괴의 눈덩이** — 부위가 터지면 그쪽 슬롯이 줄어 교환에서 더 밀린다. 발악이 이 눈덩이를 되돌리는 장치다
- 어느 적이 파괴 스킬을 몇 장 갖는가는 적 설계에서 정한다 **(미정)**

### 5.6 한 판의 리듬
부위 체력을 깎아 판을 깔고 → 기세를 밀어 짓누르고 → 시그니처로 약화된 부위를 끊고 → 다음 부위로. 일반 몹은 이 리듬 없이 체력만 민다.

### 5.7 미정
- 적의 고조 — 현재는 플레이어만 갖는다
- 열광 3레벨 이후 고조가 주는 것

---

## 6. 타격 타입 · 내성 · 흐트러짐

### 6.1 타격 타입 — 굴림의 속성
- 타입 3종 = **절단 / 관통 / 타격.** 캐릭터가 아니라 **굴림 하나하나에 붙는다.** 한 스킬 안에서도 굴림마다 다를 수 있다
- **유진만 예외** — 무기가 타입을 정하고, 그 무기를 든 동안 모든 굴림이 그 타입이다 — **낙일 = 절단 / 적설 = 타격 / 백우 = 관통**
- 스킬의 **실효 배율 = 굴림별 배율의 평균.** 섞으면 안정, 몰면 도박 — 타입 구성이 곧 「누구에게 강한가」다

### 6.2 내성 — 두 벌
대상마다 **체력 내성**과 **흐트러짐 내성**을 타입별로 따로 갖는다. 둘은 완전히 별개다.

| | 저항 | 중립 | 약점 |
|---|---|---|---|
| 배율 | **×0.5** | ×1 | **×2** |

- **곱연산은 이 내성 배율 하나뿐이다.** 결과는 버림, 최소 1
- RED의 체력 피해에는 체력 내성, RED · BLUE의 흐트러짐 피해에는 흐트러짐 내성이 걸린다. BLUE의 자기 회복에는 내성이 걸리지 않는다

**두 내성의 2×2**

| | 흐트러짐 잘 깎음 | 흐트러짐 못 깎음 |
|---|---|---|
| 체력 잘 깎음 | 전천후 | 정공법 — 그냥 때린다 |
| 체력 못 깎음 | **우회로 — 창을 열고 때린다** | 이 적에겐 쓰지 않는다 |

- 창은 **「불리를 뒤집는 문」**으로 둔다 — 체력을 잘 깎는 무기는 창을 볼 일이 없다(의도)
- 그래서 적 설계의 핵심 선택은 **「체력 저항 타입에 흐트러짐 약점을 붙이는가」**다. 체력 약점 타입의 흐트러짐 내성은 아무 값이나 무방하다

### 6.3 흐트러짐 게이지와 창
- 체력과 별개 게이지. **굴림 값만큼 깎인다**(RED · BLUE 모두 상대 흐트러짐을 깎는다)
- **0이 되면 「창」이 열린다** — 창이 열려 있는 동안 그 대상의 **체력 내성이 전 타입 ×2로 덮어써진다**(곱하기가 아님 — 원래 저항이던 타입도 ×2)
- **창의 범위 = 깨진 턴의 남은 처리 + 다음 턴 전체.** 다음 턴이 끝나면 창이 닫히고 게이지가 **100% 회복**된다
  - 한 타격 안에서 흐트러짐 피해가 체력 피해보다 먼저 처리되므로(10.2), **게이지를 0으로 만든 그 타격의 체력 피해부터 ×2**가 걸린다
- **흐트러짐 내성은 창의 영향을 받지 않는다**(창이 다음 창을 앞당기지 않게)

| 대상 | 흐트러짐 최대치 |
|---|---|
| 보스 | **400** |
| 잡몹 개체 | **100** |
| 플레이어 | **350** |

- 목표 = 보스에게 「한 명당 한 번, 잘하면 두 번」
- 플레이어도 창이 열린다. 많이 맞는 캐릭터(히후미)는 자기 창도 자주 열린다 — 의도

### 6.4 스테이지별 잡몹 내성 구성
| 스테이지 | 구성 | 성격 |
|---|---|---|
| 1 | 약점 1 · 중립 2 | 저항이 없다 — 잘못 골라도 벌이 없는 학습 구간 |
| 2 | 약점 1 · 중립 1 · 저항 1 | 처음으로 「이 무기는 안 통한다」가 아프다 |
| 3 | 중립 2 · 저항 1 | **약점이 없다** — 타입 선택이 리스크 회피가 되고 창이 유일한 증폭 수단이 된다 |

### 6.5 미정
- 흐트러짐 내성 배정 — 현재 전부 중립
- 엘리트 · 보스의 내성 두 벌 · 플레이어 3인의 내성
- 올라프 풀 · 적 스킬의 굴림 타입 배정(히후미는 19.9)

---

## 7. 빛(에너지)

누적해서 모으는 행동 자원. 「에너지」와 「빛」은 같은 것이다.

| 규칙 | 값 |
|---|---|
| 전투 시작 | **3 (가득)** |
| 수급 | **턴 시작 시 +1** (누적, 매 턴 전량 회복이 아니다) |
| 기본 최대치 | **3** |
| 최대치가 오르면 | 그 순간 **새 최대치까지 전량 회복** |
| 전투 중 최대치 상승 | **열광 레벨업마다 +1** (+ 전량 회복) — 전투가 끝나면 원래대로 |
| 적 처치 | **+1** (플레이어 전용) |
| 적 부위 약화 | **+1** (플레이어 전용) |
| 적 부위 파괴 | 없음 |
| 머리 파괴 | 최대치 **−1** |

- 비용은 **계획 단계 4번(슬롯 배치)**에서 낸다. 도사림 비용은 도사림을 누를 때 낸다
- 스킬 비용 대역은 **0 ~ 4.** 평타 · 약한 도사림은 대개 0, 결투는 1~2, 룰 브레이커는 3 이상
- **적도 에너지를 가진다** — 적 결투를 내기 위한 것이다. 적은 위의 플레이어 전용 수입을 받지 않는다
- **에너지 회복은 어느 카드 · 어느 캐릭터든 무비용으로 주지 않는다.** 반드시 대가(자원 소모 · 다음 턴 최대치 감소 등)가 붙는다. 캐릭터마다 자기 색의 대가를 쓴다 — 올라프 환율 = **출혈 2 : 빛 1 · 광기 2 : 빛 1**
- 전투 중 최대치를 올리는 효과는 「빛 N 획득」을 겸한다(전량 회복 규칙 때문)
- 증강 · 아이템으로 최대치가 늘어난 상태의 전투 시작값 **(미정)**

---

## 8. 도사림

합 바깥의 **선제 준비 행동.** 다리 · 머리 슬롯에서만 쓴다(팔은 불가).

- **합이 붙지 않는다** — 상대가 맞받을 수 없고 효과가 무조건 확정 적용된다
- **상대를 직접 조준할 수 없다**(특정 부위를 겨냥하는 효과는 못 준다 — 조준 없이 상대 전체에 동시에 미치는 효과는 가능하다, 19.7 「상호 쇠약형」 참고)
- **계획 단계에서 누르는 즉시 적용**된다. 도사림이 준 자원 · 버프는 그 턴 슬롯 배치와 합에 바로 반영된다
- **대가** — 합이 없으니 위세 충전이 없고, 결투 부가효과도 없다
- 로드아웃은 3장이지만 **턴당 실제 사용은 물리 슬롯(다리 + 머리) 최대 2개**다
- 강한 도사림 둘을 한 턴에 겹칠 수 있다(다리 + 머리). 그만큼 팔에 쓸 빛이 줄어든다

| 분류 | 설명 |
|---|---|
| **공용(약한)** | 모든 캐릭터가 쓸 수 있다 — **웅크리기**(빛 0, 방어도 +12) / **노려보기**(빛 1, 그 턴 합 판정값 +1) |
| **캐릭터 고유(강한)** | 캐릭터 절 참고 |

- 형태는 방어형 · 버프형 · 유틸형 · 공격+유틸 하이브리드형
- **다리 슬롯의 핵심 결정** — 안정적인 방어(웅크리기) ↔ 이번 턴 큰 리턴(버프 · 유틸)
- **빛 0 + 순수 이득만 있는 카드는 만들지 않는다** — 무료 방어(웅크리기)를 압도해 다리 슬롯의 선택을 깬다

---

## 9. 위세 (궁극기)

### 9.1 게이지
- 자기 자신에게만 쌓인다. **전투 시작 시 0**, 전투 간 이월 없음

| 사건 | 충전 |
|---|---|
| 합 시작 | **+1** |
| 교환 1회 (때렸든 맞았든 · 일방타격 포함) | **+1** |
| 적 처치 | **+5** |
| 도사림 · 부위 파괴 | 0 |

- 원칙 — **위세는 역전의 발판이므로 이길 때만 차는 자원이면 안 된다.** 충전량이 승패에 거의 무관하도록 짜여 있다
- 캐릭터 · 형마다 **발동 임계**가 다르다(기준 50, 실사용 40~70). 효과가 무거울수록 임계가 높다. 임계와 충전 속도는 강화로 바뀌지 않는다

### 9.2 발동
- 로드아웃 1칸 — **캐릭터별 위세 3형 중 하나를 골라 장착**한다
- 게이지가 임계 이상이면 **계획 단계 2번**에서 발동한다. **즉시 적용**되고, 합이 붙지 않으며, 슬롯을 쓰지 않는다
- 발동하면 게이지에서 임계만큼 소비한다
- 위세가 준 것은 그 턴에 바로 쓸 수 있다(자원형 위세라면 준비 행동 경고가 그대로 적용)
- 위세는 「더 자주 쏜다」보다 **「비로소 한 번」**에 값이 있는 자원이다

---

## 10. 데미지 · 방어도

### 10.1 교환 단위 정산
- **각 교환에서 진 쪽이 이긴 쪽의 굴림 값만큼** 피해를 받는다(RED). 한 합에 교환이 n번이면 정산도 n번
- 데미지에 속도 보정은 붙지 않는다. 기세 바 배율도 없다
- 도사림의 공격은 합 없이 위력이 그대로 들어간다

### 10.2 피해 처리 순서
1. 굴림 값 → **흐트러짐 피해** = 값 × 흐트러짐 내성
2. 굴림 값 → **체력 피해** = 값 × 체력 내성 (창이 열려 있으면 ×2 덮어쓰기 — 1번에서 방금 열린 창 포함)
3. **받는 피해 ±N** (flat 증감류 — 균열 +N · 보호 −N 등. 11.1)
4. 캐릭터 고유 증감(올라프 광기 등)
5. **버림, 최소 1**
6. **방어도 흡수** — 방어도만큼 1:1로 흡수하고 초과분만 다음으로
7. **부위 체력 − 피해, 전체 체력 − 피해** (이중 차감. 약화 · 파괴 판정은 피해 전 상태 기준)

- 방어도로 다 막아도 히트는 히트(바가 밀린다). 흐트러짐 피해는 방어도와 무관하게 들어간다

### 10.3 방어도
- 슬레이 더 스파이어식 **흡수 풀.** 받는 피해를 1:1로 흡수한다
- **매 턴 초기화되지 않고 전투 내내 누적**, 전투가 끝나면 초기화
- 「보호」(받는 피해 감소, 상태이상)와는 다른 축이다

### 10.4 표기 규칙
**공격력 · 방어력 스탯은 없다.** 효과는 아래 어휘로만 적는다.

| 쓰지 말 것 | 대신 쓸 것 |
|---|---|
| 공격력 +N | **위력 +N** (범위 최소 · 최대 동시 flat) |
| 방어력 +N / 피해 감소 N% | **방어도 +N** 또는 **받는 피해 −N** |
| 공격력 N% | 곱연산 금지 |

**곱연산 허용 목록** — 이것 말고는 없다
| 곱연산 | |
|---|---|
| 체력 내성 · 흐트러짐 내성 ×0.5 / ×2 | 창의 ×2 덮어쓰기 포함 |
| 상태이상 「고통」 회복 절반 ÷2 | 명시된 예외 |

- 전부 **버림, 최소 1**
- 새 효과는 flat으로 설계한다 — 곱연산을 겹치면 배율이 폭주한다

---

## 11. 공용 상태이상

캐릭터 전용 키워드(출혈 · 표식 · 뼈 등)보다 한 층 아래의 **범용 버프 · 디버프.** 전용 키워드가 공용의 상위 티어 역할을 한다.

### 11.1 공통 문법

**상태는 세 종류다.**

| 종류 | 문법 | 해당 |
|---|---|---|
| **수치형** | `[상태] N만큼 T턴 부여` | 힘 · 쇠약 · 골절 · 보호 · 균열 · 열기 · 신속 · 견고 · 무장해제 · 재생 · 지정(유진) |
| **값 없는 지속형** | `[상태] T턴 부여` | 고통 · 공포(올라프) |
| **고유 누적 키워드** | 스택 가산 — N · T 문법을 따르지 않는다 | 출혈(올라프 — 17.1) · 표식(유진 — 18.2) |

**수치형**
- **N = 효과 크기, T = 지속.** 둘 다 카드가 정한다
- **표기** — 문장형 `힘 2만큼 3턴 부여` = 줄임형 `힘 2·3`. 두 표기는 같은 뜻이다
- **지속 ∞** — T 자리에 `∞`를 쓸 수 있다(예: `힘 2·∞`, `신속 1·∞`). **턴 종료 시 자연 감쇠하지 않는다**는 뜻이지 제거할 수 없다는 뜻이 아니다 — 카드 · 증강에 적힌 제거 · 비활성 조건과 전투 종료는 그대로 적용된다
- **부여 1회마다 독립 항목이 생긴다.** 같은 상태가 이미 걸려 있어도 병합하지 않는다
- **효과는 살아 있는 항목의 N을 전부 합산해 계산한다**
- **지속은 항목마다 따로 센다** — 턴 종료 시 모든 항목의 T가 1씩 줄고, 0이 된 항목만 사라진다
- **표시** — `값 · 상태 아이콘 · 남은 턴`. 값 = 살아 있는 항목의 N 총합 / 남은 턴 = 살아 있는 항목 중 가장 긴 T. ∞ 항목이 있으면 턴 자리에 `∞`
  - 예 — `2 · [힘] · 3` / `2 · [힘] · ∞`
  - 예 — (5 · 3턴)을 걸고 다음 턴에 (5 · 3턴)을 더 걸면, 실제 적용값은 **5 → 10 → 10 → 5**, 표시는 **5/3 → 10/3 → 10/2 → 5/1**
  - 항목 하나가 내는 총량은 언제 겹치든 `N × T`로 일정하다. 재적용은 총량을 늘리는 것이 아니라 **겹치는 구간에 몰아 넣는 것**이다

**값 없는 지속형**
- N이 없다. 효과는 걸려 있는지 여부로만 정해진다
- **재부여해도 효과는 중첩되지 않고 지속만 갱신한다** — 새 남은 지속 = `max(현재 남은 지속, 새로 부여된 지속)`
  - 예 — 고통 5턴 → 턴 종료 후 4턴 → 다음 턴 고통 2턴 재부여 → 4턴 유지. 1턴 남은 상태에 3턴을 부여하면 3턴
- 턴 종료 시 T가 1 줄고, 0이면 사라진다
- **표시** — `상태 아이콘 · 남은 턴`(예: `[고통] · 3`). 필요하면 턴 자리에 `∞`

**즉시 부여와 다음 턴 예약**
- **적용 시점은 N · T와 별개다.** 카드 문구가 정한다
  - `힘 1만큼 1턴 부여` = 즉시 적용
  - `다음 턴 힘 1만큼 1턴 부여` = 다음 턴 예약
- 즉시 적용된 1턴 상태는 **현재 턴 종료 시** 사라진다
- 다음 턴 예약 효과는 **턴 시작 → 예약 효과 실제 부여 → 속도 굴림** 순으로 처리한다(2장). 그래서 예약된 신속은 그 턴 속도 굴림에 적용된다
- 위력 계열은 부여된 뒤의 다음 교환부터 적용된다. 신속은 속도 굴림에만 쓰이므로, 그 턴 속도를 굴린 뒤 즉시 부여한 신속은 그 턴에 쓰이지 않는다
- **신속은 「다음 턴 전용」 상태가 아니다** — 다른 수치형과 같은 N · T 상태이고, 많은 카드가 다음 턴 예약 형태로 줄 뿐이다

**반대 상태는 서로 지우지 않는다**
- 반대 상태끼리 서로의 항목이나 수치를 제거하지 않는다. 각자 독립적으로 남고 **효과를 계산할 때만 대수합한다**
  - 예 — 힘 (3 · 3턴) + 쇠약 (2 · 1턴) → 그 턴 굴림 보정 +1. 쇠약이 끝나면 남아 있는 힘 때문에 다시 +3
- 계산식
  - 굴림 위력 보정 = 힘 총합 − 쇠약 총합 (공포가 걸려 있으면 추가로 −1)
  - 체력 피해 = 원 피해 + 균열 총합 − 보호 총합
  - 흐트러짐 피해 = 원 피해 + 무장해제 총합 − 견고 총합
  - 턴 종료 시 위세 게이지 = 열기 총합 − 침체 총합
- **골절 · 신속은 따로 얹힌다** — 전체 이동(힘 · 쇠약)과 합산되지 않고 최댓값에만 걸린다. 굴림 종류별로 따로(골절 = 위력 굴림 / 신속 = 속도 굴림)
  - 예: 힘 +1 & 골절 −1 → 바닥 +1 · 천장 −1(폭 7). 골절은 힘으로 못 지운다

**턴 종료 시 효과를 내는 상태 (열기 · 재생)** — 턴 종료 시 이 순서로 처리한다
1. 살아 있는 항목의 N 총합을 계산한다
2. 그 총합만큼 효과를 낸다
3. 각 항목의 지속 −1
4. 0이 된 항목을 제거한다

- 전투 전체의 턴 종료 순서(출혈 도트 · 고조 적립 등 사이의 순서)는 여기서 정하지 않는다

- **상한을 두지 않는다** — 값 · 지속 · 동시 항목 수 어디에도 시스템 상한이 없다. 독립 항목이 겹쳐 높이 쌓이는 것을 허용한다(조합으로 크게 쌓는 재미). 밸런스는 **부여량 · 지속 · 발동 빈도 · 비용 · 획득 난이도**로 잡는다. `∞` 지속도 총합 상한을 두지 않는다

### 11.2 목록

**수치형**

| 상태 | N의 효과 |
|---|---|
| **힘** | 굴림 위력 **+N** (범위 전체 이동) |
| **쇠약** | 굴림 위력 **−N** (범위 전체 이동) |
| **골절** | 굴림 **최댓값 −N** (폭이 줄어든다, 최댓값 최소 1 보장) |
| **보호** | 받는 **체력** 피해 **−N** |
| **균열** | 받는 **체력** 피해 **+N** |
| **열기** | 턴 종료 시 **위세 게이지 +N** |
| **침체** | 턴 종료 시 **위세 게이지 −N** |
| **신속** | **속도 최댓값 +N** (폭이 늘어난다) |
| **견고** | 받는 **흐트러짐** 피해 **−N** |
| **무장해제** | 받는 **흐트러짐** 피해 **+N** |
| **재생** | 턴 종료 시 **체력 N 회복 + 흐트러짐 N 회복** (둘 다) |

- 열기 예 — `열기 2·3` → 세 번의 턴 종료마다 위세 +2, 총 +6. `열기 2·2` + `열기 3·1` → 첫 턴 종료 +5 → 다음 턴 종료 +2
- **열기와 침체는 서로의 항목을 지우지 않는다** — 턴 종료 시 각자의 총합을 낸 뒤 대수합해 한 번에 처리한다
- 재생 예 — `재생 4·3` → 세 번의 턴 종료마다 체력 +4 · 흐트러짐 +4

**값 없는 지속형**

| 상태 | 효과 |
|---|---|
| **고통** | 걸려 있는 동안 **체력 회복량과 흐트러짐 회복량이 모두 절반**(÷2, 버림 · 최소 1). 여러 번 걸려도 절반을 거듭하지 않는다 |

- 힘 · 쇠약은 **모든 굴림**(RED · BLUE)에 걸린다
- **보호 · 균열(체력 피해 ∓)과 견고 · 무장해제(흐트러짐 피해 ∓)는 평행한 2×2다**
- 힘 1스택은 매우 민감한 다이얼이다 — 무조건 지급하지 말고 게이트를 걸 것(13.4 원칙 3)
- 「속박」(속도 최댓값 −1)은 만들지 않는다 — 다리 약화가 그 효과다
- **캐릭터별 대표 상태**(강제 잠금 아님 — 새 효과가 필요한데 뭘 줄지 애매할 때 우선 이쪽부터 볼 것) — 올라프 = 힘 · 재생 / 히후미 = 보호 · 열기 / 유진 = 신속 · 견고
  - 근거 — **RNG 소스가 캐릭터마다 달라 같은 버프의 위험도가 다르다**(올라프 균등 주사위 / 유진 코인 극단값 / 히후미 친치로 고정 조합). 특히 **유진은 코인이 이미 극단 구조(앞면·뒷면 둘뿐)라 힘 같은 고정 위력 이동에 민감**하다

### 11.3 전용 키워드

| 키워드 | 보유 | 종류 | 효과 |
|---|---|---|---|
| **공포** | 올라프 | 값 없는 지속형 | 걸려 있는 동안 상대 굴림 위력 **−1**(고정). 효과 중첩 없음, 재부여 시 지속 max 갱신 |
| **지정** | 유진 | 수치형 | 지정된 부위에 표식이 실제로 쌓일 때마다 **추가 표식 +N**(현재 지정 총합) |
| **출혈** | 올라프 | 고유 누적 | 17.1 |
| **표식** | 유진 | 고유 누적 | 18.2 |
| **은신** | 유진 | 부위 · 행동 카운트 | 18.7 |
| **덫**(폭발 덫 · 공명 덫) | 유진 | 부위 · 스택 | 18.4 |

- 지정 예 — `지정 3·3` 상태에서 표식 +5 → 실제 표식 +8. 여기에 `지정 2·2` 추가 → 지정 총합 5 → 표식을 얻을 때마다 추가 +5
- 광기 · 살수의 감 · 뼈 같은 캐릭터 자원도 이 N · T 규칙을 따르지 않는다 — 각자 자기 장의 규칙을 쓴다
- **감정 증강** — 공용 상태이상을 실제로 쓰는 효과는 N · T · ∞ 문법으로 적는다. **감정 증강의 고유 지속 효과는 공용 상태이상 부여와 별개이며, 카드 텍스트에 적힌 자체 지속 효과로 처리할 수 있다**(14.3)

### 11.4 원칙
- **버프 · 디버프 간 효율 등가는 목표가 아니다** — 같은 1스택이 채널에 따라 몇 배 차이 나는 것을 정상으로 본다. 단 한 스킬이 「둘 중 하나를 부여」하는 형태면 그 자리에서는 효율을 맞춘다
- 슬롯을 사용할 수 없게 만드는 효과는 **「봉인」**이라 부른다. 공용 상태이상이 아니므로 N · T 문법과 항목 합산 규칙을 따르지 않고, 발동 조건 · 대상 · 지속 · 해제는 각 캐릭터 · 스킬 규칙을 따른다(유진 표식 발화 · 감정 증강 「제물(공간)」의 다리 봉인)

---

## 12. 적 설계

### 12.1 공통
- 일반 적의 RNG 소스 = **균등 주사위(1~8)**. 엘리트 · 보스는 고유 소스 예외를 허용한다
- **적도 빛(에너지)을 가진다** — 적 결투를 내기 위한 것이다
- **적 결투의 기본위력은 플레이어와 별개 값**이다(기준 14). 적 종류 · 스테이지별로 따로 매긴다
- 적의 예정 행동은 **그 턴 것만** 플레이어에게 보인다
- **적을 설계할 때 「결투를 몇 개 내는가」를 먼저 정한다.** 플레이어의 결투 효과(히후미 반격 · 뼈 · 만개 / 올라프 출혈 · 광기 · 폭발 / 유진 표식 · 처형)는 전부 결투 대 결투 게이트 안이라, 적이 결투를 안 내면 그 카드들이 통째로 사문화된다. 결투를 거의 안 내는 적은 「결투가 안 통하는 적」이라는 의도된 콘텐츠일 때만 만든다

### 12.2 체력
| 대상 | 부위 체력 | 부위 수 | 전체 체력 배수 | 전체 체력 | 흐트러짐 최대치 |
|---|---|---|---|---|---|
| 플레이어 | 100 | 4 | 1.5 | **600** | 350 |
| 잡몹 | — | — | 1.0 | 개체 135 또는 101 | 100 |
| 엘리트 | (미정) | (미정) | 1.5 | (미정) | (미정) |
| 보스 | 150 | 5 | 2.0 | **1,500** | 400 |

- 부위 체력 · 전체 체력의 절대값은 **(미정)** — 배수와 흐트러짐 최대치는 확정
- 배수는 **두 사망 루트의 상대 가격**이다 — 잡몹은 밀기, 엘리트는 반반, 보스는 부수기. 적 등급이 곧 요구되는 플레이가 된다
- 잡몹은 부위 계통이 없어 배수를 걸 수 없다. 엘리트에 배수를 걸려면 엘리트에게 부위가 있어야 한다

### 12.3 일반 몹
- **1슬롯 × 3굴림**, 부위 없음 · 발악 없음. 파괴 스킬은 **일부 잡몹만** 가진다(5.5)
- **몹 수가 난이도 다이얼**이다 — 내 공격 슬롯이 3개라 4마리부터 맞받을 손이 모자라 일방타격을 허용한다

| 조우 구성 | 개체 체력 | 총 풀 | 성격 |
|---|---|---|---|
| 3마리 | 135 | 405 | 중립(9 대 9) — 짓누름이 열리는 구성 |
| 4마리 | 101 | 404 | 굴림 경제 열세 — 열세에 오래 머무는 구성 |

- 일반전투 노드는 두 구성을 **혼합**한다. 총 풀은 같게 둔다
- 교환 수에 매달린 자원(뼈 · 위세 충전 · 반격 굴림)은 적 체력이 아니라 **적 슬롯 수**가 정한다 — 이 축을 움직이려면 마릿수를 쪼갤 것
- 스테이지별 내성 구성은 6.4

**조우 구성** — 3인 25% / 4인 75%. 인원수별 조합 10종을 각각 같은 확률로 뽑는다(3인 조합 각 2.5% · 4인 조합 각 7.5%)

| 4인 10종 | 3인 10종 |
|---|---|
| 딜러 ×2 + 디버퍼 ×2 | 딜러 ×3 |
| 딜러 ×2 + 버퍼 ×2 | 탱커 + 딜러 + 버퍼 |
| 탱커 + 딜러 + 디버퍼 + 버퍼 | 탱커 ×2 + 버퍼 |
| 탱커 ×2 + 디버퍼 + 버퍼 | 탱커 + 딜러 + 디버퍼 |
| 딜러 ×4 | 딜러 ×2 + 디버퍼 |
| 딜러 ×3 + 버퍼 | 딜러 ×2 + 버퍼 |
| 탱커 + 딜러 ×2 + 버퍼 | 탱커 ×2 + 디버퍼 |
| 탱커 + 딜러 ×2 + 디버퍼 | 딜러 + 디버퍼 ×2 |
| 탱커 ×2 + 딜러 ×2 | 탱커 + 디버퍼 ×2 |
| 탱커 ×2 + 딜러 + 버퍼 | 탱커 ×2 + 딜러 |

- **딜러 슬롯은 등장할 때 공격형 50% / 결투형 50%로 독립 결정**한다

**역할별 행동 성향**

| 역할 | 일반 | 결투 | 준비 | 색 |
|---|---|---|---|---|
| 공격형 | 50% | 50% | 0% | RED 100% |
| 결투형 | 25% | 75% | 0% | RED 100% |
| 디버퍼형 | 50% | 50% | 0% | BLUE 75% / RED 25% |
| 버퍼형 (아군 생존) | 0% | 0% | **100%** | — |
| 버퍼형 (혼자) | 75% | 25% | 0% | RED 50% / BLUE 50% |
| 탱커형 (공격 모드) | 50% | 50% | 0% | RED 50% / BLUE 50% |

- **버퍼**는 아군이 하나라도 살아 있으면 준비 · 지원만 하고 **결투 후보에서 빠진다**
- **디버퍼**는 같은 디버프가 이미 걸려 있어도 다른 것을 우선하지 않는다 — 상태이상은 독립 중첩이라 중복 회피가 필요 없다(11.1)

**탱커형 — 엄호**
- 아군 1명을 엄호한다. 엄호 대상에게 들어가는 **일방타격만** 탱커에게 전환한다
- **이미 성립한 합 · 교환에는 개입하지 않는다.** 전환된 공격은 탱커를 향한 일방타격으로 처리한다
- 대상 우선순위 — ① 체력 50% 이하 아군 ② 버퍼 ③ 그 외 아군 ④ 혼자면 엄호하지 않는다
- 행동 — 유효한 엄호가 없고 엄호할 아군이 있으면 **엄호 준비 행동** / 엄호가 살아 있으면 **공격** / 대상이 죽거나 조건이 깨지면 엄호를 끝내고 다시 지정할 수 있다. 혼자면 공격 모드
- **(미정)** — 탱커 둘이 같은 아군을 동시에 엄호할 수 있는지 · 엄호의 지속과 해제 세부

**조우 전체의 턴당 결투 수** — 1개 25% / 2개 50% / 3개 25%. 0결투 턴은 특별한 콘텐츠가 아니면 쓰지 않는다
- 역할별 결투 확률은 적마다 결투를 따로 굴리는 값이 아니라, **그 턴의 총 결투 1~3개를 누구에게 줄지 정하는 가중치**다
- 버퍼처럼 결투를 못 내는 적은 후보에서 뺀다. **(미정)** — 후보가 모자랄 때 목표 결투 수를 어떻게 처리할지

**1스테이지 일반 몹 12종** — 공격형 3 · 결투형 3 · 버퍼형 2 · 탱커형 2 · 디버퍼형 2. 종수만 확정이고 이름 · 스킬 · 수치는 (미정)


### 12.4 엘리트 · 보스
- **부위 5개**(플레이어는 4개 — 의도적 비대칭). 도사림을 두 겹으로 깔 수 있다
- **부위 구성은 적마다 데이터로 정의한다** — 부위마다 이름 · 슬롯 역할(공격 / 도사림 / 겸용) · 약화 · 파괴 디버프. 플레이어의 4부위 규칙(1.2 · 1.4)은 기본형일 뿐 적에게 강제되지 않는다
- **발악**을 가진다(대칭)
- **파괴 스킬**을 가질 수 있다(5.5)
- **3자세 로테이션** — 공격 슬롯 수를 자세로 바꾼다

| 자세 | 공격 슬롯 | 성격 |
|---|---|---|
| 보통 | 3 | 균형 |
| 웅크림 | 2 | 내 손이 하나 남는다 — 텔레그래프이자 함정 |
| 공세 | 4 | 하나는 못 받는다 — 「어떻게 맞을지」를 고른다 |

- 순서 `보통 → 웅크림 → 공세 → 보통`, 전환 간격 2~3턴 랜덤
- 부위가 파괴되면 쓸 수 있는 슬롯 총량이 줄어든다(로테이션은 유지)

### 12.5 1스테이지 보스
**플레이어 스킬 밸런싱의 기준선.** 플레이어 스킬은 이 보스를 상대로 **스킬 간 상대 비교**로 맞춘다.

| 항목 | 값 |
|---|---|
| 자세 | 보통 자세 기준 3슬롯 — 배열 **A · A · B** (자세별 슬롯 수보다 짧으면 순환) |
| 스킬 A | **3굴림 · 위력 13** (곡선 11 + 보스 프리미엄 2) · 결투 |
| 스킬 B | **2굴림 · 위력 14** (곡선 12 + 2) · 결투 |
| 스킬 A · B의 색 | **(미정)** |
| 조준 | **랜덤**(부위 균등) |
| 파괴 스킬 | 별도 스킬 **(미정)** — 스킬 A · B에는 파괴 권한이 없다 |

- 적 빛 2 · 결투 비용 1이라 매 턴 A · A가 빛을 다 쓰고 **B는 평타로 나온다.** 빛이 모자라면 **결투 플래그만 해제**하고 굴림 구성은 유지한다

### 12.6 미정
- 엘리트 6종 · 중간보스 2종 · 메인보스 2종 — 부위 구성 포함
- 적별 파괴 스킬 배정 · 1스테이지 보스의 파괴 스킬
- 적 조준 정책(보스 외) · 적이 거는 디버프 스킬 · 공세 변형

---

## 13. 스킬 구성 · 키워드 · 설계 원칙

### 13.1 로드아웃 — 전 캐릭터 공통 3 | 3 | 3 | 1
| 칸 | 장착 수 | 턴당 사용 |
|---|---|---|
| 평타 | 3 | 팔 · 머리 슬롯 수만큼 |
| 결투 | 3 | 팔 · 머리 슬롯 수만큼 |
| 도사림 | 3 | 다리 + 머리, 최대 2 |
| 위세 | 1 | 준비 행동 |
| 패시브 | 고정 | — |

- 결투 3칸은 파괴형 · 유틸형 구분 없이 자유롭게 채운다
- 로드아웃 수 ≠ 턴당 사용 수 — 3장을 들고 다니되 그 턴엔 슬롯이 허락하는 만큼만 쓴다

### 13.2 스킬 풀 원칙
| 구분 | 칸 | 구조 |
|---|---|---|
| 불변 | 패시브 | 어떤 경우에도 동일 — 캐릭터가 캐릭터인 이유 |
| 런 선택 | 위세 | **캐릭터당 3형.** 런 시작 시 하나 선택. 수집 · 교체 대상 아님 |
| 풀(수집) | 평타 · 결투 · 도사림 | 런 중 획득(상점 · 엘리트 · 조우) → 보유 풀에서 장착. **교체는 정비 구역**, 강화는 카드에 귀속 |

- **풀은 캐릭터 전용**이다(공용 풀 없음). 예외 — 공용 도사림 2장(웅크리기 · 노려보기)
- **위력은 생성 문법(곡선 + 빛 × 2)이 자동으로 정한다.** 카드가 바꿀 수 있는 축은 굴림 수 · 위치별 텍스처 · 빛 · 색 · 효과 · 타격 타입. 풀이 커져도 수치가 안 흔들린다
- 위세는 **이중 참조** — 자기 키워드 하나 + 다른 키워드 하나를 참조한다
- 맞춤 결투 = 형마다 「심화 1 + 가교 1」(가교 = 위세가 참조하는 다른 키워드로 이어지는 카드)

### 13.3 키워드는 캐릭터 전용
잔효과 키워드는 그 캐릭터만 쓴다. 캐릭터끼리 공유하지 않는다(솔로 편성이라 같은 전투에 둘이 있을 수 없다).

| 캐릭터 | 전용 키워드 |
|---|---|
| 올라프 | **출혈**(도트 + 폭발) · **공포** · **광기**(자기 자원) |
| 유진 | **표식**(상한 44에서 무기별 발화) · **지정** · **살수의 감**(자기 자원) |
| 히후미 | **핏값**(피격 적립 가산, 19.2) · **도발**(부위 강제 유도 — 공용화 여부 미정, 19.1) · 자기 자원 **뼈** · **가불**(턴 한정 임시 자원, 19.2) |

- 전용 키워드는 공용 상태이상(11장)의 상위 티어 역할을 한다

### 13.4 스킬 설계 원칙
새 카드를 만들 때 지킨다.
1. **보상을 복붙하지 않는다** — 카드마다 다른 통화(상태이상 · 게이지 · 자원)를 쓴다
2. **굴림끼리 참조하지 않는다** — 「1번째가 졌으면 2번째가 커진다」 금지. 그 굴림이 이기는 순간의 부가 상태(바 · 게이지 · 상대 스택)는 조건으로 쓸 수 있다
3. **민감한 효과는 상대 스택으로 게이트한다** — 예: 상대 출혈 0~4 / 5~9 / 10+
4. **트리거 종류 · 효과 · 조건 · 컨셉 넷이 다 달라야 다른 카드다** — 위치 번호 · 대상 · 자원 이름만 바꾸면 재탕
5. **폼별 채택 검증** — 자기 빌드 안에서 상위 3장을 못 이기는 카드는 죽은 카드다
6. **캐릭터 차별화에 하드 약점을 쓰지 않는다** — 한 캐릭터로 런 전체를 가므로 「이건 아예 못 한다」는 나쁜 경험이다. 굴림 · 패시브 · 아이템 · 스킬의 결로만 갈린다
7. **빛 0 + 순수 이득 카드 금지 · 에너지 회복은 대가 필수**(7장)
8. **파괴 권한이 없는 유틸형에 「라이더 없음」을 두지 않는다** — 존재 이유가 사라진다
9. 스킬 밸런싱은 **스킬 간 상대 비교와 유틸**로 판단한다. 절대 승률 · 골드 환산으로 스킬 값을 정하지 않는다
10. 스킬 효과 트리거는 3.4의 표를 쓴다. 즉흥 타이밍을 새로 만들지 않는다

---

## 14. 감정 증강

열광 레벨업 보상. **전투 한정 · 전 캐릭터 공통.**

### 14.1 구조
- **런 시작 전에 감정 1개를 고른다.** 그 감정의 9장이 그 런의 풀 전체다
- **9장 = 1티어 3 · 2티어 3 · 3티어 3.** 열광 레벨 n에 도달하면 **n티어 3장을 전부 보여주고 1장 고른다**
- **무작위성 없음(의도)** — 레벨 1에서는 언제나 같은 1티어 3장을 본다. 런의 변주는 시작 감정 선택(× 캐릭터)에 있다
- **얻은 증강은 그 전투 안에서만 유효하다.** 전투가 끝나면 사라진다
- 돈 · 아이템 · 증강을 주는 카드는 **전투가 끝난 뒤 결과물로** 지급한다(한 전투에서 두 번 강해지는 구멍 방지)
- 3티어는 사실상 **보스 · 엘리트에서만 열린다.** 「전투 후반에만 열린다」를 전제로 설계한다

**티어 문법**

| 티어 | 문법 |
|---|---|
| 1 | **상시 · 무조건** — 조건 없이 늘 걸린다 |
| 2 | **사건에 걸린다** — 이긴 교환 · 피격 · 처치 · 부위 파괴 · 구간 · 레벨업 · 창 개방 등 |
| 3 | **규칙을 바꾼다** |

- 한 감정 안의 카드들은 서로 경쟁한다(티어당 1장 선택) — 같은 티어 세 장의 효율이 비슷해야 한다
- 증강은 **위력**을 건드리지 않는다(스킬 강화 · 아이템의 축) — 단 **힘(공용 상태이상)**은 쓸 수 있다
- **속도**를 직접 올리지 않는다 — 대신 상태이상 **신속**(속도 최댓값 +1)을 쓴다
- 공용 상태이상을 주는 효과는 N · T · ∞ 문법으로 적는다(11.1). **증강 자체의 고유 지속 효과는 공용 상태이상이 아니다** — 카드 텍스트에 적힌 대로 처리한다
- 감정은 전부 **긍정적이고, 신을 믿는 데 쓰는 감정**이다(세계관 — 지배자가 인간에게 심으려는 감정). 스스로 나아가는 감정(각오 · 용기 · 희망 · 긍지 · 의연 · 해학)은 후속작 몫

### 14.2 7감정의 축
| 감정 | 뿌리 | 축 |
|---|---|---|
| 연민 | 받았음 | 회복(체력 · 흐트러짐) |
| 신의 | 위를 봄 | 방어도 |
| 초연 | 받아들임 | 최대체력 · 최대 흐트러짐 · 내성 |
| 감복 | 받았음 | 돈 (3티어에서 아이템 · 증강) |
| 경외 | 위를 봄 | 힘 · 신속 (3티어에서 슬롯) |
| 동경 | 위를 봄 | 위세 |
| 그리움 | 잃었음 | 기세 바 — 재원은 지나간 턴 |

미배정 축: **균열**(동경 2티어에 한 장만 씀).

### 14.3 카드 63장

**연민**
| 티어 | 카드 | 효과 |
|---|---|---|
| 1 | 재생 | 3턴간 턴 종료 시 체력 +8 |
| 1 | 즉발 회복 | 즉시 체력 +40 |
| 1 | 흐트러짐 재생 | 3턴간 턴 종료 시 흐트러짐 +5 |
| 2 | 교환 승리 회복 | 이긴 교환마다 체력 +4 · 흐트러짐 +2 |
| 2 | 흡혈 | 준 피해의 10%만큼 체력 회복 |
| 2 | 열세 회복 | 짓눌림 구간에서 턴이 끝나면 체력 +12 |
| 3 | 전환 | 이후 회복하지 않고, 회복했을 양만큼 무작위 적 부위에 즉시 피해 |
| 3 | 오버힐 | 최대치를 넘긴 회복량만큼 방어도 (전투당 최대 50) |
| 3 | 부위 재생 | 파괴된 부위 1곳을 약화 상태로 재생. 같은 전투에서 다시 파괴될 수 있다 |

- 1티어 「재생」 · 「흐트러짐 재생」은 **증강 고유 지속 효과**다 — 공용 상태이상 「재생」(11.2)이 아니다. 이름만 같다

**신의**
| 티어 | 카드 | 효과 |
|---|---|---|
| 1 | 기본 | 턴 시작 시 방어도 +3 |
| 1 | 자세 대응 | 적 공격 슬롯 1개당 방어도 +1 |
| 1 | 반사 | 그 턴 받은 피해(방어도 흡수 전)의 5%를 다음 턴 시작 시 방어도로 |
| 2 | 누적 | 매 턴 방어도 획득량이 +2씩 늘어난다 |
| 2 | 완전 방어 | 매 턴 1회, 그 턴 첫 교환의 피해를 방어도 소모 없이 무효화 |
| 2 | 반사(상위) | 받은 피해의 12%를 다음 턴 방어도로 |
| 3 | 복구 | 방어도가 소모될 때마다 33% 확률로 그 수치만큼 복구 (미정) |
| 3 | 회복 파생 | 턴 종료 시 그 턴에 얻은 방어도만큼 체력 회복 |
| 3 | 전환 | 이후 방어도를 얻지 않고, 얻었을 양의 3배를 무작위 적 부위에 피해 |

**초연**
| 티어 | 카드 | 효과 |
|---|---|---|
| 1 | 체력 그릇 | 부위 최대체력 +5% |
| 1 | 자세 그릇 | 흐트러짐 최대치 +50 |
| 1 | 약화 완화 | 약화된 부위가 하나일 때는 페널티를 받지 않는다 |
| 2 | 처치 성장 | 적 처치마다 전체 최대체력 +2 (**런 지속**, 부위는 안 오름) |
| 2 | 나눠 받기 | 이번 턴 피해의 절반을 다음 턴 체력이 가장 많은 부위로 미룬다. 전투가 끝나면 미룬 피해는 사라진다 |
| 2 | 슬롯 유지 | 부위가 파괴되어도 슬롯을 잃지 않는다 (전투당 1회) |
| 3 | 평정 | 내 체력 내성이 전부 1.0이 된다 |
| 3 | 신의 육체 | 내 흐트러짐 게이지가 사라진다 — 흐트러지지 않는다 |
| 3 | 몸을 놓는다 | 최대체력 −10% (미정), 이번 전투 동안 최대 빛 +1, 획득 즉시 빛 최대 회복 |

**감복** — 골드 값 N은 전부 (미정)
| 티어 | 카드 | 효과 |
|---|---|---|
| 1 | 사례 | 전투 종료 시 골드 +N |
| 1 | 속결 | 5턴 안에 이기면 골드 +N |
| 1 | 사두다 | 골드를 내고 방어도 +N/턴 |
| 2 | 삯 | 처치 1체당 골드 +N |
| 2 | 개평 | 이긴 교환마다 골드 +N |
| 2 | 방패값 | 맞을 때마다 골드를 내고 방어도 +N |
| 3 | 곳간 | 보유 골드가 500 이상인 동안 `힘 1·∞` (상한 1). 500 미만이 되면 곳간의 힘만 꺼지고, 다시 500 이상이면 켜진다. 다른 출처의 힘은 건드리지 않는다 |
| 3 | 낙수 | 전투 종료 시 8% 확률로 아이템 3중 1 (미정) |
| 3 | 거둠 | 처치 시 확률(미정)로 증강, 전투 종료 후 획득 |

**경외**
| 티어 | 카드 | 효과 |
|---|---|---|
| 1 | 강림 | 획득 후 처음 시작하는 턴에 힘 2만큼 1턴 부여 |
| 1 | 가호 | 매 턴 시작 시 가호의 `힘 1·∞` 활성화. 그 턴 교환에서 처음 지면 **가호의 힘 1만** 꺼진다(턴당 1회 — 같은 턴에 다시 져도 변화 없음). 다음 턴 시작 시 다시 활성화. 다른 출처의 힘은 건드리지 않는다 |
| 1 | 걸음(가칭) | 획득 시 다음 턴 신속 1만큼 3턴 부여 — 예약이라 다음 턴부터 속도 굴림 전에 걸려 있다 |
| 2 | 머리 무한 | 머리 슬롯의 속도가 무한 ← 최대 빛 −1 |
| 2 | 응답 | 짓눌림 구간에서 턴이 끝나면 다음 턴 힘 2만큼 1턴 부여 |
| 2 | 순풍(가칭) | 짓누름 구간에서 턴이 끝나면 다음 턴 힘 1만큼 1턴 부여 |
| 3 | 제물(시간) | `힘 N·∞` — 증강이 유지되는 동안 감쇠하지 않는다. N **(미정)** ← 3턴 뒤부터 매 턴 약화되지 않은 부위 전부 약화(증강 자체 효과) |
| 3 | 제물(공간) | 머리 슬롯 +1칸(공격 · 도사림 겸용) → 공격 4칸 ← 다리 봉인 |
| 3 | 제물(자원) | 팔 두 슬롯의 속도가 무한 ← 최대 빛 −1 |

- 「속도 무한」 = 속도 보정이 항상 상한(+2)으로 붙는다. 처리 순서와는 별개

**동경**
| 티어 | 카드 | 효과 |
|---|---|---|
| 1 | 때림 | 내가 때린 교환마다 위세 충전 +1 |
| 1 | 맞음 | 내가 맞은 교환마다 위세 충전 +1 |
| 1 | 합 | 합이 붙을 때마다 위세 충전 +2 |
| 2 | 균열 | 위세를 발동할 때마다 적에게 `균열 3·∞` 부여 — 발동마다 독립 항목(1회 3 / 2회 6 / 3회 9), 자연 감쇠 없음 |
| 2 | 즉시 풀 충전 | 얻는 순간 위세 게이지 +50 (임계 1회분) |
| 2 | 누적 | 얻은 뒤 경과한 턴 수 × 6만큼 합 시작 충전이 커진다 |
| 3 | 비축 | 게이지 최대치가 임계의 2배가 되어 2회분을 쟁여둘 수 있다 |
| 3 | 환전 | 매 턴 시작 시 위세 게이지가 임계에 모자라면 그만큼 내 흐트러짐에서 당겨온다 |
| 3 | 전환 | 위세를 더 이상 쓰지 못하고 동경 1 · 2티어도 없는 것으로 취급하는 대신, `힘 1·∞` · `신속 1·∞`를 얻는다(전환이 적용되는 동안 감쇠하지 않는다) |

**그리움** — 충전 ±3 값은 (미정)
| 티어 | 카드 | 효과 |
|---|---|---|
| 1 | 몰이 | 짓누름 고조 충전 +3 / 중립 · 우세 −3 |
| 1 | 버팀 | 짓눌림 · 열세 고조 충전 +3 / 짓누름 −3 |
| 1 | 저울(가칭) | 중립 구간 확장 (우세 임계 30 → 40) |
| 2 | 유지 | 짓누름(양수) 바가 다음 턴으로 이월된다 |
| 2 | 잔류 | 짓눌림(음수) 바가 다음 턴으로 이월된다 |
| 2 | 호흡(가칭) | 턴 종료 시 바가 중립이면 빛 +1 |
| 3 | 왕귀(짓누름) | 연속 짓누름 2턴 → 무조건 파괴(죽음의 저항 해제) + 적 체력 내성 전부 2.0 |
| 3 | 왕귀(짓눌림) | 연속 짓눌림 2턴 → **부활** |
| 3 | 왕귀(중립) | 중립 3턴 → **되풀이하는 턴** |

- **부활** — 전체 체력 30% · 모든 부위 약화 상태로 · 전투당 1회 · 부위 전멸로 죽는 경우도 동일 · 연속 짓눌림 카운터가 끊긴다
- **되풀이하는 턴** — [해결] 단계만 한 번 더. **내 슬롯만**(적은 한 번만 행동). 같은 턴의 두 번째 해결이라 턴 카운터 · 바 리셋 · 빛 턴 회복 · 1턴 상태이상 만료 · 턴 종료 효과는 **한 번씩만**. 되풀이에도 빛을 낸다

### 14.4 감정 × 캐릭터 상성
감정 하나 × 캐릭터 셋 = 21가지 조합이 런의 성격을 정한다. 결함이 아니라 설계다.
- **연민 × 히후미 = 친화**
- **신의 × 히후미 = 역행** — 뼈는 실제로 받은 피해 기준이라 방어도가 뼈를 깎는다. 답은 게임 안에 있다(히후미 도사림 「포커페이스」). 감정 증강에 「뼈 되사기」를 복제하지 않는다

---

## 15. 스킬 강화

범용 공격레벨 스탯은 없다. **정비 노드에서 골드로 개별 스킬을 강화**한다.

- **문서에 적힌 모든 스킬 수치 = 강화 2회를 마친 「만렙」 기준이다.** 기본 스킬은 거기서 깎아 만든다 — **문서 숫자를 기본값으로 넣지 말 것**
- **강화 상한 = 카드당 2회**
- 강화는 카드에 귀속된다

| 라인 | 1회차 비용 | 강화가 키우는 것 |
|---|---|---|
| 평타 | 50 | 무조건 적용되는 잔효과. 위력은 토큰 수준 |
| 도사림 | 100 | 그 도사림의 핵심 수치 |
| 결투 | 125 | 위력 범위 + 교환별 승리 · 패배 부가효과를 함께 소폭 |
| 위세 | 150 | 효과 규모만. **충전 속도 · 발동 임계는 안 건드린다** |

- 비용과 보상 두 수치가 묶인 카드는 **비용은 토큰(+0~1), 보상만 크게** 강화한다
- 결투는 교환 단위 효과라 굴림 수만큼 곱해진다(3굴림이면 flat +1이 합당 +3) — 강화 폭을 작게
- 유진 결투 B 「잔혹한 마무리」는 예외적으로 무기마다 강화가 키우는 대상이 다르다(18.4)
- 2회차 비용 **(미정)** · 평타 강화를 공통으로 할지 카드별로 할지 **(미정)**

---

## 16. 아이템 · 런 경제

### 16.1 아이템 층
| | 1티어 | 2티어 | 3티어 | 4티어 | 5티어 | 직업 |
|---|---|---|---|---|---|---|
| 등장 확률 | 40% | 25% | 15% | 8% | 4% | 8% |
| 정가(골드) | 400 | 550 | 700 | 850 | 1,000 | 900 |
| 판매가(33%) | 132 | 182 | 231 | 280 | 330 | 297 |
| 종류 | 10 | 8 | 6 | 4 | 2 | 캐릭터당 10 |

- 공용 30종 + 직업 10종 × 3캐릭터. **직업 아이템에는 티어가 없다**
- **고유 축 = 위력** — 증강이 위력을 건드리지 않으므로 아이템에 남은 자리다
- 아이템 효과 값 **(미정)**

### 16.2 획득
| 경로 | 주는 것 | 방식 |
|---|---|---|
| 엘리트 클리어 | 무작위 1개 (직업 포함) | 선택 없음 |
| 1 · 2스테이지 보스 클리어 | 직업 아이템 | 3종 중 택 1 |
| 상점 매대 5~6칸 | 전부 | 골드 구매. 리롤할 때마다 비용 1.5배 |

- **보유 한도 없음 · 판매 가능(정가의 33%)**
- 직업 아이템은 **안 가진 것만 뜬다.** 판 것도 그 런에서 다시 안 뜬다(획득 이력 기준)
- 런당 획득 기대치 약 **9.5개**
- 확률표는 위 획득 구조를 전제로 한다 — 선택 방식을 바꾸면 확률도 다시 잡는다

### 16.3 골드 · 노드 보상
- **골드 기준값 n = 75 + 25 × (스테이지 − 1)** → 1스테이지 75 / 2스테이지 100 / 3스테이지 125

| 노드 | 보상 |
|---|---|
| 일반전투 | 골드 n |
| 엘리트 · 턴제한 전투 | 골드 1.5n × (0.9~1.1) + 각 노드 보상 |
| 골드 노드 | 골드 2.5n × (0.9~1.1) |
| 정비 | **75골드** = 부위 1곳 상태 회복(약화 · 파괴 해제) + 최대체력 25% 회복 · 스킬 강화 |

- **상점은 두 노드로 나뉜다** — 골드 상점 / 체력 상점. 보스 직전 고정 상점만 플레이어가 고른다
- 체력 상점 환율 — 현재체력 1 = 2.5골드 (미정) · 최대체력 1 = 현재체력 3
- 스테이지 클리어 시 체력 전부 회복(무료 회복 노드 없음)

---

## 17. 올라프 — 주사위 · 출혈 · 광기

**컨셉**: 부위가 하나씩 망가질수록 강해지는 광전사. 기세 바에 가장 덜 기대고, **출혈로 바와 무관하게 부위를 썩혀 터뜨린다.**
- 소스 = 주사위(균등 1~8), 크리 없음
- 어휘 계열 = 노르드 전사. 원어 스킬명은 툴팁 첫 줄에 원어 병기(blóðörn · hólmganga · blót)

### 17.1 패시브 — 「절름발이도 말을 탄다」 (하바말 71)
패시브 칸은 하나, 엔진은 둘이다.

**① 출혈 — 「적에게 평화를 주지 마라」** (올라프 전용 키워드)
- **적립** — 올라프가 **때린 교환 1회당**, 때린 그 부위에 쌓인다. 평타 · 결투 공통, 결투 게이트와 무관
  - RED 평타가 이긴 교환의 기본 출혈량은 광기에 따라 오른다 — **광기 0~3 → 1 / 4~7 → 2 / 8(만개) → 3**
- **도트 + 감쇠** — 턴 종료 시 부위별로 **스택 수만큼 그 부위에 피해 → 스택 −1**, 0이면 제거
  - 예 — 출혈 5 → 턴 종료 5 피해 후 4 → 다음 턴 4 피해 후 3 → … → 0 제거
- **N · T 상태가 아니다**(11.1 고유 누적 키워드) — 부위별 누적 스택, **재부여 시 스택 가산**, 자연 지속시간 없음. 폭발 · 소비 효과는 현재 출혈 수치를 참조한다
- **폭발** — **출혈 폭발 특전을 가진 파괴형 결투**(표준 · 단칼)가 결투 대 결투 교환에서 이기면 그 부위 출혈 **스택 × 10**(계수 미정) 피해, 스택 전량 소비. 합당 1회(첫 승리 교환)
- 출혈 소비처 셋 — 도트 자연 소모 / 폭발 / 카드로 현금화(피를 흡수하다)
- **자기 출혈** — 부위별 누적 스택. 카드를 낸 슬롯의 부위에 쌓이고, 턴 종료 시 그 부위에 스택만큼 피해를 준 뒤 스택 −1(0이면 제거)
  - 자기 출혈은 적 출혈과 같은 도트 · 감쇠 구조만 쓴다. **부위 약화 가속 같은 추가 기본 효과는 없다**

**② 광기 — 「많이 마실수록 자기 마음을 잃는다」** (자기 자원)
- **상한 8 · 단위 4** — **광기 4당 위력 +1 이면서 받는 피해 +1.** 상한(8) = 「만개」
- **적립** — 결투 대 결투에서 **진 교환마다 +1** · BLUE 평타 승리 +1 · 부위 파괴(상대 · 자기) · 강한 도사림 · 카드 효과
- **소비 · 초기화** — 활성 카드 풀에서는 「광기를 가라앉히다」 · 중형 광기 소비 결투 · 대형 광기 전량 소비 결투와 성장 「통제」 2~4단계가 광기 소비를 시도한다. 전투 중 저절로 줄지 않는다. 만개 상태에서 적을 처치했을 때 초기화되는지 **(미정)**
- 전용 키워드 **공포** — 값 없는 지속형(11.1). 걸려 있는 동안 상대 굴림 위력 −1(고정) · 효과 중첩 없음 · 재부여 시 지속 max 갱신. 블로토가 3턴 건다

### 17.2 활성 평타 풀 11종
기본 잔효과: **RED 승리 = 출혈(광기 단계별 1/2/3) · BLUE 승리 = 광기 +1.** 아래 고유 효과는 기본값에 더하거나(가산) 대체한다.

| # | 이름 | 색 | 굴림·빛 | 위치별 범위 | 고유 효과 |
|---|---|---|---|---|---|
| 1 | 물어뜯기 | RED | 3·0 | 9~20 / 10~21 / 10~21 | 각 굴림 **승리시** 균열/골절/쇠약 중 무작위 1종을 그 순간 결정 → 다음 턴 상대에게 해당 상태 1만큼 1턴 부여(다음 턴 시작에 다시 뽑지 않는다) |
| 2 | 방패벽 | BLUE | 3·0 | 11~29 / 13~20 / 13~16 | 1번째 **승리시** 이번 턴 자신에게 방어도 (미정). 2 · 3번째 굴림에는 별도 고유 효과 없음 |
| 3 | 버텨내기 | BLUE | 2·1 | 22~27 / 7~18 | 각 굴림 **승리시** 광기 1 획득 |
| 4 | 후려치기 | RED | 2·0 | 13~18 / 16~23 | 각 굴림 **승리시** 상대 출혈 (미정) + 광기 2 획득 |
| 5 | 몰아치기 | RED | 5·1 | 13~20 / 11~17 / 10~15 / 8~14 / 6~12 | 1~5번째 각 굴림 **사용시** 상대에게 출혈 1 |
| 6 | 다시 일어서다 | BLUE | 4·1 | 13~18 / 3~10 / 12~19 / 4~11 | 각 굴림 **패배시** 광기 1 획득 |
| 7 | 감싸기 | BLUE | 2·1 | 20~27 / 12~19 | 1번째 **사용시** 그 순간 현재 체력이 가장 낮은 자기 부위 1개를 고정 지정. 그 부위는 이번 턴 파괴되지 않고 약화까지만 가능하며 자동 재지정하지 않는다 |
| 8 | 틈을 파고들다 | RED | 3·1 | 13~18 / 15~22 / 17~29 | 1 · 2번째 **승리시** 각각 상대에게 균열 (미정) / 3번째 **승리시** 해당 공격 피해 적용 직전 상대 흐트러짐이 기준 (미정) 이하라면 그 굴림의 흐트러짐 피해 증폭. 3번째 굴림이 만든 흐트러짐 감소를 소급해 판정하지 않는다 |
| 9 | 선수치기 | BLUE | 2·0 | 18~23 / 13~18 | 1번째 **승리시** 다음 턴 이 슬롯의 속도 굴림값을 최댓값으로 고정. 2번째 굴림에는 별도 고유 효과 없음 |
| 10 | 약점을 노리다 | RED | 3·1 | 11~16 / 17~24 / 12~19 | 1번째 **승리시** 다음 턴 상대 쇠약 1만큼 3턴 / 2번째 **승리시** 그 순간 상대 출혈이 5 이상이면 다음 턴 상대 균열 1 / 3번째 **사용시** 그 순간 상대에게 걸린 부정적 상태 1종당 해당 굴림 위력 +1. 앞 굴림에서 새로 붙인 상태도 포함 |
| 11 | 저며내기 | RED | 2·1 | 16~19 / 20~32 | 2번째 **승리시** 상대 출혈 2 + 자신에게 자기 출혈 1 |

`쪼개기` · `받아넘기기` · `필사의 일격` · `숨통을 조이다`는 현재 활성 풀에서 제외한다. `막아서기`는 폐기 카드다.

### 17.3 활성 결투 풀 17종
결투 17장은 성장 「침잠 / 통제」와 별도로 센다.

| 분류 | 이름 | 색 · 유형 | 굴림·빛 | 위력 · 범위 | 효과 |
|---|---|---|---|---|---|
| 기본 | **표준(블로드외른)** | RED · 파괴형 | 2·2 | 12 · 13~20 / 14~19 | 각 굴림 **승리시** 대상 출혈 폭발. 파괴 권한 유지 |
| 기본 | **끈질기게(홀름강)** | BLUE · 유틸형 | 3·2 | 14 · 15~22 / 16~21 / 15~22 | 1 · 2번째 **사용시** 상대 출혈 1 / 3번째 **승리시** 자신에게 재생 2만큼 3턴 부여 |
| 형1 심화 | **단칼** | RED · 파괴형 | 1·1 | 16 · 16~21 | 1번째 **승리시** 출혈 폭발. 파괴 권한 유지 |
| 형1 가교 | **미쳐물어뜯기** | BLUE · 유틸형 | 3·2 | 12 · 13~20 균일 | 각 굴림 **사용시** 상대에게 출혈 1~2(무작위) 부여 |
| 형2 심화 | **피의돌진** | RED · 유틸형 | 2·0 | 13 · 15~22 / 11~18 | 각 굴림 **사용시** 상대 출혈 1 / **승리시** 다음 턴 상대 골절 1만큼 1턴 / **패배시** 광기 1 획득 |
| 형2 가교 | **물고 늘어지기** | BLUE · 유틸형 | 3·1 | 15 · 14~21 / 16~23 / 18~25 | 1 · 2번째는 별도 고유 효과 없음 / 3번째 **승리시** 다음 턴 상대 균열 2만큼 2턴 |
| 형3 심화 | **제살깎기** | BLUE · 유틸형 | 2·2 | 16 · 17~24 / 18~23 | 1번째 **승리시** 광기 1 / 1번째 **패배시** 자기 흐트러짐 피해 20 / 2번째 **승리시** 광기 4 획득 |
| 형3 가교 | **물러서지않기** | RED · 파괴형 | 2·1 | 10 · 8~21 / 9~20 | 각 굴림 **사용시** 현재 자기 약화 부위 1개당 해당 굴림 위력 +2. 각 굴림 **승리시 · 패배시** 광기 1 획득. 라그나로크 중에는 패배시 광기 3 |
| 유연 | **정면승부** | RED · 파괴형 | 2·0 | 14 · 15~22 / 16~21 | 각 굴림 **승리시** 해당 굴림이 실제로 준 피해의 35%만큼 자신도 피해. 파괴 권한 유지 |
| 유연 | **굳히기** | BLUE · 유틸형 | 3·2 | 15 · 16~23 / 17~22 / 16~23 | 각 굴림 **사용시** 그 순간 상대의 현재 출혈 구간을 다시 확인해 자신에게 다음 턴 힘 부여 — 출혈 0~4 / 5~9 / 10 이상 → 힘 0 / 1 / 2만큼 1턴. 앞 굴림으로 출혈이 늘면 뒤 굴림은 새 구간 사용 |
| 신규 | **피의 대가** | BLUE · 유틸형 | 2·2 | 16 · 14~19 / 21~28 | 1번째 **사용시** 자기 출혈 (미정) 추가 → 증가 후 현재 자기 출혈 구간 판정 → 자신에게 힘 (미정) 부여. 순서를 고정한다 |
| 신규 | **피의 맹세** | RED · 파괴형(조건부) | 2·1 | 12 · 13~20 / 14~19 | 1번째 **사용시** 자기 출혈 전부 제거 → 제거량 기록 → 제거량 구간별 보상 — 0~4 추가 없음 / 5~9 위력 +2 · 재생 2만큼 3턴 · 1번째 **승리시** 상대 출혈 2 / 10 이상 위력 +4 · 각 굴림 **승리시** 상대 출혈 2 · 빛 +1 · **처형 권한**. 상위 조건은 하위 보상 사다리를 대체하지 않는다 |
| 신규 | **닥치는 대로** | RED · 파괴형 | 5·4(가변) | 8 · 9~16 / 10~15 / 9~16 / 10~15 / 9~16 | 1번째 **사용시** 광기 1 획득 + 다음 사용부터 비용 −1(전투 내 누적, 최저 1). 파괴 권한 유지 |
| 신규 | **명예로운 전투** | RED · 파괴형 | 1·3 | 17~23 | **전투 시작시** 상대가 사용하려던 스킬을 전용 대응 스킬로 강제 교체하고 양쪽이 특수 판정 구조로 진입. 버프·디버프는 정상 적용. 재굴림 세부 규칙과 종료 조건은 **(미정)** |
| 신규 | **일기토** | RED · 파괴형 | 3·1 | 9 · 10~17 / 11~16 / 10~17 | **조준 단계 독점** — 일기토가 상대 슬롯 A를 조준하면 올라프의 다른 슬롯은 A를 추가 조준할 수 없고 기존 조준은 해제된다. A→B 변경 시 A 잠금 해제·B 잠금, 카드 취소·교체·슬롯 해제 시 잠금 해제, 기존 타깃 자동 복구 없음, 해결 단계 진입 시 조준 고정. 각 굴림 **사용시** 상대 출혈 2 |
| 광기 소비 | **중형 광기 소비 결투(이름 미정)** | (미정) | (미정) | (미정) | 1번째 **사용시** 광기 N 소비 시도. 성공하면 상대에게 공포를 부여하고, 이후 이 스킬에서 실제 교환을 **승리할 때마다** 상대 출혈 N 추가. 실패하면 공포·추가 출혈만 불발하고 카드와 결투는 정상 진행 |
| 광기 소비 | **대형 광기 전량 소비 결투(이름 미정)** | (미정) | (미정) | (미정) | **전투 시작시** 현재 광기 전부 소비 → 실제 소비량 기록 → 소비량 구간에 따라 최종 굴림 수 확정. 최고 구간이면 추가 출혈 보상 활성화. 합 도중 광기가 바뀌어도 굴림 수는 다시 바뀌지 않는다 |

- **파괴형** = 결투 대 결투 교환에서 이기면 **파괴 권한**을 갖는다. **유틸형** = 파괴 권한 없음
- **출혈 폭발은 파괴형 공통이 아니다** — 표준 · 단칼 둘만 가진다
- 결투 대 결투에서 진 교환은 카드와 무관하게 **광기 +1**(패배 메리트)
- 결투 3칸은 유형 제한 없이 채운다

현재 활성 결투 풀에서 `위세를 지피다` · `몸을 사리다` · `허를 찌르다` · `전부 걸다` · `피를 흡수하다` · `전부 되갚다`와 그 밖의 과거 후보는 제외한다.

### 17.4 활성 도사림 10종
| 이름 | 분류 · 빛 | 효과 |
|---|---|---|
| 웅크리기 | 공용 · 0 | **도사림 발동시** 방어도 12 획득 |
| 노려보기 | 공용 · 1 | **도사림 발동시** 이번 턴 전체 판정 +1 |
| **블로토(blót)** | 강한 · 1 | **도사림 발동시** 자기 최저 체력 부위 약화 + 광기 2 획득 + 적에게 공포 3턴 부여 |
| 피의 서약 | 강한 · 1 | **도사림 발동시** 자신에게 자기 출혈 2 + 재생 4만큼 3턴 부여 |
| 피로 사는 숨 | 약한 · 0 | **도사림 발동시** 자신에게 자기 출혈 2 + 빛 1 획득 |
| 광기를 가라앉히다 | 강한 · 0 | **도사림 발동시** 광기 1 소비 시도. 성공하면 이번 턴 실제 교환을 승리할 때마다 상대에게 추가 출혈 1. 실패하면 효과만 불발하고 도사림 사용은 유지 |
| 상처를 지우다 | 강한 · 1 | **도사림 발동시** 현재 자기 출혈 확인. 5 이상이면 자기 출혈 5 소비 + 이번 턴 위력 +2, 5 미만이면 효과 없음 |
| 흐트러짐을 삼키다 | 강한 · 1 | **도사림 발동시** 자기 흐트러짐 20 소비 → 체력 20 회복 |
| 최후의 저항 | 강한 · 1 | **하드 게이트** — 지난 턴을 짓눌림으로 끝냈을 때만 사용 가능. **도사림 발동시** 이번 턴 자기 공격 슬롯의 속도 계열을 크게 강화. 정확한 수치 **(미정)** |
| 자기 출혈 소비 방어형(이름 미정) | 강한 · 1 | 체력과 무관하게 선택·사용 가능. **도사림 발동시** HP가 50% 미만이면 자기 출혈 전부 제거 → 제거한 양 1당 방어도 1. 50% 이상이면 조건부 효과만 불발하고 카드 사용은 유지 |

- 블로토는 「전투당 1회 사용」 기준으로 수치가 잡혀 있다 — 도배하면 결투를 못 꽂아 자멸한다
- 캐릭터 간 대칭 — 올라프 = 흐트러짐 → 체력 / 히후미 = 체력 → 흐트러짐

### 17.5 위세 3형
| 형 | 이름 | 임계 | 효과 |
|---|---|---|---|
| 1 | **룬 새기기** | 60 | 조준 부위에 출혈 3 + 광기 단계당 +1 (총 3~5). 셋업 가속 — 같은 턴 파괴형 결투와 콤보 |
| 2 | **베르세르크** | 65 | 광기 +2 → 현재 광기 × 5 피해 + 대상 부위 출혈 스택만큼 가산(출혈 소비 없음 · 파괴 권한 없음) |
| 3 | **라그나로크** | 70 | 한 턴 불사 — 그 턴 자기 부위는 파괴되지 않고 체력 1 아래로 안 내려간다(사망 · 적 파괴 스킬 포함). 불사 턴 동안 맞은 교환당 광기 +1 |

- 형 구조 — 형1 = 출혈 축(가교 광기) / 형2 = 광기 축(가교 출혈) / 형3 = 자해 축(가교 광기)

### 17.6 성장 스킬 — 「침잠 · 통제」
히후미 「몸에 새기다」(19.5)와 같은 **런 전체 영구 성장** 카드다. 두 축 — **침잠**(광기에 자신을 맡긴다) · **통제**(광기를 자신의 것으로 다룬다).

**성장 트리거**

| 축 | 진행 조건 |
|---|---|
| **침잠** | **턴 종료 시 광기 8(만개)을 유지**하고 있으면 +1 |
| **통제** | 만개 상태에서 **자신의 효과로 광기를 자발적으로 소비해 8 아래로 내려가면** +1 (한 번의 소비 행동 = 1) |

단계별 임계치 **(미정)**.

**공통 1단계 — 2굴림**
- 1번째 굴림은 기본
- 2번째 **승리시** 광기 N 획득 + 같은 양만큼 자기 출혈 획득 + 같은 양만큼 상대에게 출혈 부여

| 단계 | 굴림 수 | 침잠 | 통제 |
|---|---:|---|---|
| 2 | 3 | 3번째 굴림 추가. 3번째 **사용시** 현재 만개라면 해당 굴림 위력 증가 | 3번째 **사용시** 광기 N 소비 시도. 성공하면 해당 굴림 안정화·강화 |
| 3 | 3 | 3번째 **사용시** 만개라면 자기 출혈 N 추가 + 해당 굴림을 2단계보다 크게 강화 | 2단계 효과 유지. 3번째 **사용시** 광기 소비 성공 시 자기 출혈 전부 제거 |
| 4 | 4 | 4번째 **사용시** 광기 N + 자기 출혈 N 획득. 증가 후 만개 조건을 만족하면 추가 보상·해당 굴림 위력 강화 | 4번째 **사용시** 광기 N 소비 시도. 성공하면 자기 출혈 전부 제거하고 제거량을 기준으로 재생 획득 |

- 공통 굴림 수 = **1단계 2 / 2단계 3 / 3단계 3 / 4단계 4**
- 분류(평타/결투) · 빛 · 기본위력 · 위치별 범위 · 단계별 광기/출혈량 · 강화량 · 안정화 수치 · 침잠 4단계 추가 보상 · 재생 환산량과 지속 · 단계별 임계치는 **(미정)**

### 17.7 미정
- 시작 지급 세트
- 직업 아이템
- 폭발 계수 · 단칼 위력 · 결투 빛 비용 구조
- **광기 소비처** — 통제 트리가 광기 소비 행동을 요구하는데 지금 소비처가 적다. 스킬 풀을 최종 선별한 뒤에 다시 본다

---

## 18. 유진 — 코인 · 무기 · 표식

**컨셉**: 실질적 지배자에게 고용된 베테랑 청부살인자. 누구보다 신에 가까운 권력자들 밑에서 일하지만, 정작 본인은 그들이 휘두르는 괴력난신을 입에 담지도 쓰지도 않는다. 신을 믿는 다른 이들과 달리, 언젠가 이 판을 스스로 뒤엎을 날을 벼른다. 상황에 맞는 무기를 골라 싸우고 기회를 엿본다.
- 소스 = 코인. **무기가 코인 수 · 앞면 확률 · 크리값 · 처형 권한 · 표식 발화 · 타격 타입을 통째로 정한다.** 스킬은 부과 효과만 가진다
- 어휘 계열 = 청부업(스킬) / 일제강점기 한국 문학 · 한문 고전 인용(패시브)

### 18.1 무기 3종
| 무기 | 코인 | 앞면 | 크리값 | 앞면 판정 | 뒷면 판정 | 타입 | 처형 | 표식 발화 | 표식 부여(평타/결투) |
|---|---|---|---|---|---|---|---|---|---|
| **백우**(총) | 3 | 50% | 7 | 18 (곡선 11 + 7) | 13 | 관통 | 없음 | 바 푸시 50 | 6 / 12 |
| **적설**(쌍편곤) | 2 | 50% | 8 | 20 (곡선 12 + 8) | 13 | 타격 | 없음 | 봉인 1턴 | 12 / 24 |
| **낙일**(오오타치) | 1 | 20% | 24 | 44 (곡선 20 + 24) | 13 | 절단 | **있음** | 그 부위에 확정 체력 피해 (미정) | 8 / 16 |

- 코인 수 = 그 무기로 쓰는 스킬의 **굴림 수**다 — 백우 3굴림 · 적설 2굴림 · 낙일 1굴림
- 뒷면 = 2 공통 → 뒷면 판정 = 기본위력 11 + 2 = **13**(+ 빛 × 2)
- 역할 — **백우 = 빌드업**(빠르게 표식을 쌓아 바를 민다) / **적설 = 디버프 · 봉인** / **낙일 = 처형**
- **백우 전용 — 타격당(이긴 굴림마다) 흐트러짐 게이지에 추가 피해 +20**(체력 피해 아님). 창이 열리면 그 턴 내성이 전 타입 ×2가 되어 백우의 딜이 그 턴에 두 배로 들어간다 — 백우가 「짓누름 → 딜」을 바꾸는 통로. 무기 전용(공용으로 두면 굴림 수가 많은 캐릭터가 더 가져가 균형이 깨짐)
- **낙일 무기 효과** — **교환 승리시** **상대의 남은 굴림을 전부 삭제**한다(평타로도 발동)
- **처형(낙일)** — **낙일의 모든 타격**은 **처형**(5장) 권한을 가진다(앞면 · 뒷면 무관, 무기 기본 효과). 그 타격의 피해가 대상 부위의 남은 체력을 0 이하로 만들 수 있으면 **정상 → 약화 → 파괴를 한 타격 안에서** 처리한다. 대상 부위가 미리 약화돼 있을 필요가 없다
- **낙일은 「약화 상태」에 기대지 않는다** — 표식 발화는 그 부위의 체력을 깎을 뿐 파괴 권한이 없다. 낙일 카드의 조건도 「대상이 약화 상태라면」이 아니라 **「대상 부위의 현재 체력이 최대 체력의 (미정)% 이하라면」**으로 적는다
  - 흐름 — 피해 · 표식 발화로 부위 체력을 깎는다 → 일정 체력 이하에서 낙일 카드 보너스가 켜진다 → 낙일 타격의 피해가 남은 체력을 넘기면 처형으로 파괴

### 18.2 패시브 — 「군자불어괴력난신(君子不語怪力亂神)」
세 축을 묶는다.

| 축 | 이름 | 하는 일 |
|---|---|---|
| 표식 | **낙점** | 조준 부위에 표식 적립 → 상한 44에서 무기별 발화 |
| 코인 | **환형** | 준비 행동으로 무기 전환 |
| 재굴림 | **살수의 감** | 재굴림 자원 |

**표식(낙점)** — 유진 전용 키워드
- **낙점** — 실제 교환에서 **이긴 굴림마다** 조준 부위에 표식을 적립한다(부여량은 무기 표). 일방 공격은 승리가 아니므로 낙점이 붙지 않는다. **카드 효과로 붙는 표식은 낙점과 별도로 더해진다**
- 부위별 누적(부위 없는 몹은 몹 하나가 단위 — 1.6), **자연 감쇠 없음.** N · T 상태가 아니다(11.1 고유 누적 키워드)
- 즉시 추가와 **다음 턴 예약 추가**가 모두 있다. 예약 표식은 다음 턴 시작, **속도 굴림 전에** 실제로 적립된다
- **표식이 실제로 추가될 때의 처리**
  1. 기본 표식 증가량 계산
  2. 현재 지정 총합을 더한다
  3. 표식에 더한다
  4. 결과가 **44 이상이면 즉시 발화**
  5. 발화하면 표식을 소비한다
  6. 초과분은 이월하지 않는다(예외 — 성장 스킬 집행 3단계, 18.8)
- 발화는 별도의 턴 페이즈가 아니다 — **표식이 실제로 적립되는 경로 안에서** 판정한다. 폭발 덫 · 공명 덫처럼 발화에 걸린 효과도 이 같은 발화 사건을 기준으로 한다
- 대상 수 제한 없음 · 무기를 바꿔도 유지
- 적설은 두 곳에 동시에 걸 수 있다(디버프형)

**지정** — 유진 전용 키워드, 수치형 상태(11.1). 지정된 부위에 표식이 실제로 쌓일 때마다 **추가 표식 + 현재 지정 총합 N.** 부여량 · 지속은 카드가 정한다

**환형(무기 교체)**
- 준비 행동, **빛 1.** 쓸 때 **지금 안 든 무기 2개 중 하나를 고른다**(선택 UI)
- **무기 교체는 다음 턴부터** 적용된다. **카드 보너스(공격 · 수비)는 쓴 그 턴에 바로** 적용된다
- 도사림 카드다 — **환형(공격)과 환형(수비) 중 하나만** 장착. 다리 · 머리 어느 도사림 칸에서든 쓸 수 있다
  - 환형(공격) — 교체 + 그 턴 위력 +N (교체 **전** 무기 기준: 낙일 8 / 적설 4 / 백우 2)
  - 환형(수비) — 교체 + 그 턴 방어도 +30

**살수의 감**
- **획득** — 「유비무환」: **매 턴 +1**. 그 밖에 카드 효과 · 파괴 · 처치. **상한 없음, 전투 종료 시 초기화**
- **사용 ① 재굴림** — **슬롯 배치 단계에서 그 슬롯에 쓸지 지정**한다. 이후 그 슬롯의 **지는 교환은 스택이 남는 한 자동으로 다시 굴린다.** 결과는 확정(되돌리기 없음). 합 도중 프롬프트 없음
- **사용 ② 카드 효과** — 잔혹한 마무리 · 믿는 구석 · 때를 앞당기다 · 끝장을 보다 · 숨을 죽이다 · 칼을 갈다 등이 소비한다(자동)
- **사용 ③ 패 보기 (결투 한정)** — 결투 대 결투 판정에서 **상대 결과가 먼저 보이고**, 살수의 감이 1 이상이면 「진행 / 스택 1을 써서 접기」를 고른다. 접으면 그 굴림은 승패도 피해도 없다. 0이면 UI 없이 진행. 평타에는 없다
  - 패 보기의 UI 흐름 **(미정)**

**유진 카드 공통 원칙** — 무기마다 굴림 수가 다르므로(3 / 2 / 1), 효과는 되도록 「스킬당 1회」가 아니라 **각 굴림에 실제로 배치**한다
- **전투 시작시**(3.4) — 첫 굴림 전에 정해져야 하는 스킬 전체 구조에만 쓴다(무기 프로필 교체 · 전 굴림 확정 크리 · 자원 풀 생성 등)
- **사용시** — 그 굴림이 실제로 일어나면 판정 전에 발동한다. **일방 공격에서도 발동한다**
- **승리시 · 패배시** — **실제 교환**이 있었을 때만 발동한다. 상대 굴림이 없어 일방 공격이 되면 승리가 아니다
- 같은 턴 안에 바뀔 수 있는 자원 · 상태는 카드 사용의 하드 게이트로 쓰지 않는다 — **카드는 낼 수 있고, 효과 시점에 조건을 보고, 모자라면 그 추가 효과만 실패**한다. 지난 턴에 확정된 사실처럼 이번 턴에 바뀌지 않는 조건만 하드 게이트로 쓴다

### 18.3 평타 10장
| 이름 | 색 | 빛 | 효과 |
|---|---|---|---|
| **뒷조사** | RED | 0 | 고유 효과 없음 — 이긴 굴림에 **낙점만** 붙는다. 유진의 기준 평타 |
| **숨 고르기** | BLUE | 2 | 낙점 없음. 각 굴림 **승리시** 살수의 감 +(미정) |
| **장부 정리** | RED | 0 | 사용 조건 없음. 표식이 모자라면 그 굴림의 환전만 실패한다. 백우: 각 굴림 **사용시** 대상 부위 표식 (미정) 소비 시도 → 성공하면 빛 +(미정). 앞 굴림 승리의 낙점으로 표식이 다시 쌓이면 다음 굴림에서 다시 시도한다 / 적설: 1굴림 **사용시** 표식 (미정) 소비 시도 → 성공하면 빛 +(미정) · 2굴림 **승리시** 1굴림 정산이 성공했으면 소비한 표식 중 (미정)을 대상에게 되돌려준다 / 낙일: 1굴림 **사용시** 표식 (미정)을 한 번에 소비 시도 → 성공하면 빛 +(미정) |
| **정조준** | RED | 1 | 각 굴림 독립 — 실제 교환에서 **승리**했고 그 굴림이 **앞면(크리)**이면: 백우 대상 표식 +(미정) / 적설 다음 턴 골절 (미정) / 낙일 확정 추가 피해 (미정). 낙점은 별도로 붙는다 |
| **확인 사살** | RED | 1 | 각 굴림 **승리시** 조준 부위가 「지정」 상태면 기세 바 +(미정). 지정이 아니면 추가 효과 없음(카드 사용은 제한하지 않는다) |
| **손에 익히다** | BLUE | 0 | **조건**: 지난 턴 환형이 완료돼 이번 턴부터 새 무기가 활성화된 첫 턴에만 사용 가능. 각 굴림 **사용시** 살수의 감 +(미정)(백우 최대 3회 · 적설 2회 · 낙일 1회) |
| **믿는 구석** | RED | 1 | **전투 시작시** 살수의 감 (미정) 소비 시도 → 성공하면 이 스킬의 모든 굴림을 앞면(크리)으로 취급. 실패하면 추가 효과만 없고 스킬은 정상 진행. **확정 크리는 자동 승리가 아니다** — 승패는 정상 판정하고 낙점은 실제로 이긴 굴림에만 |
| **현상수배** | BLUE | 1 | 각 굴림 **승리시** 다음 턴 **모든 적**에게 — 백우 쇠약 / 적설 골절 / 낙일 균열 (미정). 굴림마다 반복 발동한다 |
| **은신 부여 평타**(가칭) | (미정) | (미정) | 각 굴림 **사용시** 이 카드를 쓰는 자기 부위에 **다음 턴 은신 (미정) 누적** |
| **은신 보상 평타**(가칭) | (미정) | (미정) | 은신 여부와 무관하게 쓸 수 있다. 스킬 시작 시 이 카드를 쓰는 부위가 **은신이면** 각 굴림 **사용시** 대상에게 표식 +(미정). 은신이 아니면 추가 표식 없음. 굴림마다 은신을 쓰지 않는다 |

### 18.4 결투 15장
모든 유진 스킬은 **「백우 / 적설 / 낙일」 3줄로 표시**한다. 내용이 같으면 세 줄 같은 텍스트, 다르면 각각. 기본은 무기 무관 통일, 필요할 때만 분기한다. 모든 결투의 이긴 굴림에는 **낙점이 별도로** 붙는다(무기별 결투 부여량).

| | 이름 | 색 | 빛 | 효과 |
|---|---|---|---|---|
| A | **의뢰 대상** | RED | 1 | 백우: 각 굴림 **사용시** 기세 바 +(미정) / 적설: 각 굴림 **사용시** 대상에게 골절 (미정) / 낙일: 추가 사용시 효과 없음. 이 스킬로 **처형에 성공해 부위를 파괴하면** 살수의 감 +(미정) |
| B | **잔혹한 마무리** | RED | 1 | 각 굴림 **승리시** 살수의 감 1 소비 시도 → 성공하면 무저항 추가타(아래 상한 표). 모자라면 추가타 없음. **추가타는 승리시를 다시 일으키지 않고, 추가타를 다시 만들지 않는다** |
| C | **목표 지정** | BLUE | 1 | 1굴림 **사용시** 조준한 상대 부위에 지정 (미정)만큼 (미정)턴 부여. 첫 굴림 전에 적용되므로 1굴림 낙점부터 지정 보너스를 받는다 |
| D | **숨통을 끊다** | RED | 1 | 각 굴림의 피해 처리 뒤, **그 굴림이** 대상 부위를 약화시키거나 파괴했으면 **그 대상**에게 표식 +(미정) |
| E | **소문을 내다** | RED | 1 | 각 굴림의 피해 처리 뒤, **그 굴림이** 대상 부위를 약화시키거나 파괴했으면 **모든 적**에게 표식 +(미정) |
| F | **퇴로 차단** | RED | 1 | 효과 발동 시점에 기세가 **짓누름 구간**이면 — 1굴림 **승리시** 상대 다음 턴 빛 최대치 −(미정) / 2굴림 **승리시** 대상 표식 (미정) 소비 시도 → 성공하면 빛 +(미정) / 3굴림 **승리시** 기세 바 +(미정). 낙일은 1굴림 효과만 · 적설은 1~2굴림 · 백우는 1~3굴림 |
| G | **빌려 쓰다** | RED | 2 | **전투 시작시** 지금 안 든 무기 2개 중 **무작위로 하나**를 골라, 이 스킬에만 그 무기의 **전투 프로필 전체**(굴림 수 · 앞면 확률 · 크리값 · 타격 타입 · 처형 권한 · 낙점 표식량 · 무기 고유 규칙)를 쓴다. 스킬이 끝나면 원래 무기로 돌아간다 |
| H | **덫을 놓다** | RED | 1 | 각 굴림 **승리시** 대상 부위에 **폭발 덫** (미정)스택 설치 |
| I | **시한을 정하다** | BLUE | 1 | 각 굴림 **승리시** 대상 부위에 **공명 덫** (미정)스택 설치 |
| J | **마침표를 찍다** | RED | 1 | 각 굴림의 피해 처리 뒤 무기별 조건을 이루면 살수의 감 +(미정) — 백우: 그 굴림으로 적을 **처치** / 적설: 그 굴림으로 적을 **흐트러뜨림** / 낙일: 그 굴림으로 **처형에 성공**. 스킬당 제한 없음, 한 굴림에서 조건이 여러 번 잡혀도 1회 |
| K | **때를 앞당기다** | RED | 2 | 각 굴림 **사용시** 살수의 감 (미정) 소비 시도 → 성공하면 대상 부위 표식 +(미정). 그 결과 44에 닿으면 즉시 정상 발화한다. 모자라면 그 굴림의 추가 표식만 실패 |
| L | **끝장을 보다** | RED | 4(쓸 때마다 −1, 최소 1, 전투 내 누적) | **전투 시작시** 살수의 감을 **전부** 소비 → 실제 소비량에 비례한 **마무리 피해 풀** (미정)을 만든다. 이 스킬에서 실제로 적중하는 공격마다 풀에서 **확정 추가 피해**를 나눠 쓰고, 스킬이 끝나면 남은 풀은 사라진다. 이 스킬의 기본위력은 빛 2 기준으로 고정(비용이 내려가도 위력은 안 따라 내려간다) |
| M | **은신 부여 결투**(가칭) | (미정) | (미정) | 각 굴림 **사용시** 이 카드를 쓰는 자기 부위에 **다음 턴 은신 (미정) 누적** + 살수의 감 +(미정) |
| N | **은신 보상 결투**(가칭) | (미정) | (미정) | 은신 여부와 무관하게 쓸 수 있다. 스킬 시작 시 이 카드를 쓰는 부위가 **은신이면** 각 굴림 **사용시** 확정 추가 피해 (미정). 은신이 아니면 추가 피해 없음. 굴림마다 은신을 쓰지 않는다 |
| O | **성장 스킬** | (미정) | (미정) | 18.8 |

**B 「잔혹한 마무리」 강화별 추가타 상한** — 세 무기가 자라는 대상 자체가 다르다(적설은 디버프 · 봉인 역할이라 추가타 극대화 노선이 안 맞아 강화2에서야 합류)
| 무기 | 기본 | 강화 1 | 강화 2 |
|---|---|---|---|
| 백우 | 추가타 1회 | 위력 +1 | 위력 +1 |
| 적설 | 추가타 1회 | 위력 +1 | 추가타 2회 |
| 낙일 | 추가타 1회 | 추가타 3회 | 추가타 5회 |

- **숨통을 끊다 · 소문을 내다** — **한 타격에서 정상 → 약화 → 파괴가 연속으로 일어나도 1회만** 발동한다. 뒤 굴림이 따로 약화 · 파괴를 일으키면 다시 발동한다. 낙일 처형으로 직접 파괴한 경우도, 일방 공격이 실제 피해로 약화 · 파괴한 경우도 발동한다. 일반 몹은 처치를 파괴로 취급한다
- **빌려 쓰다는 환형이 아니다** — 장착 무기를 바꾸지 않으므로 환형 완료 조건이나 「손에 익히다」의 조건을 채우지 않는다
- **덫** — 적 부위에 스택으로 쌓는 유진 전용 상태 계열(11.3). 두 종류다
  - **폭발 덫** — 그 부위의 표식이 44에서 발화할 때 함께 발동해 **폭발 덫을 전부 소비**하고, 스택에 비례한 확정 피해 (미정)
  - **공명 덫** — 그 부위의 표식이 발화할 때 함께 발동해 **공명 덫을 전부 소비**하고, **현재 무기의 원래 표식 발화 효과를 강화**한다 (미정)
  - 덫은 **표식을 부여하지 않는다.** 처리 순서 — 굴림 승리 → 덫 설치 → 낙점 → 44에 닿으면 발화 → **방금 설치한 덫도 그 발화에 함께 발동**한다
- **때를 앞당기다는 발화를 직접 일으킨다** — 덫은 발화에 얹는 효과일 뿐 발화를 만들지 않는다. 덫이 걸린 부위에 쓰면 그 덫도 함께 발동한다
- **끝장을 보다의 풀은 하나의 총량이다** — 「소비 스택 × 굴림 수」로 불어나지 않는다. 백우는 3굴림에 나눠 쓰고, 적설은 2굴림, 낙일은 1굴림에 몰아 쓴다. 한 번 쓰고 나면 살수의 감을 다시 모을 때까지 다음 풀이 작다 — 「터뜨리고 다시 모으는」 리듬
- **마침표를 찍다** — 백우 · 적설은 처형 권한이 없어 조건이 처치 · 흐트러뜨림이다. 낙일은 부위 파괴 단위(처형)라 보스전 중간에도 여러 번 발동한다

### 18.5 도사림 10장
도사림은 굴림이 없으므로 **도사림 발동시**(3.4) 즉시 처리한다. 도사림은 상대를 조준하지 않는다.

| 이름 | 분류 · 빛 | 효과 |
|---|---|---|
| 웅크리기 | 공용 · 0 | 방어도 +12 |
| 노려보기 | 공용 · 1 | 그 턴 합 판정값 +1 |
| 노림수 | 강한 · 1 | 그 턴 코인 앞면 확률 +10%p |
| 선금 받기 | 강한 · 1 | 그 턴 코인 뒷면 값 +2 |
| 환형(공격) / 환형(수비) | 강한 · 1 | 18.2 참고 — 둘 중 하나만 장착 |
| 기회를 엿보다 | 강한 · 0 | 살수의 감 +1 |
| 숨을 죽이다 | 강한 · 0 | 살수의 감 (미정) 소비 시도 → 성공하면 방어도 +(미정). 모자라면 실패 |
| 칼을 갈다 | 강한 · 0 | 살수의 감 (미정) 소비 시도 → 성공하면 다음 턴 신속 (미정). 모자라면 실패 |
| 손질하다 | 강한 · (미정) | 지금 든 무기를 **손질 상태**로 지정한다 → **다음 턴** 그 무기로 이긴 굴림의 **낙점 표식 +(미정)**. 다음 턴이 끝나면 사라진다. 그 사이 환형으로 무기가 바뀌면 효과는 손질한 무기에만 남고 새 무기에는 붙지 않는다 |

- 노림수 = 앞면 확률 보정 / 선금 받기 = 뒷면 성능 보정
- 숨을 죽이다 = 살수의 감 → 지금의 안전 / 칼을 갈다 = 살수의 감 → 다음 턴의 공격 준비
- 환형 = 상황에 맞는 다른 무기로 바꾼다 / 손질하다 = 지금 든 무기를 계속 쓸 준비

### 18.6 위세 3형 (임계 전부 50)
| 형 | 이름 | 효과 |
|---|---|---|
| 1 | **밑작업 착수** | 조준 부위에 크리값만큼 표식(적설은 두 곳) + 그 턴 이긴 교환마다 표식 +3 |
| 2 | **확실한 일처리** | 확정 크리 한 방(1굴림) + 그 공격 중 처치 · 파괴 · 발화가 일어나면 연쇄(상한 4) |
| 3 | **유연한 대처** | 그 턴 뒷면은 자동으로 한 번 더 굴린다(같은 굴림 1회). 살수의 감 소모 없음 |

### 18.7 은신
자신의 부위에 거는 **조준 차단** 마커다. 히후미 「도발」(19.1)과 대칭이다.

- **은신 N** — N > 0인 부위는 **적이 조준할 수 없다**
- **행동 = 스킬 하나를 쓰는 것.** 은신 부위가 스킬 행동 하나를 마치면 **은신 −1**. 굴림 수와 무관하다 — 3굴림 스킬도 −1이다. 0이 되면 은신이 끝난다
- **턴 종료로 줄지 않고, 굴림마다 줄지도 않는다**
- **다음 턴에 부여하는 은신은 이번 턴에 이미 성립한 조준을 취소하지 않는다**
- 한 스킬의 여러 굴림이 부여하는 은신은 **누적**된다
- 이미 은신이 걸린 부위에 다시 부여할 때의 처리 **(미정)**
- **보상 카드** — 은신 여부와 무관하게 쓸 수 있다. 스킬 시작 시 **이 카드를 쓰는 부위**가 은신이면 추가 효과가 켜진다. 은신을 따로 소비하지 않는다 — 행동이 끝나면 위 규칙대로 −1

### 18.8 성장 스킬 — 「내가 결정하고, 내가 끝낸다」
히후미 「몸에 새기다」(19.5)와 같은 **런 전체 영구 성장** 카드이고, **결투**다(18.4 O). 두 축 — **집행**(한 번 정한 일을 끝까지 한다) · **판단**(상황을 보고 방법을 바꾼다).

**성장 트리거**

| 축 | 진행 조건 |
|---|---|
| **집행** | **표식 44 발화 1회당** +1. 자연 발화든 강제 발화(「때를 앞당기다」)든 실제로 터지면 인정한다 |
| **판단** | **살수의 감을 실제로 소비한 양 1당** +1 — 재굴림 · 패 보기 · 카드 효과 등 경로를 가리지 않는다 |

단계별 요구 진행도 **(미정)**.

**공통 몸체** — 이 스킬 자체가 **표식을 부여하는 공격**이다.
- **스킬 시작 시** 대상에게 표식이 **이미 있었는지** 본다 — 없으면 **기본 상태**, 있으면 **강화 상태**로 동작한다
- 이 스킬의 앞 굴림이 새로 붙인 표식으로 뒤 굴림이 강화되지 않는다. **스킬을 쓰기 전에 있던 표식**이어야 한다
- 각 굴림 **승리시** 이 스킬 고유의 표식 +(미정)(기본 · 강화 상태별). 낙점은 별도로 붙는다
- **성장 트리는 「지정」을 부여하지 않는다** — 「목표 지정」의 자리를 침범하지 않기 위해서다

**집행** — 앞 단계의 효과를 유지한다

| 단계 | 효과 |
|---|---|
| 2 | 이 스킬이 부여한 표식이 마지막 한 방이 되어 **직접 44 발화를 일으켰다면**, 발화 처리 뒤 그 부위에 표식 +(미정)을 다시 남긴다 |
| 3 | 이 스킬이 직접 발화를 일으킬 때 **44를 넘친 표식을 버리지 않고 보존**한다(18.2 「초과분 이월 없음」의 예외). 2단계의 재부여도 그 위에 더해진다 — 예: 기존 40 + 이 스킬 +8 → 발화 → 넘친 4 보존 + 재부여 |
| 4 | 이 스킬이 부여한 표식이 **직접 44 발화를 일으켰다면** 현재 무기의 **원래 표식 발화 효과를 2배**로 발동한다(백우 · 적설 · 낙일 각자의 발화 효과). 그 턴에 발화가 있었다는 것만으로는 안 된다 |

**판단**

| 단계 | 백우 | 적설 | 낙일 |
|---|---|---|---|
| 2 | 강화 상태에서 각 굴림 **승리시** 추가 흐트러짐 피해 +(미정) | 강화 상태에서 각 굴림 **승리시** 대상에게 골절 (미정) | 강화 상태에서 부위 피해 +(미정) |
| 3 | 이 스킬이 준 흐트러짐 피해가 대상의 남은 흐트러짐을 넘으면 **넘친 만큼을 체력 피해로 바꾼다**(비율 (미정)) | **봉인된 부위**를 대상으로 쓰면 각 굴림 **사용시** 골절 (미정) 추가 — 봉인된 부위는 행동이 없어 일방 공격이 될 수 있으므로 사용시로 처리한다 | 강화 상태에서 적중하면 **고정 부위 피해** +(미정). 원래 공격은 정상적으로 내성을 적용하고, 그 위에 따로 더한다(내성 무시 예외를 만들지 않는다) |
| 4 | 이 스킬의 공격으로 상대 흐트러짐을 0으로 만들어 **창을 직접 열면** 살수의 감 +(미정). 이미 창이 열린 대상은 해당 없음 | 상대에게 디버프가 **2개 이상** 걸려 있고, 이 결투에서 유진이 **체력 피해 0 · 흐트러짐 피해 0**이면 살수의 감 +(미정). 봉인 자체는 필요 없다 | 이 스킬의 공격으로 대상 부위를 **직접 파괴하면**(처형 포함) 살수의 감 +(미정) |

- 빛 · 굴림 수 · 위력 · 표식량 · 살수의 감 획득량 전부 **(미정)**

### 18.9 미정
- 직업 아이템
- 에너지 회복 카드

---

## 19. 히후미(一二三) — 친치로 · 뼈 · 반격

**컨셉**: 살을 주고 뼈를 취한다(육참골단). 적의 공격을 피하지 않고 몸으로 받아낸 뒤, 받아낸 피해를 뼈로 응축해 되갚는다. **회피를 쓰지 않는다.**
- 소스 = 친치로(4.4). **대실패가 이득이 되는 유일한 캐릭터**
- 여성 · 일본. 어휘 계열 = 도박 + 육참골단(한자 병기: 육참 肉斬 · 골단 骨斷)
- 승리 경로 = **부위 파괴로 보스 슬롯을 봉쇄**하는 것. 오래 버틸수록 강하다(약점은 장기전이 아니라 단기전)

### 19.1 기본위력 — 히후미 전용 +1
**기본위력 = 곡선 + 빛 × 2 + 1.** 범위는 균등이 아니라 친치로 분포다(「일반 범위」와 아라시 · 시고로 「대박」이 따로 있다).

**완전 차단** — 그 합 동안 **체력 피해와 흐트러짐 피해를 한 번도 받지 않은 것.** 진 교환이 있었는지는 보지 않는다

**도발 N/T**(19.10 참고 — 공용 규칙으로 승격할지 미정, 우선 히후미 전용으로 적는다) — 도발된 부위는 현재 도발 N이 높은 순서대로 처리한다. 각 부위는 최대 N개의 적 공격 행동/슬롯을 해당 부위로 강제 유도한다.
- 공격 행동/슬롯 단위이며 굴림 단위가 아니다
- 적 공격은 속도 높은 순서로 가져온다. 도발 N 동률·적 속도 동률은 무작위
- 높은 도발 부위가 먼저 자기 N개를 가져간 뒤, 다음 도발 부위가 남은 공격을 가져간다. 남은 공격은 원래 타겟을 유지한다
- 공격 불가능한 부위는 그 시점에 유도 효과가 발생하지 않는다

### 19.2 뼈 — 자기 자원
- **피격 적립 = 실제로 받은 피해만큼 뼈를 얻는 히후미의 기본 뼈 획득 방식**(방어도 · 뎀감 · 보호가 적용된 뒤의 값 1 : 1). 그래서 방어는 뼈를 깎는다
  - 카드가 뼈 N을 명시적으로 직접 지급하는 것(직접 지급)은 피격 적립이 아니다. 공통 대실패의 고정 뼈 60도 직접 지급이라 피격 적립이 아니다
- **핏값 N/T** — 피격 적립으로 얻는 뼈가 N만큼 증가한다(예: 실제 피해 20 · 핏값 8 → 피격 적립 28). **직접 지급에는 적용하지 않는다.** 히후미 전용 키워드(13.3)
- **상한 500 · 한 구간 = 100 (1~5구간) · 만개 = 500**
- 뼈 100당 반격 위력 +1(최대 +5) — 19.3의 반격 위력 공식과 같은 것을 가리킨다

**가불 — 그 턴만 쓰는 임시 지불 능력**

실제 뼈와 **따로 보관한다.** 실제 뼈 = 상처의 축적이자 구간 계산 자원 · 가불 = 그 턴에만 쓸 수 있는 지불 능력.

| | 실제 뼈 | 가불 |
|---|---|---|
| 저장 상한 500 | 걸린다 | **안 걸린다**(별도 보관) |
| 뼈 구간 계산 | 쓴다 | **안 쓴다** |
| 뼈 보유 조건 · 뼈 비용 지불 | 쓴다 | 쓴다 |
| 만개 500 조건 | 쓴다 | 쓴다(실제 뼈 + 가불 합산) |
| 턴 종료 | 남는다 | **사라진다** |

- **비용은 가불부터 낸다** — 모자란 만큼만 실제 뼈에서 뺀다
- 골단의 `20 − 뼈 구간 × 4`는 **실제 뼈만** 본다
- 가불을 실제 뼈로 바꾸는 순간에만 상한 500이 걸린다 — **넘치는 양은 사라진다**
- 용도 제한은 없다 — 무엇에 쓰든 플레이어가 정한다

### 19.3 반격 — 「적립 → 합 종료 후 한 번」
- **「(반격)」 태그가 붙은 스킬로 진 교환마다 반격 굴림 +1을 적립**하고, **합이 끝나면 모인 굴림으로 즉시 반격**한다. 반격 굴림 수 = 진 교환 수(base 0)
- 반격 굴림은 맞부딪힐 적 굴림이 없으므로 **전부 무저항 타격.** 무저항이라 곡선 아래로 누른다
  - **결투 반격 = 8 + 뼈 구간 × 1 + 1(히후미 전용) + 친치로 값** / **평타 반격 = 4 + 1 + 친치로 값**(구간 보너스 없음)
- **대실패는 어디서 나오든 같다** — 그 굴림 확정 실패 · 자해 60(체력 피해) · 뼈 60(직접 지급, 핏값 미적용) · 반격 굴림 +1. 자해와 뼈 60은 서로 다른 두 효과다 — 자해가 피격 적립으로 이어져 뼈를 또 만들지 않는다
  - **(반격) 태그 스킬이 대실패로 교환까지 지면 둘 다 적립된다** — 대실패 +1, 태그 패배 +1
  - **반격 굴림에서 또 대실패가 나오면** 반격 굴림 +1이 새로 생기고, 지금 처리 중인 반격이 끝난 뒤 이어서 즉시 해소한다
  - **예외** — 카드가 대실패 결과를 직접 다시 정의하면 카드 문구가 우선한다(결투 H 「운명을 흔들다」)
- 결투의 반격은 **결투 대 결투 게이트 안**이다

| 태그 | 스킬 |
|---|---|
| 결투(반격) | 결투 A 「육참」 · 결투 B 「골단」 · 결투 J 「굴림 도감」 |
| 일반(반격) | 평타① 「푼돈 걸기」 |
| 없음 | 결투 C 「무모한 베팅」 · 평타② · 앞으로 만들 결투 |

**스킬별 반격 효과**

| 스킬 | 반격 효과 |
|---|---|
| 결투 A · 육참 | 반격 발동 시 **뼈 +(구간 + 1) × 20** → 구간1 40 / 2 60 / 3 80 / 4 100 |
| 결투 B · 골단 | **만개 반격 전용** — 만개(실제 뼈 + 가불 500)일 때: **회복 350 + 조건부 파괴(합당 1회) + 바 +25.** **반격이 터지면 뼈 전량 소모**(만개든 아니든). **만개 시 반격 굴림 수 ×2** |
| 평타① (반격) | 되갚음(딜)만. **뼈 무소모 · 구간 효과 없음 · 게이트 없음** |

- 골단 만개 반격의 파괴는 **약화 선행 게이트를 면제받는다**(약화 선행 예외 — 처형과 함께 둘뿐이다)

### 19.4 패시브 — 「몸을 버려야 떠오를 여울도 있다」
1. **열세 · 짓눌림 구간에서 턴을 끝내도 고조 +3 칸** (다른 캐릭터는 0)

### 19.5 결투
결투 풀은 11자리 · 3트리(육참골단 / 과부화 / 무난). 트리 이름 「과부화」는 대가를 치르고 크게 얻는 카드의 분류명이며, 과부화 계열의 효과 중 슬롯을 사용할 수 없게 되는 것을 「봉인」이라 부른다(11.4).

| 자리 | 이름 | 굴림·빛 | 기본위력 | 일반 범위 · 대박 | 효과 |
|---|---|---|---|---|---|
| A | **육참** | 4·1 | 12 | 13~18 · 30 | (반격). 져서 뼈를 캐는 상시 연료 엔진 |
| B | **골단** | 2·1 | 20 | 21~26 · 38 | (반격). **본체 위력 20 − (뼈 구간 × 4)** · 만개 반격 전용 방출기 · **만개 시 반격 굴림 수 ×2** |
| C | **무모한 베팅** | 5·2 | 13 | 14~19 · 31 | **사용시** 실제 뼈 + 가불이 문턱 (미정) 미만이면 → **가불 (미정) 획득** + 다음 턴 균열 (미정)만큼 1턴 / 문턱 이상이면 → **뼈 (미정) 지불**(가불 우선), 추가 페널티 없음 |
| E | **판돈을 올리다** | 2 · 0(시작) | (미정) · **전투 내내 고정** | (미정) | 태그 없음 · 과부화. **교환 승리시** 위력 +1(전투 내내 누적, 전투 종료 시 리셋) · **교환 패배시** 뼈 (미정) 지불 시도(가불 우선 · 부분 지불 없음) → 지불 성공하면 그걸로 끝, **지불 실패하면** 이 카드의 빛 비용 +1(전투 내내 누적) → **비용이 4에 도달하면 그 이후 카드 자체를 못 낸다.** 비용이 올라도 기본위력을 공식으로 다시 계산하지 않는다 — 지는 것이 위력 상승으로 이어지지 않게 한다 |
| G | **곱절을 노리다**(임시명) | 3 · (미정) | (미정) | (미정) | 태그 없음 · 무난. **각 굴림 직전** 고정 뼈 K(미정)를 건다 → **승리시** 뼈 3K 획득(순이익 +2K) / **패배시** K 소실. 굴림 단위 독립 정산 · 보유 뼈가 K 미만이면 그 굴림은 배팅 없이 정상 판정 |
| H | **운명을 흔들다**(임시명) | 2 · (미정) | (미정) | (미정) | 태그 없음. 기본위력은 같은 굴림 수의 일반 결투보다 **낮게** 잡는다. **각 굴림의 친치로 결과가 판정을 대신한다** — 목 · 꽝 = 정상 판정 / **시고로 = 그 굴림 자동 승리** / **아라시 = 그 굴림 자동 승리 + 상대의 다음 굴림 1개 무효화** / **대실패 = 그 굴림 위력 0**(이 카드는 공통 대실패 규칙을 덮어쓴다 — 자동 패배가 아니라 위력 0으로 정상 비교하며, 자해 60 · 뼈 60 · 반격 굴림 +1이 없다) |
| I | **몸에 새기다**(임시명) | 1 → 4 · 0 / 0 / 1 / 1 | 1굴림 **15**(곡선 예외) · 이후 (미정) | (미정) | **런 전체에 걸쳐 영구 성장**(전투를 넘어 유지). 1굴림으로 시작해 굴림 4개까지 자란다 — 아래 표 |
| J | **굴림 도감**(임시명) | 3 · 2 | (미정) | (미정) | **BLUE** · (반격). 친치로 결과별 보너스의 정확한 처리 시점은 **(미정)** — 구 `매칭시` 트리거는 폐기. 아라시 1~2눈 = 그 굴림 위력 +(미정) / 아라시 3~4눈 = 방어도 (미정) / 아라시 5~6눈 = 실제 뼈 (미정) 직접 획득 / 시고로 = 기세 바 +(미정) / 대실패 = 공통 대실패 규칙 적용 + 이번 합 반격의 **모든 굴림 위력 +1**(누적) |
| L | **도발형**(임시명) | 2 · (미정) | (미정) | (미정) | **BLUE** · 태그 없음. **사용시** 내 부위 하나에 **다음 턴** 도발 (미정)만큼 1턴 예약 · 굴림 **승리시** 다음 턴 **견고** (미정)만큼 1턴 · **핏값** (미정)만큼 1턴 |
| M | **전체 정화형**(임시명) | (미정) | (미정) | (미정) | 태그 없음. **사용시** 뼈 (미정)를 크게 치른다(가불 우선) → **자신에게 걸린 부정적 효과를 전부 제거**한다(긍정 효과는 유지, 부위형 상태는 대상이 아니다) |
| K | **판을 엎다**(임시명) | 3 · (미정) | (미정) | (미정) | RED · 태그 없음 · 과부화. **사용시** 기세 바가 **양수일 때만** 그 값을 전량 소모하고 바를 0으로 만든다(소모량 X) — 0 이하면 아무 일도 일어나지 않는다. 1굴림 위력이 X에 비례 · 2굴림 뼈 획득이 X에 비례(승패 무관 확정) · 3굴림 **승리시** 다음 턴 골절 (미정)만큼 1턴(고정, X와 무관) · **대가** 자신에게 침체 (미정)만큼 (미정)턴 — **사용시 뼈 (미정)를 치르면**(가불 우선) **대가를 면제**한다 |

- **자기 거버너** — 결투에서 이긴 교환마다 자신의 **다음 턴 결투 위력 감소**(1턴 · 비누적, 감소량 미정). 이기기만 해서 뼈가 안 쌓이는 모순을 막는다

**결투 I 「몸에 새기다」 — 두 축으로 자란다**

1굴림으로 시작해, 교환에서 **이기면 승리 축 · 지면 패배 축**에 적립한다. **먼저 임계치에 닿은 축이 트리를 확정**하고(확정 뒤에는 그 축만 적립한다) 2 → 3 → 4굴림으로 자란다. 1~3굴림은 **턴당 1회**, 4굴림은 **턴당 2회** 쓴다. 단계별 임계치 **(미정)**.

| 단계 | 승리 축 | 패배 축 |
|---|---|---|
| 1굴림 | 공통 시작 — (반격) | 공통 시작 — (반격) |
| 2굴림 | **(반격) 태그 제거** · 모든 굴림을 이기면 가불 (미정) | (반격) 유지 · 진 굴림마다 뼈 (미정) |
| 3굴림 | 이긴 굴림마다 가불 (미정) | 처음 두 번의 패배마다 뼈 (미정) · **세 굴림 전부 패배**하면 세 번째 보상 대신 **반격 굴림 +1** |
| 4굴림 | **「체득」** — 절단 · 태그 없음 · 이긴 굴림마다 가불 (미정) · **완전 차단**이면 그 시점의 가불을 전부 실제 뼈로 바꾼다 | (반격) 유지 · 1패 견고 (미정)만큼 1턴 · 2 · 3패 뼈 (미정) · 4패 없음 · **네 굴림 전부 패배**하면 **반격 굴림 +1** · 그 시점에 실제 뼈 + 가불이 500이면 이번 반격을 **만개로 발동할 수 있다**(만개 문턱을 우회하지 않는다 · 일반 만개와 같이 자원을 소비한다) · 뼈 (미정)를 치르면 이번 반격의 모든 굴림 위력 +1 |

- 굴림 수는 **전투 밖(성장 시점)에** 바뀐다. 판정 도중 굴림 수가 변하는 일은 없다
- 성장 상태(현재 단계 · 축 · 적립량)는 **런이 끝날 때까지 유지**된다

- 트리 — 육참골단 A · B · I / 과부화 C · E · K / 무난 G · H · J. **L · M의 트리 배분 (미정)**
- **핏값**은 19.2에서 정의한 히후미 전용 키워드다(13.3) — 피격 적립에만 거는 가산 효과, 직접 지급에는 안 걸린다

### 19.6 평타
| 자리 | 이름 | 굴림·빛 | 기본위력 | 효과 |
|---|---|---|---|---|
| ① | **푼돈 걸기** | 2·0 | 12 (미정) | (반격). 빠른 기본 탈출 — 한 대 맞고 넘긴다 |
| ② | **과감한 판단** | 3·1 | 14 | **사용시** 뼈 (미정) 지불(가불 우선) → 지불 실패 시 처리 **(미정)**(직접 뼈 지급 대체 여부 판단 필요) · 다음 턴 속도 −1 / **합 종료시** 완전 차단이면 **가불 (미정) 획득** |
| ③ | **동귀어진** | (미정) | (미정) | 태그 없음. **사용시** 자신에게 고정 피해 N(실제 피해라 **피격 적립** 발동, 핏값 적용) + 상대에게 같은 값(N) 고정 피해. 판정 없이 확정으로 서로 맞바꾼다. 자기 피해는 이 카드를 쓴 슬롯의 부위에 들어간다 |
| ④ | **안정형 필러**(가칭) | 3·1 | (미정) | (반격). 추가 효과 없음 — 폭이 좁은 안정적인 반격형 |
| ⑤ | **승패 양쪽 보상형**(가칭) | 2·1 | (미정) | (반격). 굴림 **승리시** 열기 (미정)만큼 1턴 즉시 부여 / 굴림 **패배시** 보호 (미정)만큼 1턴 즉시 부여(다음 턴 예약 아님) |
| ⑥ | **기세 소비형**(가칭) | 2 | (미정) | (반격). **사용시** 기세 바 −K(미정) → 이번 판정 위력 +N(미정). 현재 기세 값·지불 가능 여부와 무관하게 항상 발동, 기세가 더 내려갈 수 있다 |
| ⑦ | **정화 A**(가칭) | 2·1 | (미정) | 태그 없음. **사용시** 뼈 (미정) 소모(가불 우선) → 자신의 **부정적 효과 중 무작위 1종 제거**(긍정 효과는 대상 아님). 제거할 게 없으면 대신 **가불 (미정) 획득** |
| ⑧ | **자기 과부화형**(가칭) | 3 | (미정) | 태그 없음. **사용시** 뼈(가불 우선)로 비용 (미정)을 지불 가능하면 → 뼈 지불 + 이번 판정 위력 +M(미정) / 지불 불가능하면 → **이 슬롯 다음 턴 봉인 1턴**(11.4) + 이번 판정 위력 +M(미정) + **가불 (미정) 획득** |
| ⑨ | **골절 부여형**(가칭) | 2·1 | (미정) | 태그 없음. 승리시 상대에게 다음 턴 골절 (미정)만큼 1턴. **굴림별 발동인지 카드 전체 1회인지 (미정)** |
| ⑩ | **당첨형**(가칭) | 2·0 | (미정) | 태그 없음. 각 굴림에서 아라시 또는 시고로가 나오면 그 즉시 빛 +1. 다른 결과는 추가 효과 없음 |
| ⑪ | **빚지기**(올라프에서 이관) | (미정) | (미정) | 태그 없음. **사용시** 빛 +1 즉시 · **다음 턴 시작**에 뼈 (미정)을 지불할 수 있다 → 내면 페널티 없음 / 안 내거나 모자라면 그 턴 최대 빛 −1. 가불은 턴 종료에 사라지므로 이 다음 턴 비용에는 못 쓴다 |

- 히후미 평타에는 잔효과 키워드가 없다(끈 없는 깨끗한 슬롯). 평타 강화는 위력으로 수렴

### 19.7 도사림
| 이름 | 분류 · 빛 | 효과 |
|---|---|---|
| 웅크리기 · 노려보기 | 공용 | 8장 참고 |
| A **포커페이스** | 강한 · 1 | 이번 턴 받는 피해를 피격마다 (미정) 감소 · 이번 턴 핏값 (미정)만큼 1턴 |
| B **뼈에 새긴다** | 강한 · 1 | 이번 턴 **실제 교환마다**(승패 무관, 일방타격 제외) 뼈 (미정) 직접 획득. 피격 적립이 아니라 핏값 미적용 |
| C **살은 내주지** | 강한 · 1 | **사용시** 실제 뼈 (미정) 직접 획득 · 다음 턴 자신에게 균열 (미정)만큼 1턴 예약(직접 지급이지 가불 아님) |
| **이 판 접지**(환전) | 강한 · 0 | 뼈 (미정) 지불(가불 우선, 예외 없음) → 즉시 체력 (미정) 회복 (위급 전용 비상 밸브) |
| D **친치로 안정 상승형** | 강한 · (미정) | **사용시** 이번 턴 자신의 모든 친치로 판정(일반 스킬·반격·올인 등)에서 시고로 확률 +(미정)%p · 아라시 확률 +(미정)%p |
| **친치로 양극화형**(가칭) | 강한 · (미정) | D와 별개 카드. **사용시** 이번 턴 아라시 확률 +(미정)%p · 대실패 확률 +(미정)%p(시고로 확률은 그대로, 늘어난 만큼 일반 결과 확률이 줄어든다) |
| E **반격 위력 강화형**(가칭) | 강한 · (미정) | **사용시** 이번 턴 발생하는 모든 반격 굴림 위력 +(미정)(반격 굴림 수는 안 늘어난다). 반격이 안 나오면 효과 소멸 |
| F **균열 + 핏값**(가칭) | 강한 · (미정) | **사용시** 자신에게 이번 턴 균열 (미정)만큼 1턴 + 이번 턴 핏값 (미정)만큼 1턴. 포커페이스보다 위험한 대형 피격 적립 카드 |
| **상호 쇠약형**(가칭) | 강한 · (미정) | **사용시** 자신에게 쇠약 (미정)만큼 (미정)턴 + **동시에 상대에게도 같은 값**의 쇠약 (미정)만큼 (미정)턴(특정 부위 조준 없음, 피해 없음) |
| **기세 바 태우기형**(가칭) | 강한 · (미정) | **사용시** 기세 바 −(미정)(0 리셋이 아니라 현재 기세에서 그대로 감소) + 이번 턴 핏값 (미정)만큼 1턴 |

- 도사림 A · B · C는 **전투당 1~2회 사용**에서 값이 난다. 매 턴 쓰면 붕괴한다
- 감정 「신의」와 역행(방어도가 뼈를 깎음) — 포커페이스가 게임 안의 답이다

### 19.8 위세 3형
| 형 | 이름 | 임계 | 효과 |
|---|---|---|---|
| 1 | **도박수** | 50 | 쌓인 뼈(**실제 뼈 + 가불**, 가불부터 전량 소모 후 실제 뼈)를 전량 태우고, **태운 뼈 100당 그 턴 모든 슬롯 위력 +6** |
| 2 | **올인** | 50 | 쌓인 뼈(**실제 뼈 + 가불**)를 **전량** 걸고 친치로를 3번 굴린다. 하나라도 **대실패**면 그 즉시 뼈 0(다른 결과보다 우선) · 아니면 셋 중 **최고 결과**로 배당한다 — 아라시 · 시고로 = **뼈 500** / 목 = **걸었던 뼈 + 100** / 꽝 = **뼈 0** |
| 3 | **속임수** | 65 | 그 턴 히후미의 모든 친치로 굴림을 「아라시」 또는 「대실패」로 고정 |

- 위세 게이지는 공통 규칙(9장)을 그대로 쓴다. 뼈와 위세 게이지는 별개다
- 형1은 뼈를 태우므로 골단 만개 반격과 같은 뼈를 두고 경쟁한다

### 19.9 굴림 타입 배정 (절단 / 관통 / 타격)
| 슬롯 | 굴림 | 구성 | 성격 |
|---|---|---|---|
| 결투 A 육참 | 4 | 2 / 1 / 1 | 안정 — 어떤 적에게도 최악 ×1.0 |
| 결투 B 골단 | 2 | 2 / 0 / 0 | 몰빵 |
| 결투 C 무모한 베팅 | 5 | 0 / 5 / 0 | 완전 몰빵 |
| 결투 E 판돈을 올리다 | 2 | (미정) | (미정) |
| 결투 G 곱절을 노리다 | 3 | (미정) | (미정) |
| 결투 H 운명을 흔들다 | 2 | (미정) | (미정) |
| 결투 I 몸에 새기다 | 1 → 4 (성장형) | 전 단계 **절단** | 육참골단 |
| 결투 J 굴림 도감 | 3 | (미정) | (미정) |
| 결투 K 판을 엎다 | 3 | (미정) | (미정) |
| 결투 L 도발형 | 2 | (미정) | (미정) |
| 결투 M 전체 정화형 | (미정) | (미정) | (미정) |
| 평타① | 2 | 0 / 0 / 2 | 몰빵 |
| 평타② | 3 | 1 / 2 / 0 | 관통 편향 |
| 평타③ 동귀어진 | (미정) | (미정) | (미정) |

트리 주 타입 — 육참골단 = 절단 / 과부화 = 관통 / 무난 = 타격. **엔진은 늘 돌고 나머지가 판을 건다.** 유진이 무기를 바꿔 대응한다면 히후미는 슬롯을 골라 대응한다.

### 19.10 미정
- 스킬 색(RED / BLUE) 배정
- 평타 · 결투 · 도사림 풀 확충 · 직업 아이템
- **도발(19.1)을 공용 규칙(8장 이전 · 1.5장 근처)으로 승격할지 히후미 전용으로 둘지** — 유진 「은신」(18.7)이 대칭 메커니즘이라 같이 검토할 것. 은신은 09-23 재설계로 **턴이 아니라 행동 단위로 줄고, 페이오프가 은신 부위를 골라 쓰는** 형태가 되어 「부위 배정이 매 턴 다시 정해진다」는 문제를 비켜갔다. 도발도 같은 방식으로 풀 수 있는지 볼 것

---

