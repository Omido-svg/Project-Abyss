using UnityEngine;

/// <summary>
/// Verification sandbox가 Production BattleRuntimeFactory의 coroutine host 계약을
/// 실제 런타임과 동일하게 만족시키기 위한 최소 MonoBehaviour.
/// </summary>
public sealed class GameSystemVerificationCoroutineHost : MonoBehaviour
{
}
